# 消息与小事簿修复：本地修改与验证记录

最新状态：本地代码修改与自动回归已完成，未提交、未推送、未部署服务器、未打包、未做真机验证。用户要求等待明确授权再打包。以下保留早期排查及暂停历史，当前结论以末尾收尾章节为准。不得作为已发布版本或真机验收结果。

## 用户确认的范围

- 主动消息偶发语义重复，现有后台大部分正常。不能凭一张截图全局增加模糊拦截。
- X 出现 A 应发的内容由 B 发出的现象；用户说可能是定时自动发，不确定。尚未复现当次现场。
- 小事簿只主要记录有意义的情绪变化与已经答应的约定；普通身体、睡眠、工作琐事不单独记录。由角色自己的视角组织总结，不照抄用户的话，不能声称是用户亲手写的。
- 新增默认关闭的原文输出实验开关。开启时合法功能标签照常执行，其余正文不因格式或重复拦截。涵盖普通聊天、线下、共同生活、电话。关闭保留旧处理。接口没有正文仍无内容可显示。
- 权限、真实设备操作授权、付款校验、过期会话及后台任务 ID 幂等不属于正文格式拦截，不放开。

## 已定位证据

- 旧 lifeNoteOnUserMsg 按关键词取用户整句，交给 lifeNoteAdd 改代词并截短存储。这能解释截图中的碎句与照抄问题。
- roleTweetGenerate 的独立自动发帖未明确指定角色模型路线，可能受到当前页面模型选择影响。已加角色路线及独立请求标志，但不能据此断言当次 A/B 错位根因。
- 主动消息的现有完成轮次、前后台接力及消息 ID 防重必须保留。本次仅补最近用户发言后角色已经说过的话，以及未变电量/时长不等于新话题的生成说明，不增加新的语义硬拦截。

## 本次改动文件

- app.js：小事簿总结标签、证据核验、记录来源及角色范围；X 独立模型路线和发布前归属校验；主动续聊上下文；设置及普通/线下/电话的原文显示分支。
- cohab-theater.js：配角输出实验分支，不按格式、重复或长度削掉正文；正常模式不变。
- native/private-small-phone/XcodeProject/PhoneCompanionTest/PhoneWeb.bundle/app.js 与 cohab-theater.js：对应私人网页逻辑，保留其已有独立实现。未整文件用网页覆盖私人实现。
- supabase/functions/phone-role-push/index.ts：后台接收实验配置并保留模型正文；应用锁定仍只接受合法结构化动作。仅本地代码，未部署。
- scripts/check_reply_output_experiment.cjs：完全隔离外网的浏览器模拟测试。

新增小事簿协议为 [小事簿|情绪或约定|本轮证据原句|角色自己的总结]。情绪证据只能来自用户，角色当轮承诺可来自角色可见回复；证据仅用于核验，保存总结和证据归一化键。旧记录保留，不自动重写、删除。该策略仍需完成更广的语义误判和消息时序测试。

## 已完成验证与未完成验证

- 两份 app.js 语法检查通过；已改文件 git diff --check 没有空白错误，仅 LF/CRLF 提示。
- .qa/reply-fixes-browser.log：网页及私人网页各 6 项浏览器模拟结果通过（合计 12 项）：普通模式、开启实验、重新关闭；小事簿情绪/角色承诺正例与原句/愿望/普通琐事反例；共同生活保留重复和未知标签；电话保留重复和未知标签。两份页面均未报 pageerror。
- 浏览器测试使用本地假模型返回，没有调用真实模型或声称真机 TTS 已验证。
- 全量旧测试第一轮 1591 项中 1561 通过、30 失败，不是可交付结果。部分为新增参数/分支导致旧源码匹配与孤立函数 mock 失效，部分为同期私人原生版本变更。第二轮进行时私人资源还在变化，不能作为稳定回归结论。
- 仍需完善旧测试依赖与断言、新增 X 并发角色归属/删角色/切账号运行时测试、后台实验开关同步及合法标签运行时测试、单次约会与多人剧场实页回归、设置持久化和所有功能标签检查。
- 注意：模型标签合法执行、内容原样保留和真实功能结果不能混为一谈；原文模式不能跳过真实操作的权限或把失败操作报告为成功。

## 暂停原因：另一个过程正在修改同一私人包

本轮起始工作区为空，随后出现本次未修改的 HomeKitLockBridge.swift、PhoneNativeBridge.swift、Info.plist；再出现 project.pbxproj、LocalPhoneWebView.swift、私人 HTML、manifest、staging 脚本和 328-a100-lock 版本标记变化。随后私人 PhoneWeb.bundle/games/pixel-home 整个目录在 git status 中显示删除。

以上原生/打包/资源改动不是本次消息修复所做，未撤销、未恢复、未纳入提交。因为与私人包交付入口和版本状态重叠，停止继续修改私人包与版本文件，等待用户协调正在运行的其他任务。

继续前必须先核对 git status、私人资源与两个 app.js 的实际内容，确认其他任务是否结束，以及本次改动是否被 staging 覆盖。不能直接 rerun .qa 下从 HEAD 生成的早期补丁脚本；它们仅是一次性编辑辅助，重复运行可能覆盖后来改动。

尚未合并到三份维护 DOCX：项目说明需记载功能范围；Bug 记录需记载证据、未确认根因、测试和失败尝试；修改规范需记录独立角色请求归属、原文开关的权限边界和并行打包冲突。交接短 TXT 也尚未更新为完成状态。

## 2026年9月8日 继续修改后的收尾

用户确认先修改完，等明确说可以打包后再打包。已核对私人 app.js 与 cohab-theater.js 恢复基线的现场，仅补回本次消息逻辑，保留私人性能、启动、数据恢复代码以及其他任务的原生修改；没有整文件覆盖、重建资源包或操作版本号。

新设置入口：设置 → 角色、时间与消息 → 模型原文输出（实验），默认关闭。开启时不以格式、措辞、旁白、重复或气泡长度丢弃普通角色正文；合法功能标签照常执行，接口未返回的内容仍无法显示。关闭恢复旧分支。单独的共同生活查手机后正文重试也接入开关，读取事实与权限不放开。

小事簿进一步限制为情绪和已经答应的约定：不再关键词搬运整句，不采集普通日常流水账；约定区分用户与角色是谁答应，不收愿望和问句。角色候选承诺只有实际进入该角色对话后才允许保存，未显示或过期请求的承诺不提前落地。作者、角色及账号范围明确，旧记录不改写或删除。情绪与语义判断仍需实际模型使用观察，不能说词法证据核验等于完全理解。

验证：当前全仓 Node 测试 1613 项，1613 通过、0 失败；其中新增本次运行时测试 12 项。两份 app.js 语法检查通过，git diff --check 没有空白错误（仅 Git 的 LF/CRLF 提示）。浏览器实页模拟共 20 项通过，网页与私人入口各 10 项，均无 pageerror；覆盖微信默认/开/关、小事簿、共同生活、电话字幕、合法发推/来电与内心标签、单次约会、7 条重复长配角正文、设置保存后普通 URL 重启恢复。所有请求均为隔离外网的测试夹具，没有使用用户密钥或真实云端。报告位于忽略的 .qa/reply-fixes-current.xml、.qa/reply-fixes-browser-expanded.log、.qa/reply-targeted.log，运行入口为 scripts/check_reply_output_experiment.cjs 与 tests/reply-output-experiment.test.mjs。

三份维护 DOCX 已追加“2026年9月8日 消息与小事簿修复 本地未发布”章节，分别记录功能、证据/失败尝试、工程规则；原有 1594 / 1776 / 1366 段内容逐项验证保留。渲染器实际尝试失败：当前环境缺少 soffice.exe，因此只完成结构与历史保留验证，未声称逐页视觉验收。

补充接力回归：scripts/check_v1200_handoff.cjs 增加可选 REPLY_RAW=1，默认原行为不变。开启实验时网页/私人各覆盖后台先回、本地先回、落盘失败后重试、普通定时消息和新用户轮次共 10 个场景，全部通过（.qa/reply-handoff-raw.log）。同任务/同回复 ID 仍然只落一次，保存失败先不 ACK，新用户轮次绑定仍正确。连同前述 20 项，实页模拟共 30 个通过场景。

发布前还须完成：用户明确授权后，检查并行原生任务最终资源，重新分配唯一版本、同步资源清单与两个私人入口，另行部署 phone-role-push 才能影响云端定时生成。真实模型是否减少今天那类主动语义重复、X 当次归属事件的唯一根因、真实 iPhone/电话音频均尚未验证。当前不提交混合工作区、不推送、不部署、不打包。
