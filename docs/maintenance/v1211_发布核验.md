# v1211 发布核验

用户于2026年9月8日确认：已完成的两边共有功能推送网页并进入私人包；智能家居新改动仅私人版。私人后台模型拒绝说明隔离继续按单独授权仅私人生效，未完成的快捷指令不发布。

候选身份：网页及私人内置网页 v1211，私人 iOS 1.0.336（336），原生桥36。线上实际结果和包 SHA256 以父目录《发布结果_v1211_2026-09-08.md》为准，本文件不将发布前检查冒充线上完成。

| 用户要求 | 代码与入口 | 网页 | 私人包 | 结果和测试 |
| --- | --- | --- | --- | --- |
| 每条消息北京时间 | message-beijing-time.js；设置开关；app.js/cohab-theater.js 气泡渲染 | 包含，默认关 | 包含，默认关 | UTC+8、跨日、双方气泡、未知时间、关闭回退和持久化测试通过 |
| 后台明确 AI 拒绝不当作角色 | 私人 app.js profile/收件/状态；phone-role-push 严格私人标记 | 不启用新规则 | 包含 | 截图旧代码复现；开关两态、动作不执行、诊断保存重载、正常角色拒绝保留；不绕过安全拒绝 |
| 智能家居新改动 | 私人 Resources/Web 和 PhoneWeb.bundle 的 private-smart-lock.js/css | 不引入；smart-home.js 与旧提交一致 | 包含 | SVG 图标对齐；仅解锁事件通知角色；保留认证和真实回读；自动测试通过，角色解锁尚未真机验证 |
| 保留私人卡顿修复 | 私人 app.js 与原生容器的既有任务队列 | 不引入 | 保留 | 边界测试及普通聊天门禁通过 |
| 防止聊天中断 | 两份 app.js 的 myActivity/buildSystem 既有修复 | 保留 | 保留 | 授权与未授权、旧存档、原文模式、输入与让TA回、自动查看和重载通过 |
| 快捷指令锁屏执行 | 独立 SQL 和 core.ts 草稿 | 排除 | 排除 | 未完成；不执行迁移，不部署新函数 |

完整 Node 测试1636/1636通过。两边核心聊天16组合及恢复、原文多场景、本地真实后台收件和北京时间可见性检查通过。测试仅模拟模型 HTTP、后台 RPC 与私人容器能力，没有调用用户真实模型或实际门锁。

版本批量替换首次误及335色相、花束0.335比例及对应测试，已恢复原数值，改为只匹配版本字段/资源参数。公开及私人原有 smart-home.js、gift-effects.js 与基线不产生业务差异。首次新身份回归34项失败包含旧版标题断言、guide尚未更新及私人资源源副本身份不一致；逐项修正身份和历史路径后全量通过，不改业务断言掩盖失败。

后台部署前两个CLI查询列出版本28，而旧记录写版本29；已下载该项目线上phone-role-push源码，统一换行后与HEAD基线完全相同，未发现未合入远端逻辑。仅部署phone-role-push，维持verify_jwt=false及函数内部鉴权；不部署phone-smart-home等其他函数，不执行任何数据库迁移。部署后另下载源码比较，不单凭函数列表版本下结论。

打包来自已提交树；工作区允许保留仅两份已点名的未跟踪快捷指令草稿，二者不进入提交、静态网页包或私人包。检查私人 ignored-but-required 的北京时间模块已显式跟踪。包内双入口一致、共享JS/CSS及游戏素材齐全、最新门锁源副本一致、私人策略不入网页，归档后逐文件比对和SHA256核验。

Windows没有Mac/Xcode签名编译与真实iPhone验收能力。包仅Mac待编译源码候选，不是IPA；角色发起解锁仍待设备日志和实测确认，不能因为本次图标/事件改动就声称解决。旧App、旧包、旧工程和用户数据均保留。
