from pathlib import Path
from docx import Document

ROOT = Path(__file__).resolve().parents[1]
SECTIONS = {
    'AI开发项目_项目说明文档.docx': [
        '范围：两边共有。网页源码和私人内置页 v1190，私人 iOS 1.0.316 (316)，原生桥 35。网页不推送；私人包仅为 Mac 待编译源码候选。',
        '情侣空间权限页新增默认关闭的聊天监管与软件监管。软件名单默认空，由用户选择。聊天连续前台停留随机 30–60 秒，软件随机 20–60 秒；每次进入最多一轮，退出重进独立计时。短暂进入只计本日次数，不通知角色。设备当地午夜清零，后台挂起时恢复后清理。',
        '只观察小手机内部页面与真实参与者身份，不读取被观察角色的聊天内容、草稿或游戏正文。伴侣自己的聊天和正在与伴侣共同参与的活动不触发。角色使用自己的模型路线、基础人设及双方已有上下文自然发微信；不把通知文本伪装为角色回复。',
        '角色忙于正常回复时等待空闲，页面/账号/权限变化则取消。模型生成结束后再次检查授权、页面、参与者和双方消息，避免关闭后仍发送。模型失败或非法输出保持静默，不新增系统提示，不自动重试。',
        '入口与脚本：公共小手机.html及私人两个HTML入口加载 couple-watch.js、couple-watch-runtime.js；Service Worker缓存两个新脚本；原生资源清单同步包含。保留v1189私人键盘布局与旁白拦截，不修改微信输入或原生键盘行为。',
    ],
    'AI开发项目_Bug记录模板.docx': [
        '本节记录新增功能风险，不把推测写成已复现的Bug。风险包括短停留累加误报、重绘重计数、共同活动误报、关闭后迟到回复、模型路线被当前游戏页面覆盖，以及只打包开关但缺少运行脚本。',
        '执行层防护：每次进入独立随机阈值；页面离开、后台、锁屏、换账号和解绑清除会话；每日只保存计数；当前活动实际参与者包含伴侣时抑制。生成前后复核授权与页面；本轮聊天上下文变化时丢弃旧候选。',
        '新增测试使用可控时钟、随机数和模拟模型覆盖阈值上下界、短停留、重进、午夜、参与对象切换、多种伴侣游戏和影院排除，以及异步作废、默认关闭、名单过滤、不读取另一会话。旧测试的版本断言按新身份更新，业务断言保留。',
        '验证边界：Windows自动测试与源码包结构核对不能代替真实模型、Mac签名编译或真实iPhone验收。v1189共同生活键盘修复仍是未真机通过的候选，本轮不得提升其验收状态。',
    ],
    'AI开发项目_Bug修改规范.docx': [
        '停留监管必须基于前台连续会话，不能把多次短停留相加；重绘不算重新进入；同一软件内换子页面不算再次打开，参与对象变化必须重置等待。计数与连续停留分开，且按设备本地日期清零。',
        '只知道对象和软件名称的权限不得读取对方正文或草稿。角色身份必须来自当前活动结构化参与者，不允许用软件名称或曾选过的默认角色推断伴侣在场。',
        '异步生成必须在实际发送前复查开关、绑定对象、账号、前台页面、参与者及新消息。权限关闭、页面退出或上下文改变使旧结果永久失效；不自动补发或用固定台词代替失败模型。',
        '非当前页面角色发起的独立反应须明确使用该角色模型路线，不能被游戏页面的模型选择覆盖。公共与私人脚本保持同字节，新资源必须加入两端入口、离线缓存及私人资源清单。',
    ],
}
for name, paragraphs in SECTIONS.items():
    path = ROOT / 'docs/maintenance' / name
    doc = Document(path)
    title = '2026 09 06 v1190 情侣空间聊天与软件监管'
    if any(p.text == title for p in doc.paragraphs):
        raise RuntimeError('Entry already exists: ' + name)
    old_text = [p.text for p in doc.paragraphs]
    doc.add_page_break()
    doc.add_heading(title, level=1)
    for text in paragraphs:
        doc.add_paragraph(text)
    doc.save(path)
    assert [p.text for p in Document(path).paragraphs][:len(old_text)] == old_text
    print('Appended, preserved previous paragraphs:', name)
