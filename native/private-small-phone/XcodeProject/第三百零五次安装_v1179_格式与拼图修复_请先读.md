# 第三百零五次安装：v1179 格式与拼图修复

本包对应网页与私人版共同版本：网页源码和私人内置页面均为 v1179，私人 iOS 为 1.0.305 (305)，原生桥 35。请完整解压到新的目录后再用 Xcode 打开，不要覆盖或混用旧工程、旧 ZIP 或旧 DerivedData 产物。

## 本次可见改动

- 语音、游戏邀请／接受邀请和内心格式能够恢复常见的括号、分隔符与语音时长变体，避免结构化内容掉成普通聊天文字。
- 能在本地恢复的内心格式不再触发第二次模型请求；真正完全缺失时仍使用现有真实模型补救，不制造固定角色内容。
- 像素拼拼乐只从上传图片创建图纸，支持重新调整识别精细度、1 至 100 格每轮额度、保存进度、保存退出、局部放大与全貌定位。
- 双方已填格采用一致边界，避免浅色格被白色描边误判；继续拼图后可以从画布返回游戏大厅，再返回小手机主屏。

## Mac 安装步骤

1. 不要先删除 iPhone 上现有的小手机 App，以免失去原数据容器。
2. 完整解压本次 ZIP 到一个全新目录，打开 `PhoneCompanionTest.xcodeproj`。
3. 在 Xcode 中确认各 Target 的 Version 为 `1.0.305`、Build 为 `305`，继续使用原来的 Team、Bundle Identifier、Automatic Signing、Provisioning Profile 和 Entitlements。
4. 执行 Product → Clean Build Folder，再连接原来的真实 iPhone，选择正确 Target 后直接 Run 覆盖安装。
5. 正常启动后确认设置页显示网页核心 `v1179`、私人 iOS `1.0.305 (305)`、原生桥 `35`。

## 真机重点验收

1. 分别让角色输出带时长语音、游戏邀请／接受邀请、标准和常见误括号内心格式，确认不会作为普通文字露出，能本地恢复的内心格式只调用模型一次。
2. 上传一张含浅色与深色的图片，切换至少两个精细度，确认格子数量和图纸立即变化。
3. 把每轮额度改为 1、10、100，确认双方各轮上限正确；缩放后双方格子边界一致，浅色格不会被白框误导。
4. 拼一部分后点保存进度并退出，再继续拼；从画布返回游戏大厅后继续返回小手机主屏，不能循环回画布。
5. 冒烟检查普通微信、主动消息、共同生活、视频通话、私人 HomeKit 和外卖入口没有退化。

本 ZIP 是 Windows 生成并完成源码、自动测试与包内结构核验的 Mac 待编译源码候选。它尚未经过本机 Mac/Xcode 编译、签名、真实 iPhone 安装、启动和触控验收，不能据此称为可运行真机交付。
