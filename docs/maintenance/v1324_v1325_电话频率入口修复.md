# v1324 与 v1325 电话频率入口和手机宽度修复

2026-09-26，用户反馈找不到电话频率。线上 v1322 实测经角色资料、联系人设置、资料与记忆、编辑可找到 DOM 字段，但 390px 视口中 c_callprob 的实际宽度为 0。此前测试只赋值并读回，没有验证可见尺寸与真实点击，不能证明用户能用。

网页和私人均改：角色编辑器的电话频率使用既有 vertical 标签布局，说明文字不再横向挤占输入框；角色资料右上角更多、聊天与主动页顶部新增聊天偏好/电话频率入口，弹窗编辑保存，只写当前角色 c.callProb，仍走既有主动来电、有限回拨及后台同步。没有修改全局 CSS，也未改变电话调度概率或音频链路。

scripts/check_role_call_preference_ui.cjs 通过实际联系人设置按钮、聊天与主动、电话频率、保存、编辑、保存角色资料完成交互；两端各320/390/430宽度，独立输入宽250/320/360px，编辑器输入宽264/334/374px，均可见可填且不溢出。保存0、72及另一角色35不变均通过。0页面错误。全量2572/2572通过，核心聊天两端回归通过。

版本网页v1324、私人v1325、iOS1.0.396（396）、桥41。上一包、旧主分支、旧逐条去重和备份修复保留；邀请码问题状态仍未解决。本包为Mac源码，Mac编译与真实iPhone未验证。维护DOCX只追加；本机缺LibreOffice，版式视觉验证仍不可用。
