# v1213 发布范围与核验

## 范围

| 用户要求 | 网页入口／代码 | 私人包 | 核验 |
| --- | --- | --- | --- |
| 公开 North 伴生迁移 | public-north-policy.js、public-north-runtime.js；情侣空间伴生页 | 同步共享模块，原生私人分支保留 | 旧报告、权限、角色隔离、稳定ID命令、实际页面 |
| 后台提示词与通知记录 | phone-role-push、公开云补齐诊断和接力函数 | 私人云同步工作器，共享历史按钮 | 公开能力白名单，历史主人鉴权 |
| 快捷指令两种模式 | phone-shortcuts.js 设置入口，phone-shortcuts 云工作器 | 相同入口和私有云独立规则 | 队列领取、去重、配额、撤销、加密、单次模型请求、收件持久化 |
| 像素少女长时间打不开 | pixel-home.js、assets/game/wardrobe、sw.js | 全部游戏资源一致 | 故障恢复、兼容回退、照片及衣柜存档、file协议 |
| 私人智能家居／性能修复 | 不迁移到网页 | 保留既有最新代码 | 产物排除／包含检查，Face ID 和真实回读保留 |

共享消费者：正常聊天 buildSystem、查手机、共同生活查看、主动消息上下文、伴生命令、后台接力及收件。没有改共享键盘、原生容器或用户模型设置。

## 公开边界

已核对公开 North 1.0（8）源代码及商店身份。支持步数、电量、屏幕时间、应用锁定/解锁/限额和用户授权定位/足迹；不支持手表健康和手动解锁事件。旧屏幕报告没有使用日/时区，显示“最近报告”而非猜测今天；0步存在未授权/未返回歧义。

公开云 lkhlyfpssmrjkkzhuzag 和私人云 qvuahlqimcfgeoetosnl 分开。公开只补充诊断/接力、通知历史和快捷指令表；私人只新增历史及快捷指令表。未改许可证、AI余额函数、用户数据或任何云密钥。公开唤醒函数保留下载的原源码，仅调整JWT网关以兼容publishable key，内部主人和命令鉴权仍在。

## 已做校验与限制

全仓Node、实际网页和私人壳分支核心聊天、公开伴生页面/权限/提示词/收件去重、游戏故障恢复、file协议；模型和设备响应均为隔离夹具。两个云数据库在事务回滚内验证领取、去重、配额和撤销，未留下测试规则。最终计数以发布结果记录为准。

快捷指令执行由Edge后台队列及既有一分钟云心跳负责；通知受iOS授权与调度影响。创建时资料为快照，修改人设、模型或清除记忆后须撤销旧动作并重建。没有真实付费模型、锁屏快捷指令、安卓故障机、Mac签名或iPhone安装验收，不能称私人App已真机可运行。

维护Word仅追加历史，结构复查；环境没有可用的捆绑LibreOffice时，视觉渲染状态单独注明，不冒充通过。
