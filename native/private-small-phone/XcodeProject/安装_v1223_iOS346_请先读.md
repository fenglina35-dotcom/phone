# v1223 私人汇总源码包 iOS 1.0.346 346

本包包含网页共享的重新生成及旧卡住聊天恢复修复，并保留全部既有共享功能。私人部分包含最新智能空调真实能力与提示词、灯、门锁、通知头像保护、存档合并写入与卡顿诊断、电量健康刷新、角色锁定名字和批量解锁整体回应。原生桥37；公开 North App 不在本包。日常事件簿是后续独立开发，不包含在本次发布。

## 安装与验收

这是 Mac 待编译源码，不是已签名 IPA。解压到新目录，打开其中 PhoneCompanionTest.xcodeproj；保留旧工程和旧 App，不要先删除旧 App。核对全部 Target 的 Team、Bundle ID、Automatic Signing、App Group、HomeKit 和健康权限。六个 Target 的 Debug 与 Release 应为 1.0.346（346）。编译安装后核对设置页 v1223 与原生346。

更新后，之前重新生成导致卡住的聊天可点“让 ta 回”恢复，不必清空聊天。请实测普通发送、连续重新生成、失败重试、退出重进和后台接力。

智能家电页应同时显示卧室小灯、家庭门锁和卧室空调，用户自定义名称照旧。空调按设备真实能力显示温度、模式与离散风速，角色提示包含最近真实读取值及时间；写入仍须回读确认。门锁 Face ID 保护不变。Windows 无法代替 Mac SDK 编译及真实 ACN1-AIR、Apple Home 和 iPhone 的控制验收。

通知头像保护所需私人后台更新，以同包 SOURCE_COMMIT.txt 的 private-cloud-avatar-migration 字段和本轮发布记录为准；未明确 VERIFIED 前不要安装分发。对应 SQL 保留在后台先行目录。旧锁屏通知不会自动改变，没有成功照片时仍需照片读取恢复。

已通过的自动测试及隔离浏览器测试不能保证零缺陷。真实模型、Mac 编译签名、真实 iPhone 通知及长时间性能改善仍需验收。
