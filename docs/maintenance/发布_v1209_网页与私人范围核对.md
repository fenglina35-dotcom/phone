# v1209 汇总发布范围

用户于2026年9月8日授权汇总当前相关任务打包推送，并要求私人智能家居和卡顿修复不得进入网页版。发布基于 main 的 v1206，加上当前三个任务在 phone-work 的实际改动。两个旧 v1128 私人工作树是历史开发现场，保留原样，不用旧整树覆盖现行代码，也不删除旧 ZIP。

| 用户要求 | 代码位置 | 网页入口及结果 | 私人入口及结果 | 对应验证 |
| --- | --- | --- | --- | --- |
| 主动续聊与 X 角色归属 | app.js、phone-role-push | 近期角色发言上下文、独立发布角色及路由 | 同样包含，并保留私人后台单通道 | reply-output-experiment、proactive-contact、role-server-push |
| 小事簿情绪与约定 | 两份 app.js | 角色视角与证据校验，实际发出的承诺才落地 | 查手机及聊天同样可见 | notebook 运行时及浏览器模拟 |
| 原文实验开关 | app.js、cohab-theater.js、后台函数 | 设置默认关闭，开关保存，合法标签执行 | 微信、单次约会、共同生活、多角色、电话均包含 | check_reply_output_experiment、check_v1200_handoff |
| 私人智能家居 | HomeKitLockBridge.swift、Resources/Web/private-smart-lock.*、私人 smart-home.js | 不添加门锁脚本、入口或桥指令；原 smart-home.js 实现不变 | 独立设备页和门锁页，Face ID 与真实锁态回读 | private-homekit-lock、release-v1209-boundary |
| 私人卡顿修复 | 私人 app.js、private-runtime-diagnostics.js、LocalPhoneWebView.swift | 不加入新后台调度器或私人性能改动 | 自动后台任务单通道、任务耗时诊断；手动聊天/电话不走自动队列 | private-ios331-background-task-lane、既有私人性能/恢复回归 |

网页版本 v1209；私人内置网页 v1209；私人 iOS 1.0.333（333）；原生桥36。网页与私人 app.js 有各自合法实现差异，不做整文件覆盖。入口和共享静态资源分别核验，门锁脚本原被忽略的问题通过明确跟踪两份运行资源解决。

## 产物与发布核验

提交前全仓 Node 检查 1622 项通过、0 失败；网页和私人入口各10项消息与设置实页模拟通过，共20项；原文模式前后台接力另10项通过。脚本语法、Git空白检查及新增行密钥格式扫描通过。打包预检识别网页360个静态文件、私人587个工程及历史说明文件；最终包会剔除旧安装说明并增加提交及哈希清单。文档按维护技能只追加历史，结构保留检查通过，当前机器缺少 soffice.exe，逐页渲染未完成。

两个包都从同一个干净 main 提交归档，使用全新文件名，不覆盖旧包。网页版只收静态运行资源，不包含 native、tests、scripts、docs、supabase 或用户临时数据；私人包包含 Xcode 工程与经核验的 PhoneWeb.bundle。逐文件反读 ZIP 并记录 SHA-256，SOURCE_COMMIT.txt 写入实际提交。不得使用旧 staging 脚本重建私人核心。

提交推送只自动尝试一次，失败后不重试；远端 SHA、GitHub Pages 部署和正式入口另行检查。phone-role-push 是本次共享后台逻辑，部署只针对该函数，不改数据库和其他函数。网页是否上线、函数是否部署以实际工具回执为准，代码入库不等于部署成功。

## 未经真实设备验收的边界

自动测试与浏览器夹具不证明今天的偶发主动重复或 X 事件唯一原因已复现，也不证明卡顿彻底消失。未在本机执行 Mac/Xcode 签名构建、真实 iPhone 安装、真实门锁控制或真实语音播放。私人 ZIP 是 Mac 待编译源码候选，不是 IPA；可运行私人交付受外部签名／真机环境阻塞，不可交付。

仓库现为公开 GitHub 仓库。“私人改动不进网页”在本次指网页版包和运行入口不包含这些功能，不代表推送到 native 目录的源码对外保密。
