# 第二百二十次安装：v1095 / 私人 iOS 1.0.220（220）

本版只针对“角色登录微信并退出后没有补一句可见消息”的真实原因做窄修复：

- 微信退出完成改回与远程操控结束一致的可靠功能事件队列。
- 退出后的角色回复不再把退出前那条“登录微信/改备注”要求误当成新一轮查手机，从而避免模型已生成、系统却在气泡落盘前再次启动检查并误报成功。
- 若模型意外带出 `[登录微信]` 或 `[申请远程操控]` 标签，只删除操作标签，保留角色本人真正要说的可见内容。
- 仍要求至少出现一条真实可见角色消息；空白或纯操作结果不会被当作成功。

Windows 自动验证：根目录 1135/1135、外卖服务 154/154；两份 `app.js` 字节一致。Mac 编译与私人真机安装仍需在 Mac/Xcode 上完成。
