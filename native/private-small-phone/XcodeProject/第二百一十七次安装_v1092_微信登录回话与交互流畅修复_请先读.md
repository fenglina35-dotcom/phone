# 第二百一十七次安装：v1092 微信登录回话与交互流畅修复

- 网页版本：v1092
- 私人 iOS：1.0.217（217）
- 原生桥：25

角色完成一分钟微信登录后，会通过真实角色模型发出一轮包含本人想法或态度的消息；不能只说“看完了、改好了”，也不会因为拟人忙碌状态而延后。第一次没有形成有效可见消息时最多再调用同一真实模型一次，不使用固定角色台词。

用户最近明确要求角色改备注时，本次要求优先于自主改名的两三天冷却。登录模型第一次漏掉备注动作时，只追加一次真实模型补判；成功后不会再次执行。用户没有明确要求时，原有自主改名冷却不变。

聊天引用和取消引用只更新输入区引用条，不再重建整个聊天页面，草稿和键盘焦点会保留。主屏滑动在 WKWebView 中只使用一套 Pointer／Touch 事件，减少重复监听造成的卡顿。

Windows 已完成自动测试和本地浏览器操作，但不能代替 Mac 编译、签名和真实 iPhone。安装后请重点验证：登录一分钟后的真实回话、明确改备注、引用时键盘与草稿、长聊天滑动和主屏图标点按。
