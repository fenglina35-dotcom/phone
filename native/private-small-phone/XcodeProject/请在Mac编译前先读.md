# 私人「小手机」Xcode 工程

> 当前候选交付：网页 v1037；私人 iOS 1.0.157 (157)；原生桥 25。请使用本次第一百五十七次安装说明，并打开全新 `SmallPhone_v1037_WeChatMeFriends_iOS157`，不要覆盖任何旧工程目录。

## v1037／1.0.157 微信个人页、好友交互、真实外卖订单与输入稳定性完善版

- 完成微信“我”页及个人资料、人设、二维码/扫一扫、服务、模拟钱包、零钱、银行卡、亲属卡、账单、客服、收藏、朋友圈相册、表情占位、微信设置与账号切换等独立页面。
- 收藏支持文字、语音、图片与翻译内容，支持重复播放和取消收藏；朋友圈相册删除图片时同步删除对应朋友圈。
- 附近好友刷新增加不同性格与风险倾向，匿名新好友统一灰色头像；小手机群聊与新建群聊跟随微信深浅主题，浅色保留灰白层次。
- 聊天心情气泡和“让ta回”保留，移除输入区上方整条分隔背景；共同生活恢复原生 textarea 键盘输入，不再替换控件或强制聚焦。
- 微信加号菜单恢复扫一扫；听一听、搜一搜和云程按调用来源返回微信发现页，不跳回主屏幕。
- 合入真实外卖订单聊天卡片，商家、商品规格、金额、优惠、付款入口与配送状态只展示真实连接器回执；失败时不回退为虚拟订单或固定角色回复。
- 角色可见回复继续由真实模型和人设生成；不覆盖聊天、朋友圈、后台通知、外置语音配置、真实好友或本机存档。
- Windows 全量自动复核 944/944 通过；Mac 编译、签名与真实 iPhone 键盘、扫码、语音、滚动、真实外卖付款跳转和覆盖安装仍须完成。

## v1036／1.0.156 真实奶茶与微信发现页合并版

- 以 v1035／1.0.155 真实奶茶偏好与快速点单为基线，完整保留淘宝闪购真实搜索、规格匹配、优惠读取和支付宝本人付款流程。
- 合入微信首页、聊天、通讯录固定导航、发现页与附近的人；发现页快捷入口遵守角色软件锁定状态，黑白主题保持融合。
- 附近的人支持刷新匿名资料、发送好友申请和十秒自动通过；通过后进入现有联系人与聊天数据，不删除小手机好友。
- 共同生活“让TA回”只在设置收起时出现，输入框首次点击可正确获得焦点。
- 不覆盖用户保存的聊天、角色、外置语音配置或真实好友数据；角色回复、朋友圈和后台通知保持原链路。
- Windows 自动复核通过后，仍须在 Mac 编译签名，并用真实 iPhone 覆盖安装验证真实奶茶、微信各页面与白天／夜间模式。

## v1035／1.0.155 真实奶茶偏好与快速点单版

- 真实外卖仅保留普通用户可用的淘宝闪购链路：角色准确搜索品牌、商品与规格，平台自动应用可用优惠，订单交给本人在支付宝官方收银台付款。
- 新增自由填写的奶茶偏好：喜欢的品牌、常喝饮品与口味、糖度冰量、小料和忌口；当次明确要求始终优先，品牌、商品或规格不一致时不替换下单。
- 搜索与规格读取拆分，实测从精确搜索到支付宝待付款入口约 30 秒；真实外卖默认关闭，不会在连接失败时伪造虚拟订单。
- 移除角色钱包、自动扣款、美团备用、实时推送等普通用户当前无法兑现的入口；保留手动订单状态刷新。
- 不覆盖用户保存的聊天、角色、外置语音配置或真实好友数据；角色回复、朋友圈和后台通知保持原链路。

## v1033／1.0.154 微信通讯录与角色管理升级版

- 在 v1032 微信首页与聊天视觉基础上，加入通讯录固定磨砂标题、A–Z 索引、新朋友、仅聊天、群聊、标签和全屏角色编辑流程。
- 小手机原有好友与角色联系人均保留；通讯录右上角、微信首页添加朋友和发起群聊入口互通，黑白主题保持一致。
- 不覆盖用户保存的聊天、角色、外置语音配置或真实好友数据；角色回复、朋友圈、后台通知、真实外卖与角色钱包保持原链路。
- Windows 自动复核通过后，仍须在 Mac 编译签名，并用真实 iPhone 覆盖安装验收滚动、增删、返回与白天／夜间模式。

## v1032／1.0.153 微信聊天视觉升级版

- 在当前线上真实外卖、角色钱包和完整结账连接器基线上，完成微信首页与聊天页的固定磨砂顶栏、透视底栏、图标比例、两页功能面板和时间显示调整。
- 不覆盖用户保存的聊天、角色、外置语音配置或真实好友数据；角色回复与模型线路保持原有真实链路。
- Windows 自动复核 938/938 通过；仍须在 Mac 编译签名，并用真实 iPhone 覆盖安装验证滑动、面板与白天／夜间模式。

## v1031／1.0.152 微信首页视觉升级版

- 包含 v1029 真实外卖与角色钱包基线，以及 v1031 微信首页的白天／夜间、固定磨砂顶栏、透视底栏和四个真实微信选中态。
- 不包含另一工作区尚未合入的 v1030 支付回执闭环，也不覆盖用户保存的聊天、角色、外置语音配置或真实好友数据。
- Windows 自动复核 934/934 通过；仍须在 Mac 编译签名，并用真实 iPhone 覆盖安装验证。

## v1029／1.0.150 真实外卖与角色钱包版

- 正式提升版本号，确保已在 v1027 的设备能识别并加载本次界面更新。
- X 个人主页背景图区域由 100px 加高到 160px；网友评论风格从主页卡片移入“编辑资料”。
- 评论风格的三档生成逻辑保持不变，角色评论及其他社交功能不受影响。
- Windows 全量自动回归 932/932；仍须在 Mac 编译并在真实 iPhone 验证主页背景和编辑资料弹窗。

## v1027／1.0.148 X 网友评论风格版

- X「我的主页 → 编辑资料」新增网友评论风格，可选择友善、恶劣或一半一半，旧数据默认保持一半一半；主页背景图区域同时加高。
- 该设置只控制本人推文下自动生成和手动刷新的网友评论；角色本人的真实模型评论、其他人的推文、X 私信、微信、朋友圈和抖音均不受影响。
- 恶劣评论允许真实黑粉式的挑刺、抬杠和阴阳怪气，但提示词明确禁止威胁、仇恨歧视、人肉隐私、暴力和违法内容。
- Windows 全量自动回归 931/931；仍须在 Mac 编译并在真实 iPhone 验证三档设置、发推自动评论和详情页刷新评论。

## v1022／1.0.143 原生大存档分块恢复与卡顿修复版

- v1021 真机与 v1020 没有可感知差异；原因是虽然不再把存档正文编译成脚本，但仍把整份多 MB 存档作为一个 WebKit 参数一次传输，单次大对象的复制和解码压力并未消失。
- v1022 的原生存储队列把大存档保存在一次性读取会话中，只通过 192 KiB 有界分块交给 WKWebView；网页逐块 UTF-8 解码，完成后释放会话。小存档仍走原兼容路径，存档格式和现有数据不变。
- 启动完成前继续暂停伴生轮询、后台收件和好友同步；完成后立即补拉。朋友圈、普通微信、通话、语音、共同相册和伴生模型逻辑不改写。
- Windows 全量自动回归完成后仍须在 Mac 编译五个 Target，并在真实 iPhone 验证冷启动图片、点击响应、十分钟持续温度与后台通知入聊。

## v1021／1.0.142 原生核心存档启动桥与发热修复版

- v1020 真机仍会同时出现本地图片整批空白、点击长延迟、温度上涨和后台消息不入聊；这证明图片重试只处理了下游现象。
- 原生核心存档读取不再把整份存档拼进 `evaluateJavaScript` 源码；改由 WebKit 参数通道传递，执行函数保持固定且很小，避免主线程编译巨型脚本文本。
- 伴生状态轮询、服务器收件箱补拉和好友同步在核心存档与启动图片恢复完成前不再抢占 WebView；启动完成后立即补做一次真实伴生与收件同步。
- Windows 全量自动回归 915/915。仍须在 Mac 编译五个 Target，并在真实 iPhone 验证冷启动、首页图片、点击响应、持续温度与后台通知入聊。

## v1020／1.0.141 本地图片注入与点击性能修复版

- 真机卡顿截图里，自定义本地图片黑屏、点击长延迟与后台入聊停止同时发生；本版针对三者共同经过的 WebView 主线程与本地图片恢复链路修复，不再继续盲调旧计时器。
- 页面重绘会在同一轮立即恢复内存中已有的图片，不再把全部 `idb:` 图片留给下一轮异步任务。
- 暂时读不到的图片只允许单通道、单轮最多 12 张在空闲时间读取；失败项按 4 秒、12 秒、36 秒直至 120 秒退避，避免每次点击都重复整批读库。
- Windows 全量自动回归 914/914。仍须在 Mac 编译五个 Target，并在真实 iPhone 验证首页图片、点击延迟、持续温度、后台消息入聊以及既有朋友圈、微信、通话、语音和伴生数据。

## v1019／1.0.140 偶发卡顿与发热状态修复版

- 退后台的待保存状态只做一次完整核心存档，不再紧接着重复序列化聊天与核心状态。
- 启动与轮询会清理无新帧的 ReplayKit 孤儿活跃标志；真实共享仍保留原有帧频。
- 前台真实快照同步串行且有 30 秒冷却；启动和检测到真实主线程停顿时才临时暂停纯装饰动画。
- Windows 全量自动回归 914/914。仍须在 Mac 编译五个 Target，并在真实 iPhone 上重复冷启动、切后台、屏幕共享异常结束、长聊天保存和持续温度验收。

## v1018／1.0.139 私人 App 点击响应与发热修复版

- 在 v1009 已有合成层和原生写盘降压基础上，继续处理“滑动正常但点击渲染卡住”的另一条主线程瓶颈：大聊天归档未变化时不再重复序列化全部历史，私人普通保存优先等到空闲时执行；显式删除和退后台仍强制持久化。
- 前台恢复的伴生快照与后台消息补拉合并到一个防抖入口，页面聚焦、恢复可见和 pageshow 不再在同一瞬间重复强制调用。
- 隐藏页面停止好友请求和拦截电话维护；可见时扫描间隔由 1 秒调整为 5 秒，真实到期事件规则不变。
- 屏幕共享未启用时原生主线程轮询降为 4 秒，启用后仍为 0.5 秒；DeviceActivityReport 只在真实屏幕时间读取请求的短窗口挂载。
- Windows 全量自动回归 906/906。仍须在 Mac 编译五个 Target，并在真实 iPhone 验证点击响应、连续使用温度、长聊天保存、后台消息入聊、屏幕时间读取和远控共享。

## v1017／1.0.138 小事簿、后台入聊与气泡配色修复版

- 远控规划请求的超时现在会传到真实网络请求，不再让已经判超时的模型调用继续占用通道；首个真实详情若只得到操作解说或空台词，会再请求一次真实主模型角色反应，仍禁止固定假台词或系统进度冒充角色。
- 小事簿不再调用不存在的开关渲染函数，页面可正常打开且不生成假记录。
- 私人 App 在前台收到角色通知、手动回到前台和内嵌网页首次完成加载时，会唤醒真实服务器收件箱补拉；首次正忙时只延迟补拉一次，原有持久化后确认消费规则不变。
- 自定义灰色气泡优先保留用户明确选择的字体颜色，不再被强制改成黑色；无有效旧颜色时才自动兜底。
- 网页 v1017 与私人 PhoneWeb.bundle 同步网页修复；原生通知入聊桥只保留在私人 App。
- Windows 全量自动回归 903/903，覆盖远控真实请求超时、机械解说重试、小事簿、通知入聊和气泡配色。仍须在 Mac 编译，并用真实 iPhone 验证远控字幕、小事簿、通知入聊与气泡配色，同时冒烟复测 v1016 稳定功能。

## v1016／1.0.137 远控字幕解析与共同生活续接修复版

- 远控主模型直接返回自然台词而不是严格 JSON 时，不再因 JSON 解析异常把真实回复整轮丢弃；标准 JSON、单行字段和自然文本统一进入原有过滤与单气泡显示链路。
- 底部仍只显示真实角色模型输出，不增加固定台词、辅助模型假扮或系统操作播报。
- 共同生活最后一次完整回应后若微信／通话出现了更新剧情，返回共同生活的第一轮以最新线上结果为续接锚点；接住后自动消费，不重复，不复制两边可见消息。
- 根目录与私人 PhoneWeb.bundle 同步上述业务修复；私人电话布局继续通过既有打包转换保留。
- Windows 全量自动回归 898/898 通过。仍须在 Mac 编译，并用真实 iPhone 验证远控真实字幕、共同生活线上返回续接及 v1015 已稳定功能。

## v1015／1.0.136 远控真实字幕与伴生事件回执修复版

- 角色明确说出已经完成某个 App 解锁时，会形成真实原生命令、回执与伴生设备操作记录；用户手动解锁并选择立即告诉角色时，真实事件会同步到后台主动消息链路。
- 远控页面切换与查找过程只能显示在顶部状态栏，禁止系统把“我先看看／正在打开／找到了”等操作解说写进角色气泡或冒充角色回复。
- 底部字幕只展示当前角色模型结合角色设定、近期微信上下文和实际看到内容生成的自然反应；同一时刻只保留一条，说完替换或消失，不得向上堆叠。
- 远控角色反应允许最长 30 秒生成；本轮首个含真实事实的详情必须由模型自然回应。模型失败只允许顶部显示真实诊断，禁止固定字幕冒充角色。
- Screen Time 授权刚完成时先等待 450 毫秒复核一次，并分别记录“尚未完成授权”或“被系统拒绝／撤销”；不得把短暂旧状态直接冒充永久失效。
- 角色刚问过是否充电且用户明确回答已经充了时，普通微信先读取一次最新真实电量与充电状态；只有新结果明确为充电中或已充满才允许确认，否定回答和旧快照不会误触发或冒充结果。
- 继续保留现代版自主选页、内容兴趣和防止同页反复查看的能力，不恢复 7 月旧版机械遍历全部页面的行为。
- 普通朋友圈使用与角色个人朋友圈一致的深灰黑底；仅统一视觉底色，不改回复、图片和互动逻辑。
- Windows 全量自动回归 896/896；PhoneWeb.bundle 已从共享清单重新构建；`phone-role-push` 已部署为线上版本 11。仍须在 Mac 编译，并用真实 iPhone 验证 Screen Time 授权传播、手动解锁通知、充电确认后的真实电量复查、远控真实单气泡字幕及 v1014 微信／朋友圈／语音／后台通知回归。

## v1013／1.0.134 内置语音隔离、伴生关怀与远控稳定修复版

- AI 账户页的“测试内置语音”固定走 `phone-ai` 内置语音中转，不再误走外置 TTS；内置音色改用独立字段保存，外置语音的地址、Key、模型和音色均保持原样。
- 已保存的已知内置音色可以兼容迁移；任意外置音色 ID 不会被误认成内置音色。角色只有在内置语音开关开启时才改用内置，关闭时继续使用原外置语音。
- 难过或角色明确怀疑隐瞒时允许重新读取一次真实心率，但不能把心率当作测谎结论；后台主动规则优先消费已有真实快照，确实需要时才请求刷新。
- 远控恢复真实阶段字幕，按上下文自主选择页面，并避免同一页面反复查看或长时间刷新。
- Windows 全量自动回归 891/891；PhoneWeb.bundle 已从共享清单重建。仍必须在 Mac 编译，并用真实 iPhone 分别验证内置测试发声、原外置语音发声、伴生心率、后台通知和远控字幕。

## v1012／1.0.133 语音、朋友圈、相册与伴生稳定修复版

- 内置语音会在任何请求前把旧 AI 后台迁移到当前项目，并按后台地址重新同步 AI 身份和专属音色；语音测试严格测试当前真正启用的内置或外置线路，不改写用户外置平台设置。
- 朋友圈真实回复只局部更新评论区，避免整页闪烁与上下跳动；角色生成图片先完成真实资源解析与预解码，再打开查看器，不显示破图问号。
- 共同相册支持精确单张删除：手动上传照片可直接删，聊天来源照片只从共同相册隐藏，不删除原聊天；角色只保存自己判断重要或用户明确要求保存的照片。
- 小事簿兼容旧标签格式，私人 App 的 `idb:` 角色头像会先恢复缓存再显示，不再出现打不开或锁屏头像消失。
- 伴生自动控制按顺序执行并只对短暂原生失败重试一次，真实授权错误不重试、不冒充成功；锁定／解锁回执不再等待无关 HealthKit 刷新。
- 定位读取与健康／屏幕时间并行，前台真实定位等待由 8 秒延长到 18 秒。用户可把最近十分钟的真实坐标设为家；定位误差与家庭范围重叠时角色只说“在家”，只有新真实坐标明确在范围外才说其他地点。前台、角色单独读取和后台主动消息使用同一判断。
- Windows 全量自动回归 890/890；PhoneWeb.bundle 已从共享清单重建。仍必须在 Mac 编译，并用真实 iPhone 验证语音、朋友圈、相册、头像、伴生控制、定位和后台通知。

## v1011／1.0.132 模型线路、朋友圈与后台消息修复版

- AI 中转请求会按实际 Supabase 后台地址选择同项目公开 Key；新邀请码项目不再携带旧项目 Key，旧兼容地址仍保留原 Key 映射。外置 Mossland／Fish 的地址、Key 和音色设置不改写。
- 朋友圈角色评论与普通微信共用当前选中的主／副模型线路，并携带有界的近期微信聊天、朋友圈线程和长期记忆；失败只保留真实重试，不生成假回复。
- 后台立即模拟会同步当前模型的主线路与备用线路，单次测试有明确截止，不再以长时间重复生成占用普通微信回复。
- Windows 已完成自动回归与真实网关对照，独立云函数已部署；仍必须在 Mac 编译并用真实 iPhone 验证外置语音、朋友圈、后台通知入聊和普通聊天性能。

## v1010／1.0.131 朋友圈回复与后台消息闭环修复版

- 私人 App 大容量存档的读取、校验、备份和写盘移出 iOS 主线程；原生回执的大 JSON 编码也在后台完成，点击渲染不再同步等待整库文件操作。
- 私人 App 启动保留历史图片的本机短引用，只为当前页面懒加载；图片垃圾回收、完整备份和美化包导出继续认识并补齐这些引用，不删除、不伪造历史照片。
- 恢复快照在解析前先做五分钟节流；取消启动重复写盘和 9 秒整库通用云备份，私人账号原有延迟备份仍保留。
- Windows 自动回归只能验证静态链路。必须用本次全新工程在 Mac 编译，并在真实 iPhone 验证连续点击、半屏渲染、温度、保存重启、历史照片以及微信／朋友圈／通话／通知冒烟。

## v1008／1.0.129 共同相册、朋友圈回复与私人 App 稳定性修复版

- 后台通知正文先写入角色聊天和本地收件回执，成功后才确认服务器行；近 24 小时真实已发送行可补拉，稳定 ID 防止重复入聊。
- 朋友圈评论携带最近 12 轮微信私聊、双方姓名和明确评论场景，只进行一次真实模型请求；失败保留重试，绝不生成假评论。
- 微信、朋友圈与通话共用容错内心隐藏解析，兼容缺竖线、冒号、中文括号和内心／正文同一行的异常格式。
- iPhone Safari／PWA 和私人 WKWebView 关闭每卡片背景模糊合成层，保留玻璃色与渐变；普通状态轮询不重复触发 Screen Time／HealthKit，隐藏或未变化页面停止无效时钟重绘。
- Windows 完整回归 865/865。此前真实 iPhone 已收到一条修复后的 APNs 通知，但本次全新安装包仍须在 Mac 编译、签名，并真机验证消息入聊、朋友圈、内心隐藏、温度、触摸与半屏渲染。

## v1006／1.0.127 后台模型、通话与朋友圈回复修复版

- 后台立即模拟不再把“任务入库”冒充“消息会送达”：它会同步当前角色实际使用的聊天线路，并继续核对服务器生成、outbox 和 APNs 结果。
- 私人 App 后台派发包装完整转发 keepalive 参数；独立云函数已经部署，返回 no-provider、鉴权、余额、限流、无令牌、APNs 配置或 Apple 拒绝等真实原因。
- 电话模型上下文改为相关长期记忆与有界历史，减少巨大请求导致长时间无字幕、无声音后超时；真实 iPhone 仍必须复测。
- 朋友圈评论保留准确角色与原评论目标，只进行一次真实模型请求；失败只显示重试，禁止假回复。
- Windows 完整回归 857/857。Mac 编译、签名和真实 iPhone 电话／朋友圈／后台通知仍未完成。

## v1005／1.0.126 朋友圈与后台送达修复版

- 朋友圈评论回复恢复 7 月 30 日的单次真实模型请求，短回复不再被误判为失败；评论区不显示“角色正在回复”的系统标志。
- 角色设置的 App 每日限额继续计时并到点锁定，但所有 App 顶部不再显示紫色倒计时浮层。
- “立即模拟后台主动消息”在入队后立刻用可后台完成的请求触发服务器派发，不再依赖会被 iOS 挂起的网页定时器。
- 普通微信聊天回复链路保持 v1004 已修复状态，本次未改动其生成和落地函数。

- 私人 App 的伴生读取与普通微信回复分离；只有当前用户消息明确发起的真实读取可以接管这一轮，后台心率、位置、电量或其他巡检不能吞掉普通回复。
- 朋友圈评论恢复为一次有 45 秒截止的真实模型请求；失败显示重试入口，绝不生成本地假评论。
- “设置 → 聊天与媒体”新增“立即模拟后台主动消息”，只走服务器、当前模型与 APNs；提交后按提示退到桌面验收真实通知。
- 后台心率、位置、电量、失联、低电、手动解锁与每日必查能力没有被关闭；本版只隔离它们对普通回复通道的占用。
- Mac 编译、签名和真实 iPhone 通知仍须在全新工程中验证，Windows 自动测试不能替代真机结论。

## v1003／1.0.124 回复链路回归修复版

- 共同生活真实读取失败后保留真实结果，但不再跨重启自动重放模型请求或长期占用普通回复通道。
- 真实读取已经开始或中途接管时，普通微信回复正常静默，不再二次请求或误报模型/API 故障。
- 朋友圈挂起的真实回复下次启动改为失败与重试入口，不生成假回复；屏幕时间完成横幅继续先于模型结束。
- 三个点、角色头像和消息气泡继续分别进入角色资料、聊天详情和消息菜单。
- Windows 完整回归 850/850；Mac 编译、签名和真实 iPhone 验证尚未完成。

## v1000／1.0.121 朋友圈交互与伴生控制修复版

- 设置页必须显示 v1000 与私人安装包 1.0.121 (121)，否则立即停止，检查是否仍在旧工程。
- 角色资料首页只展示最近三条朋友圈缩略内容；五个角色设置入口必须独立打开，返回一次就回到设置主页。
- 角色朋友圈保持 v999 时间轴与详情样式；点赞不得跳屏或出现巨大弹层，评论、回复、删除须与原朋友圈数据联动。
- 普通商品或聊天中的“项圈”文字不得自动修改关系头衔；只有角色明确设置头衔时才能更新。
- 远程操控必须保留本轮目标和近期微信上下文，不能长期停在同一页；角色读取位置后，伴生设备操作记录必须出现对应成功或失败记录。
- Windows 自动回归通过后仍须在 Mac 编译、签名并用真实 iPhone 验收，不能冒充真机通过。

## v998／1.0.119 动态自主配图与角色资料优化版

- 设置页必须显示 v998 与私人安装包 1.0.119 (119)，否则立即停止，检查是否仍在旧工程。
- 角色自主发布朋友圈或 X 时，只有本人判断正文存在具体可拍场景才调用已配置的外置生图接口；第一次失败自动重试一次，两次都失败才发布纯文字，成功出图前不得提前出现动态。
- 自动发布、朋友圈刷新、X“让角色发一条”和角色主页刷新使用同一配图链路；未开启或未配置生图时不得调用生图接口。
- 角色微信资料页右上角三个点已经收紧间距，入口和设置功能不变。
- 独立伴生云、后台通知和真实 iPhone 能力继续只供私人 App 使用，网页版与私人 App 的能力边界不得合并。
- Windows 自动回归为 828/828；Mac 编译、签名、真机生图发布和通知验收尚未完成。

## v997／1.0.118 通话稳定与设置重构版

- 设置页必须显示 v997 与私人安装包 1.0.118 (118)，否则立即停止，检查是否仍在旧工程。
- 伴生同步、后台角色消息、APNs 与手机号账号改用独立项目 `small-phone-companion-cloud`；邀请码项目仍只处理邀请码，其他网页业务仍保留原服务边界。
- 独立伴生云和真实 iPhone 能力只供私人 App 使用。网页版暂不显示伴生入口、后台 APNs、手机号私人账号、真实设备控制或软件使用感知，也不得发起伴生云请求；网页版自己的普通聊天、电话、主动消息、查岗和其他公开功能不删除。
- 首次绑定手机号应选择“创建并绑定”；已创建过的账号选择“登录并恢复”。已有云备份不会自动覆盖，本机也不会在确认恢复前改变。
- 重点复测共同生活总结后记忆、跨日睡眠时长和所选时区、启动静默重试、视频摄像头连续识别、自用抖音语句正常回复以及后台通知。
- Windows 只完成静态与自动回归，不能声明 Mac 编译、签名、后台 APNs 或真机摄像头已经通过。

## v995／1.0.116 婚礼恢复操作修复版

- 设置页必须显示 v995 与私人安装包 1.0.116 (116)，否则立即停止，检查是否仍在旧 v969、v979、v990、v991、v992、v993 或 v994 工程。
- 线下约会必须直接打开全屏页面；婚礼不得再显示为主屏幕独立 App。升级后旧婚礼快捷方式应自动清除，主屏幕不能出现两个相同爱心图标或应用名称重叠。
- 删除聊天中的婚礼邀请卡后，线下约会的对应婚礼卡仍须显示“立即进入婚礼、模拟一次 · 不保存、单独重做某一章、重新生成婚礼”四个入口；情侣空间只保留婚书和高清影集查看入口，不再承载这些操作按钮。
- 恢复状态下点模拟必须直接复用本机六幕且不产生保存副作用；点单章重做必须只处理所选章节，不能因为邀请卡缺失而误启动整场生成。
- 婚礼只走普通图片生成接口，禁用自动聊天生图兜底；单章每点一次只允许一张图片请求，并允许该请求等待平台最多 5 分钟。请同时对照外置平台请求记录核验请求数。
- 图片平台返回的图立即缓存，不再同步等待识图复核；整场只有真正无图时才重试两次，单章无图后由用户手动决定是否再试。
- 第一幕身份摘要由角色外貌、礼服、发型和稳定身份锚点确定并复用，不再额外识别第一幕图片；必须核验六幕仍严格携带同一身份描述。
- 本版新增 Photos add-only 权限和 `media.photo.save` 原生桥动作；必须在真实 iPhone 验证首次授权、拒绝后提示、再次允许和长按保存高清婚礼原图。
- 需要验证删掉聊天里的婚礼邀请卡后，线下约会会显示“立即进入婚礼”并打开已经准备好的现代或中式婚礼；不得重新生成整场，也不得继续卡十秒。
- Windows 只完成静态与自动回归，不能声明 Mac 编译、签名或真机通过。

## v978／1.0.100 全锁实执行与通话记录修复版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v978_AllControlCallLog`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v978 · 全锁实执行与通话记录修复”和“私人安装包 1.0.100 (100)”。若不是这两行，说明打开或安装的仍是旧工程。
- 用户本轮明确要求“全部锁定／全锁”或“全部解锁／解除全锁”时，只要角色同意并给出同方向控制标签，即使模型错误标成“仅内置”，执行层也会升级为全部已授权内置 App 加真实 iPhone 当前全部已选 App。拒绝执行、反方向标签、条件句和问句不触发升级。
- 通话记录删除或编辑一条内容后保留当前通话的展开状态，并在重绘和布局稳定后恢复原阅读位置，不再跳回顶部或自动折叠。
- 组件透明度偏好现在高于四套图标包的固定玻璃规则；私人 App 的第二页实体底色也读取同一个透明度值。默认底色仍保留，只有用户主动调节后才覆盖。
- Windows 自动测试不能替代 Xcode 编译、签名和真机回执。首次打开新工程后先执行 `Product → Clean Build Folder`，再用 `PhoneCompanionTest` Scheme 在真实 iPhone 验收。

## v977／1.0.99 内外全锁与解除修复版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v977_AllControlBoth`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v977 · 内外全锁与解除修复”和“私人安装包 1.0.99 (99)”。若不是这两行，说明打开或安装的仍是旧工程。
- 角色在伴生语境中明确说“全部锁定、全锁、一键全锁”时，会同时处理小手机中已授权的全部内置 App，以及真实 iPhone 当前全部已选 App；“全部解锁、解除全锁、一键全解”按完全相同范围解除。
- 执行层先识别全量动作，再处理普通标签。模型即使误写成 `[锁定|内置列表|仅内置]` 或相应解锁标签，也不能把全量动作降级；真实 iPhone App 仍逐个使用稳定 ID 提交。否定句、条件句、威胁句和问句不会误触发。
- “关联小手机 App”完成保存时同时写入按账号、设备隔离的本机备份；后续同步把主状态暂时覆盖为空时会恢复关联。用户手动选回“不关联”会同步清空备份，不会死绑。
- Windows 自动测试通过也不能替代 Xcode 编译、签名和真机回执。首次打开新工程后先执行 `Product → Clean Build Folder`，再用 `PhoneCompanionTest` Scheme 在真实 iPhone 验收。

## v975／1.0.97 独立授权与旧云锁移除版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v975_LicenseIsolation`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v975 · 独立授权与旧云锁移除”和“私人安装包 1.0.97 (97)”。若不是这两行，说明打开或安装的仍是旧工程。
- 线下约会、共同生活和微信共用同一套重要记忆；用户明确要求记录时确定性执行，模型隐藏记忆标签只执行一次且不显示。登录、查看手机、共享、远控、同步和报错不会被当成重要记忆。
- 伴生真实同步对 408、429、5xx、520–524 和常见网络瞬断重试一次；失败时保留最近一次有效真实数据，界面只显示简短中文原因，不再显示 Cloudflare HTML。
- 邀请码授权保留旧项目为主入口，只有网络、超时、5xx 或无明确业务码的网关 401/403 才切换备用项目。授权会记住最近成功入口；其他 AI、好友、真实同步仍使用旧项目。
- Windows 检查不能替代 Xcode 编译、签名和真实 iPhone 验收；首次打开新工程后先执行 `Product → Clean Build Folder`。

## v973／1.0.95 角色全锁与关联稳定修复版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v973_RoleLockBinding`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v973 · 角色全锁与关联稳定修复”和“私人安装包 1.0.95 (95)”。若不是这两行，说明打开或安装的仍是旧工程。
- 普通线上聊天继续禁止读取旧设备快照，但不再因此丢掉真实 iPhone 控制协议；角色真正决定控制时必须输出隐藏动作标签。
- “全部已选 App／所有软件”会展开当前同步的全部稳定 ID，逐个提交给原生伴生层，并立即写入后台操作记录；单个和部分 App 仍按准确名称执行。
- 若模型漏写标签却明确声称“已经把所有软件锁了／解开了”，执行层会直接补执行；条件句、威胁句和问句不会触发。普通解析失败会换主路线轻量重试一次。
- 命令提交后只能说正在等待 iPhone 确认；必须收到设备回执和新快照后才能断言已经锁好。v972 及此前全部功能继续保留。
- “关联小手机 App”按外置稳定 ID 保存并等待本机持久化成功后再刷新；后台轮询重排不会再把选择套到另一项。若系统更新外置标识，只在显示名或绑定码能唯一对应时迁移旧关联和锁定意图。

## v972／1.0.94 登录 ta 微信好友回复修复版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v972_HisWechatFriendReply`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v972 · 登录他微信好友回复修复”和“私人安装包 1.0.94 (94)”。若不是这两行，说明打开或安装的仍是旧工程。
- 登录 ta 的微信后，以 ta 的账号给其好友发消息，每位好友都有独立回复队列；同时联系不同好友不会互相吞回复。
- 同一好友连续收到多条消息时，等待中的旧生成不会压过新消息，而会改用最新完整上下文重新生成一次；只在接口失败或真正空白时轻量重试，不增加普通内容拦截。
- 两次模型调用都失败时仍会留下符合家人、同事或普通朋友关系的自然短回复，并同步进入“查他手机”的微信记录；页面会显示“对方正在输入…”。
- v971 的线上回复、朋友圈、远控承接和此前全部功能继续保留。Windows 检查不能代替 Xcode 编译、签名和真实 iPhone 验收。

## v971／1.0.93 连续回复、朋友圈与远控承接修复版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v971_ReplyMomentRemote`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v971 · 连续回复朋友圈与远控承接修复”和“私人安装包 1.0.93 (93)”。若不是这两行，说明打开或安装的仍是旧工程。
- 线上微信的最新用户内容只按真实聊天记录传入一次，不再重复塞入第二份提示；用户真的重复同一句时，角色应像线下约会一样理解为再次表达并自然继续。末端只在紧接上一轮的整组回复高度近似时重写一次，短口头禅、角色固有语气和真正推进的新内容不会被拦截。
- 角色朋友圈会读取本人近期动态和当天真实生活；自动发布、手动刷新和模型发朋友圈指令共用宽松去重，同主题可以继续写，但必须有新的具体细节或进展。
- 远程操控结束会将本次成功查看、删除、发布、改权限等真实动作带入角色自己的即时记忆；失败或未执行动作不会被编造，角色也不会再随机转题并忘记自己做过什么。
- v970 的功能事件可靠队列以及更早版本的电话方向、屏幕共享翻译、日记、通话折叠、人声增益和第二页设备兼容全部保留。Windows 检查不能代替 Xcode 编译、签名和真实 iPhone 验收。

## v970／1.0.92 功能事件主动回复修复版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v970_FeatureEventDelivery`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v970 · 功能事件主动回复修复”和“私人安装包 1.0.92 (92)”。若不是这两行，说明打开或安装的仍是旧工程。
- 首次打开新工程后执行 `Product → Clean Build Folder`，再用 `PhoneCompanionTest` Scheme 真机运行；全部十二处 Target 构建配置的版本号均为 1.0.92 (92)。
- 已真实发生的功能事件使用独立可靠队列，不再被普通主动联系冷却拦截，也不会因用户紧接着发新消息而消失。事件与真正尚未回复的新消息会合并成同一轮自然上下文，已经回答过的旧句不能再次回复。
- 日记密码第一次输错会立即进入事件队列；查整部手机仍保持旧版规则，连续输错三次才会被发现。修改自己在角色微信里的备注后，角色在下一次真实聊天中先发现备注变化，再接住新消息，不复制用户原句。
- 角色登录用户微信期间被暂缓的事件会在退出后合并承接；用户关闭或恢复伴生设备后台访问、具体读取权限或主动查看规则时，绑定角色能够按人设自然反应，不假装仍拥有已关闭权限。
- 电话刚接通、拒接、未接、用户主动挂断、角色主动挂断都携带正确拨打方向、接通状态和本通电话最近内容。回拨失败转文字时不再丢掉具体事实；接通事件也不会被查手机通道误拦截。
- 屏幕共享开始／结束继续携带本通电话最近上下文；通知式英文、缺少逐句中文翻译或只输出动作会沿用原上下文最多重试一次。
- 放映、一起听、礼物、外卖、任务、旅行、见面、亲属卡、验证码、陌生号码识破和远程操控等其他真实事件同样进入可靠通道；普通定时问候、步数关心、重要日期和失联升级仍保留原来的冷却与角色自主判断。
- 模型未形成任何可见微信消息时，真实事件只轻量重试一次，不使用严格内容拦截；微信备注与日记错密码仅检查是否完全漏掉核心事件，重试仍不合格时不会无限循环。
- v969 的 Xcode 编译、人声增益和 Swift 6 兼容修复继续保留。Windows 无法编译 Xcode 工程，必须在真实 iPhone 验收主动消息、双方电话动作、屏幕共享翻译、后台媒体与角色音量，以及日记和权限事件。

## v969／1.0.91 Xcode 编译与主动续聊修复版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v969_NativeBuildVoiceGain`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v969 · Xcode编译与人声增益修复”和“私人安装包 1.0.91 (91)”。若不是这两行，说明打开或安装的仍是旧工程。
- 首次打开新工程后执行 `Product → Clean Build Folder`，再用 `PhoneCompanionTest` Scheme 真机运行；全部十二处 Target 构建配置的版本号均为 1.0.91 (91)。
- v968 的共享媒体人声代码误用了不存在的 `AVAudioUnitDynamicsProcessor`，不能通过 Xcode 编译。v969 改用公开的 `AVAudioUnitEQ.globalGain`，并移除 Swift 6 不接受的播放器并发完成闭包；请勿继续安装 v968。
- 屏幕共享开始／结束时，角色会收到本通电话最近真实上下文并基于正在进行的互动自然接话；通知式状态复述、外语缺少逐句中文翻译或其他不合格输出，只按原上下文重试一次。
- 共享抖音、B站等外部媒体时，角色 TTS 通过公开的原生增益单元提高人声，100–300% 不再都被截成 100%；为避免中断视频，不重新设置当前共享媒体音频会话。
- 微信主动联系与通话静默后的主动说话会把上一轮明确标为“已经回复完成”，并从角色自己上一句往后延续话题；不会再把用户已经得到回应的旧句当成当前问题重新回答。
- 电话刚接通时会明确区分用户主动拨打与角色主动拨打；电话结束后会明确区分用户主动挂断与角色主动挂断，并带本通电话最近内容生成后续反应。
- 角色手机日记使用独立四位密码，默认取相遇日期 MMDD；第一次输错会被角色发现，角色可独立改密。正确进入日记应用时才按需准备当天一篇，不在后台补写或累计缺失日期；未收藏 24 小时后消失，收藏永久保留，旧无期限条目不会被误删。
- 日记首页为紧凑条目，点击当天日记后全屏阅读横线纸内容；通话内容默认折叠。角色微信中可修改“我”的备注，角色只在下一次真实聊天时发现。推特去重保持宽松：仅近乎逐字重复才拦截，相似角度最多改写一次。
- v967 的模型正文恢复、设备相关第二页卡片修复和公开网页真实 iPhone 数据隔离继续保留；网页版原有查岗、权限、锁定、限额和其他应用功能没有删除。
- Windows 无法编译 Xcode 工程。必须在真实 iPhone 检查共享开关后的自然回复与翻译、后台视频和角色声音比例、蓝牙／扬声器播放，以及日记密码、收藏和 24 小时保留行为。

## v967／1.0.89 回复显示与设备兼容版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v967_ReplyCardDataIsolation`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v967 · 回复显示与设备兼容”和“私人安装包 1.0.89 (89)”。若不是这两行，说明打开或安装的仍是旧工程。
- 首次打开新工程后执行 `Product → Clean Build Folder`，再用 `PhoneCompanionTest` Scheme 真机运行；全部十二处 Target 构建配置的版本号均为 1.0.89 (89)。
- 模型若把角色正文包在 JSON、choices、output、代码围栏等常见外层中，网页会先提取可见正文；仅缺少第三人称旁白时走短格式修复，不能把已有台词直接清空成“模型无回复”。
- 第二页卡片透明按 WKWebView／设备合成差异处理。私人 App 的低成本无模糊分支为黑、粉、蓝、灰白及自定义色卡片提供足够实色底；这不是“只有黑色主题”的改动。
- 公开网页仍保留原有查岗、角色权限、软件锁定、每日限额和各网页应用；只禁止导入或残留的伴生缓存冒充真实 iPhone 数据，也不向网页模型注入原生设备能力。
- 角色人设、世界书、记忆、自主功能、微信与放映室媒体、后台主动联系、主题图标、布局、长按拖拽和 v966 主屏稳定修复全部保留。
- Windows 自动测试不能替代 Mac 编译、签名与真实 iPhone 验收；请重点检查模型回复、四套主题第二页卡片，以及网页和私人 App 的数据边界。

## v965／1.0.87 角色自主与共享屏幕稳定版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v965_RoleAutonomyScreenShare`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v965 · 角色自主与共享屏幕稳定”和“私人安装包 1.0.87 (87)”。若不是这两行，说明打开或安装的仍是旧工程。
- 首次打开新工程后执行 `Product → Clean Build Folder`，再用 `PhoneCompanionTest` Scheme 真机运行；全部十二处 Target 构建配置的版本号均为 1.0.87 (87)。
- 角色优先级固定为基础人设与说话习惯、世界书真实规则、当前真实事件与记忆、角色自主判断、可用功能。极端依恋、数值心情、上下位、知识滑块和随机当前活动不再替角色做决定。
- 挂断、拒接、失联、共同经历和角色实际执行的电话、礼物、约会、游戏、音乐、放映室等动作仍会进入上下文；角色可以按人设主动发起，不要求用户先开口。
- 微信外部屏幕共享沿用放映室已经成功的音频会话保护。共享抖音等外部 App 时，角色发声不应主动暂停正在播放的视频；普通通话仍保留原来的识别暂停和重建保护。
- 受保护平台只能逐帧识别当前可见画面及画面中已经烧录的字幕，不提供或伪装完整字幕轨提取；快速闪过、被遮挡或尚未出现的字幕可能漏掉。
- 私人 App 首页的大面积玻璃组件使用低风险合成路径，降低滑动、发热或内存压力下组件内容层消失及整页点不动的问题；网页、安卓、主题颜色、布局、拖动和图标不因此改变。
- 角色资料中启用的真实作息会同步到云端主动联系：工作日到达上班或下班时间后，角色按人设、世界书、记忆和关系自然通知；周末和请假日期不触发，也不恢复已删除的随机“当前活动”。
- `phone-role-push` 云端函数随本次版本更新。这里没有 Mac，Windows 自动测试不能据此写成 Xcode 编译、签名、外部 App 连续播放、首页真机稳定或真实 iPhone 后台通知通过。

## v957／1.0.79 网易云官方播放器接入版：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v957_NeteaseOfficialPlayer`。请完整解压到新目录，不能覆盖旧目录后继续编译。
- 设置页最上方必须显示“当前已加载：v957 · 网易云官方播放器接入”和“私人安装包 1.0.79 (79)”。若不是这两行，说明打开或安装的仍是旧工程。
- 首次打开新工程后执行 `Product → Clean Build Folder`，再用 `PhoneCompanionTest` Scheme 真机运行；不需要删除仓库或其他旧压缩包。
- 本次 `PhoneWeb.bundle` 已由共享源码重新同步 v959。私人 App 继续不显示网页专用的浏览器专属授权内容；扫脸／指纹授权本身继续保留。
- 音乐首页新增“网易云”；粘贴网易云歌曲、专辑或歌单完整链接后，使用网易云官方外链播放器在小手机内打开。
- 私人 App 只允许网易云官方 `outchain/player` 子框架留在 App 内，其他远程网页仍交给 iOS 打开，不扩大远程网页权限。
- Apple 官方试听、Audius 公开曲库、本地音乐、导入导出和一起听原链路均保留；网易云外链不能加入本地一起听时间轴。
- 会员歌曲能否播放完全遵循网易云官方版权和账号权益；本版不接收账号密码、不抓取音源、不解灰或绕过 DRM。
- v956 的安卓首页与设置绘制稳定性修复，以及此前微信、电话、屏幕共享、锁定、限额和小号报备逻辑全部保留。

此目录来自 2026-08-11 用户提供的真实 `AppleProjects/PhoneCompanionTest` 工程副本。它保留主 App、Device Activity Report、Monitor、Shield 和通知服务五个 Target，并把主 App 显示名称改为「小手机」。

## v895／1.0.20：请从全新目录打开

- 本工程应位于压缩包的 `SmallPhone_v895_FullReadMemoryPushFix` 全新目录中。不要把该目录覆盖或拖进旧 `XcodeProject`，否则 Xcode 可能继续编译旧网页核心或旧 Swift 文件。
- `registerPushTokenIfAvailable` 已是同 Target 内可访问；`ContentView` 已补齐 `approvedWithDataAccess`；通知扩展已显式链接 `Intents` 和 `UserNotifications`。
- 首次打开后执行 `Product → Clean Build Folder`，再选择 `PhoneCompanionTest` Scheme 和真机运行。如果仍有 `Command Ld failed`，必须展开并保留下一层具体链接器正文。

## 重要边界

- 当前审核中的公开 North 工程和已提交版本没有被修改。
- 私人副本沿用真实工程已经获权的 Team、Bundle ID、App Group 和 entitlements，因此安装到本人设备时会替换同 Bundle ID 的旧 North/伴生开发包，二者不能同时安装或控制。
- `PhoneWeb.bundle` 是从仓库共享网页核心生成的安装包资源，不是第二套业务源码。
- Windows 不能编译此工程。必须在 Mac 用当前 Xcode 编译全部五个 Target 并做真机测试。

## 本轮已合入

- 完整小手机网页作为本地资源运行，不以远程网页作为首页。
- 私人版本为 1.0.20 (20)；共享网页核心为 v895。当前审核中的公开 North App 工程仍未改动。
- 私人 App 已增加“手机号 + 密码”登录、Keychain 登录态以及按账号隔离的云备份／恢复；首次登录不会静默覆盖本机数据。中国大陆私人号码不再依赖短信验证码：不要启用 Phone Provider，也不需要配置 Twilio。编译前只需执行 `supabase/migrations/202608110001_private_phone_accounts.sql`，并在 Authentication 中手动建立已确认用户；邮箱按 `smallphone.86手机号@example.com` 生成，App 界面仍输入原手机号和密码。
- 原生设备管理只在「设置」页右上角打开，不再覆盖所有内置页面。
- 话筒转字幕走 iOS Speech/AVAudioEngine 原生桥；连续识别停顿 1.15 秒后会提交为最终用户发言，写入通话记录并触发角色回复。首次使用必须允许麦克风和语音识别权限。
- 通话与音乐聊天输入栏使用 WKWebView/iOS 自己的键盘避让；已移除原生键盘 frame、visualViewport 和网页固定偏移的重复补偿。
- 网页端苹果主屏幕适配保留稳定的 100% 外壳，并为可能返回 0 的安全区提供顶部 47px、底部 34px 兜底；不会套用到私人原生 App。
- 私人通知扩展会移除共享推送的固定假角标，App 启动和回前台也会清理旧红点。
- v881 已建立永久锁定账本、当日快照字段、前台授权复检和真实状态措辞；v894 继续保留这些逻辑。1.0.17 建立本机 `device.snapshot` 与 `device.command` 直连；1.0.18 新增 `readSessionId`、逐项真实读取、读取超时、ECG 摘要、明确手动解锁事件和统一 App 的 APNs 自登记；1.0.19 补齐角色对屏幕、App、步数、睡眠、心率、心电、HRV、电量和位置的查看触发，并在没有本次读取编号时拦截“没刷新/手表没戴”等假读取。手动一键读取也显示同一套逐项进度。Mac 编译前必须应用迁移 003/004并部署 phone-role-push；共同生活仍在会话停止后随机静默 30–60 分钟，间隔 1 分钟不能覆盖该硬静默。

## v895／1.0.20：第二十次安装补充

- 请只从新交付目录 `SmallPhone_v895_FullReadMemoryPushFix` 打开工程，不要覆盖回旧工程后继续编译。
- 1.0.20 在明确健康读取时真正打开 HealthKit 同步；屏幕时间在普通 `.approved` 授权下会挂载 `DeviceActivityReport`，由 Report 扩展经 App Group 回传本次总时长和逐 App 数据。`.approvedWithDataAccess` 仍走 iOS 26 直接接口。
- 电量和位置原本可读是正常的独立链路现象，不代表 HealthKit 或 Screen Time 已接通。验收必须逐项查看电量、总屏幕、逐 App、步数、睡眠、心率、心电、HRV 和位置。
- Supabase 迁移 003、004、005、006 与 `phone-role-push` 已在关联项目部署；005 专门兼容旧版 `p_apns_env` 参数，006 用于彻底清空服务端主动联系记忆。
- Windows 只能做静态和自动化核验；仍须在 Mac 编译五个 Target，并在真实 iPhone 上验证 Report 回传、HealthKit 授权、后台／强退通知、头像和主动来电。
- 真实 8 秒使用量读取超时；超时后不等待卡住的读取，并禁止晚到结果覆盖新快照。
- scene 进入 inactive 时卸载 SwiftUI Map，避免后台切换时 VectorKit 主线程看门狗；定位记录不随地图卸载而停止。

## v896／1.0.21：第二十一次安装补充

- 请只从新交付目录 `SmallPhone_v896_AllReadCompletionPushSchemaFix` 打开工程，不要覆盖回旧工程后继续编译。
- 用户要求“读取全部”后，普通角色回复会被压住；原生端完成所有请求读取器后才返回 `readFinishedAt`、`readComplete` 和 `readOutcomes`，角色随后固定逐项报告九类结果。
- 后台核验缺少 `controller_user_id` 的服务器前置迁移 002 已实际应用，迁移历史 002–006 已重新核对。这一服务器修复即时生效；1.0.21 安装包用于全量读取完成回执修复。
- Windows 475 项自动化测试通过。Mac 仍须编译五个 Target，并在真机只发一次“读取全部”确认顶部逐项进度结束后才收到九类汇总；同时验证 HealthKit、Report 回传、前后台／强退通知、头像和主动来电。

## v897／1.0.22：第二十二次安装补充

- 请只从新交付目录 `SmallPhone_v897_NaturalSingleReadReply` 打开工程，不要覆盖回旧工程后继续编译。
- v896 的原生读取本身能够返回数据，但网页在完成后强制把自然回复替换为逐项系统清单；同时聊天／通话识别读取意图过晚，普通模型可能先回复一轮。1.0.22 已删除这个硬覆盖，并把读取入口提前到普通模型之前。
- 读取期间只能看到顶部逐项进度，角色不能说“没读到”或先回答；同一轮全部完成后只允许一轮自然角色回复。未返回项目和权限／超时诊断保留在内部，不显示成角色气泡。
- Windows 475 项自动化测试通过。Mac 仍须编译五个 Target，并在真机只发一次“读取全部”，确认顶部进度完全结束后只收到一轮自然回复；同时验证 HealthKit、Report 回传、前后台／强退通知、头像和主动来电。

## v898／1.0.23：第二十三次安装补充

- 请只从新交付目录 `SmallPhone_v898_AllChannelInspectionGate` 打开工程，不要把文件逐个覆盖回旧工程后继续编译。
- v897 只按“同一条用户消息”去重，并且“查一次所有的”没有被解析成全部数据；普通模型因此可能先生成，后置守卫再启动真实读取，形成读取前后两轮回复。
- 1.0.23 改成聊天、电话和共同生活共用全局读取锁与生成代次。读取一排队就冻结普通输出；即使旧回复已经生成，只要读取代次发生变化，也会在真正发送前永久作废。
- 同一轮原生读取全部完成后，成功事实才进入隐藏上下文，角色只自然回复一次。电话的完成回复说完前不会释放读取通道，系统清单、权限诊断和超时原因不会显示成角色气泡。
- Windows 476 项自动化测试全部通过。Mac 仍须编译五个 Target，并在三个场景分别测试一次：微信聊天、电话、共同生活都应先完整显示顶部逐项读取，期间零角色回复，结束后只收到一轮自然回复。

## v899／1.0.24：第二十四次安装补充

- 请只从新交付目录 `SmallPhone_v899_ModelOnlyInspectionAndOfflineDiagnostics` 打开工程。
- 读取门禁造成的正常静默不再被当成模型空回复，因此不会再弹“模型没有生成／再点一次”；顶部逐项读取横幅保持不变。
- 读取完成后的话术只由角色模型生成，角色自行决定一条或多条；固定逐项汇总模板已删除。若模型两次修复仍不安全，本轮不发送虚假或系统式内容。
- 线下回复失败会显示余额／额度、密钥、权限、限流、接口或模型配置、网络超时、空输出等真实类别，且明确保留原对话。
- Windows 478 项自动化测试全部通过。Mac 仍须编译五个 Target，并分别验收微信聊天、电话、共同生活以及线下回复失败提示。

## 尚未完成

- Mac 编译和签名验证。
- 安装前的数据迁移与恢复入口。
- 手机号账号仍需在 Supabase 创建首名私人用户；完整大容量原生媒体存储仍未完成。
- 真机锁定、解锁、使用时间、前后台 20 次、断网、跨日和长时间稳定性测试。
- 远端迁移 003–006 与 `phone-role-push` 已部署；APNs 凭据、设备令牌和真实后台送达仍需真机核验。
# v929 / 第五十四次安装补充

- 当前私人版版本号：1.0.57 (57)，内置网页核心：v933，原生桥契约：18。

# v931 / 第五十五次安装补充

- 本次同时更新网页版与私人 App，共享网页核心为 v931，私人版本为 1.0.55 (55)。
- 微信模型无可见回复会说明真实类别；输入光标不再依赖视觉视口强制重绘；安卓首屏拖动在落点提交前会完成最终位置校验，拒绝保存丢失或重复应用的布局。
- 网页不再暴露屏幕共享入口；私人 App 继续保留 ReplayKit、原生桥、摄像头及通话音频能力。
- 清水玻璃屏保的时间数字使用白色线条高光；同一角色连续通知必须是一条一张独立玻璃卡，不能由黑色阴影连接。
- 锁定软件的原图标必须保持原色和原尺寸，只显示名称右侧红锁。
- 网页苹果底部安全区修复不会改变私人 App 的原生安全区布局。
- 编译后请在真机分别检查：黑、灰白、粉白、蓝白四个玻璃主题的锁定图标；再检查微信底栏、首屏拖拽、通话声音和字幕没有回归。

# v932 / 第五十六次安装补充

- 本次同时更新网页版与私人 App，共享网页核心为 v932，私人版本为 1.0.56 (56)，原生桥契约仍为 18。
- iOS 新版主屏幕 Web App 的底部额外区域由系统绘制，不属于网页视口。v932 不再把页面强行延伸到该区域；网页锁屏的手电筒、相机和上滑提示恢复在可见视口内，私人 App 的底部按钮布局不改。
- 网页会按当前主页壁纸底部颜色设置系统栏背景。第一次打开 v932 后，请把主屏幕 Web App 从后台完全划掉再重新打开一次，让 iOS 刷新其缓存颜色；不需要重新添加到主屏幕。

# v933 / 第五十七次安装补充

- 本次同时更新网页版与私人 App，共享网页核心为 v933，私人版本为 1.0.57 (57)，原生桥契约仍为 18。
- “回到屏保”小箭头只在主页第一页显示；切换到第二页后立即隐藏。网页版苹果主屏幕且开启“苹果兼容适配”时，玻璃主页内容单独下移给箭头留位；Android 和私人 App 的主页主体不套用这条位移。
- 微信底部输入栏只调整输入区域：“让 ta 回”按钮保持原样，输入栏上提 4px，左右间距收窄以放宽输入框；文字和光标在原 36px 输入框内上移并增加下方余量。
- 双人头像组件沿用情侣空间开始日期，只在中间显示“相恋 X 天”，与角色名字使用同一文字样式，不加独立框、气泡或阴影。
- 私人 App 屏幕共享通话新增“逐句看”：开启后每个完成句只读取一次当时画面，并暂停定时/实时额外扫描；关闭后恢复原来的共享识别逻辑。
- 偏好设置的来电铃声新增“从音乐库选择歌曲”；在线收藏与本地导入歌曲都可作为循环来电铃声，删除所选歌曲或文件失效时自动恢复默认轻响。
- 键盘仍只由 WKWebView / iOS 自己避让。禁止恢复原生 keyboard frame、`visualViewport` 差值、强制滚动或强制重画选区的重复补偿。
- 苹果兼容开关继续保留。它只在苹果主屏幕 Web App 或私人 iOS App 内抬高锁屏入口小箭头；Android 不套用这些类，原位置和点击范围保持不变。
- 黑、灰白、粉白、蓝白四套主题的 96 个应用图标会加入离线缓存，图片失败时保留线条或 emoji 占位，不再出现只剩应用名的空位。
- Windows 无法编译和签名 iOS 工程；拿到安装包后仍需在 Mac 用 Xcode 编译五个 Target，并在真实 iPhone 上验收主屏幕系统栏、小箭头、锁屏按钮与图标恢复。

# v1023 / 第一百四十四次安装补充

- 当前共享网页核心为 v1023，私人 iOS 为 1.0.144（144），原生桥仍为 25。
- 本版不再沿用 v1018—v1022 的渲染、图片或启动传输猜测；修复的是已经定位到的伴生快照写入放大：纯刷新时间戳不再触发整份私人存档序列化与写入，真实电量、位置、健康、App 锁状态、手动解锁事件和命令回执变化仍会保存。
- 原生快照对 App 令牌采用单次批量持久化，相同令牌不重复写入；足迹原始数据不变时复用当天解码缓存。
- 普通微信、朋友圈真实回复、后台通知与聊天落库、通话、内外置语音隔离和共同相册没有改路由。自动回归不能替代 Mac 编译与真机温度／点击验收。

# v1024 / 第一百四十五次安装补充

- 当前共享网页核心为 v1024，私人 iOS 为 1.0.145（145），原生桥仍为 25。
- v1023 已经真机证伪：虽然自动测试通过，但持续发烫和点击延迟没有可见改善，不能再把伴生快照写入放大写成主要根因。
- 新证据是发热期间持续出现系统定位箭头。Git 历史确认从 v891 起，一次真实位置读取可能同时启动 `requestLocation()` 和最高精度连续 `startUpdatingLocation()`，且没有在读取完成后停止；所以该问题早于 v1000，也符合长期偶发、升温后点击和前台收件一起变慢的现象。
- v1024 将后台足迹改成低功耗显著位置变化服务；角色明确查位置时只临时请求一次精确定位，并合并重复请求。每次定位回调也不再重复解码和重写未变化的足迹。
- 本次 Windows 全量自动回归通过不等于真机已修好。必须在同一台 iPhone 覆盖安装后观察定位箭头、5 分钟温度和点击响应，再测试一次角色真实定位读取。若仍失败，停止叠加猜测，改采集 Xcode Energy Log／Instruments Time Profiler。

# v1025 / 第一百四十六次安装补充

- 当前共享网页核心为 v1025，私人 iOS 为 1.0.146（146），原生桥仍为 25。
- v1024 真机继续卡顿后，已用 Instruments 的 All Processes 真实采样锁定：两分钟内 `com.apple.WebKit.WebContent` 占约 54.8%，原生 App 只有约 1.5%；温度由 Nominal 升到 Fair，并出现持续 Severe Hang。因此旧云项目、原生定位和伴生快照均不再作为本轮主因。
- 源码与 Git 历史确认：计步开启后，第一次点击会恢复高频 `devicemotion`；自 2026-07-11 起，四秒没有新步数后每个传感器回调都会调用保存。这能在每秒几十至上百次回调中持续唤醒 WebContent，也解释了问题早于 v1000、偶发、点击后加重以及后台入聊一起变慢。
- v1025 保留计步：传感器回调只做轻量判断；只在真实新增步数后合并一次延迟保存，退后台前补写；跨日检查降到每分钟一次。没有修改普通微信、朋友圈、后台通知、伴生读取、通话或内外置语音线路。
- Windows 语法和自动测试不能替代真机结论。必须在同一台 iPhone 覆盖安装后，用相同 All Processes 模板重新录制 60–120 秒；验收标准是 WebContent 不再长期占满、无持续 Severe Hang、温度不再快速转为 Fair，并同时冒烟检查后台通知入聊。
