# v1222 重新生成后不回复修复 未发布

用户确认故障前点过重新生成。日志v1216里此前9个请求HTTP200并解析出82–136字符，最后7轮只有preparing，3组成对约180ms；实际浏览器复现“正常回复→重新生成→零模型调用→手动重试仍零调用并误报模型未回复”。不能将这份日志推断为中转站一直返回空正文。

原因：regenMsg删除最后一组角色回复，保留用户源消息上的_replyHandoffCompleted；aiReply在请求前将其视为前后台重复回复并提前return，网页诊断创建后未完成，手动重试180ms后仍被同一标记阻止。

范围：两边共有的微信线上聊天。修改两份app.js的手动重生/回复入口、replyHandoff本地显式重开、完成标记判断和旧队列处理；未改模型、密钥、提示词、内容拦截、电话/共同生活生成及后台服务。私人外层诊断包装继续自行收尾，不能向其内部不存在的_replyAudit变量写入。

修复：用户显式手动回复才能重开已完成但已无后续回复的源消息；完成标记不清除，保存仅本条源消息的重生标记以阻止旧outbox迟到重放。新本地候选只在开始时的完成对象仍一致且取得单一所有权后写入，成功更新完成记录，失败可继续重试。角色/账号/用户消息ID保持不变。排队的显式恢复不绑定已完成旧任务；重生复用手动队列、忙碌保护和replyTouch过期令牌。

影响面核对：普通用户新消息、手动继续、连续重生、失败后恢复、旧故障存档重载、同源并发、服务器先完成/本地先完成、同条outbox重拉、不同角色/账号/新用户消息、查手机授权与普通聊天持久化。原始网页Bug在修复前实际页面复现；新增所有权测试旧实现8失败2通过，修复后10通过。专项、完整测试与浏览器最终结果以本轮交接记录为准。

测试环境说明：页面及持久化是真实代码，模型/云/原生网络为隔离夹具，不调用用户真实Key。浏览器夹具须退出northPreview再验存档恢复，模型请求计数只统计messages请求，不能把私人取消后台任务的RPC也计为模型调用。未苹果SDK编译/真实iPhone验收。

本轮未提交、推送、后台部署或打包。网页与私人网页候选统一v1222；私人原生仍iOS345/桥37，打包时另分配新iOS版本。原有公开North未提交现场保留；开发中观察到另一任务新增空调Swift和专项测试改动，未覆盖，不以整仓暂时失败断言聊天修复失败，也不将其混入发布。智能家电入口问题用户已暂停；头像SQL仍未部署。

最终本地验收：1752/1752通过（.qa/v1222-tests-final.log），针对重生/所有权/手动回复/路由/离线门禁专项33通过；scripts/check_reply_regenerate.cjs网页和私人原文两态连续重生、失败重试、并发点击、隐式重放阻止和旧存档恢复通过。scripts/check_chat_authorized_phone.cjs双入口既有授权/笔记、可见回复、自动查看和保存重载通过，页面错误0。此前整仓空调测试随另一任务继续修改后已通过，本轮未改其业务代码。全部网络为隔离夹具，不代表真实模型或iPhone验收。Word Bug记录和规范已追加原历史，项目说明无新增功能无需重复；缺LibreOffice导致Word未视觉验收。
