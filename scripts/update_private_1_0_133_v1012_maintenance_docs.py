from pathlib import Path

from docx import Document


ROOT = Path(__file__).resolve().parents[1]
DOCS = ROOT / "docs" / "maintenance"


def append_section(filename, title, paragraphs):
    path = DOCS / filename
    doc = Document(path)
    doc.add_heading(title, level=1)
    for text in paragraphs:
        doc.add_paragraph(text)
    doc.save(path)


append_section(
    "AI开发项目_项目说明文档.docx",
    "当前发布基线｜网页 v1012／私人 iOS 1.0.133 (133)／原生桥 25（2026-08-21）",
    [
        "v1011 已由用户真机确认朋友圈真实回复和后台通知恢复，本版必须保留这两条成功链路。v1012 只在显示、图片加载、共同相册、查他手机、语音身份迁移与伴生设备回执／定位层做有界修复，不改变朋友圈模型生成规则，不生成假回复。",
        "朋友圈回复成功后只替换对应动态的社交区并保持滚动位置；图片查看器等待 idb／网络资源解析和预解码后再显示。共同相册以精确照片标识做单张删除，聊天来源照片仅从相册隐藏，原微信照片不删除；角色照片仅在角色判断重要或用户明确要求时收录。",
        "伴生锁定／解锁命令在本地原生队列串行执行，只对超时、忙碌或屏蔽写入核验等短暂错误重试一次，授权或稳定 ID 错误立即失败。Swift 控制回执不再等待无关健康刷新；定位与其他读取并行，等待窗口从 8 秒增至 18 秒。",
        "家庭位置由用户用十分钟内的真实坐标设定，半径按定位精度生成。只有位置点到家庭范围的最短可信距离明确大于半径时才判为外出；定位误差与范围重叠时统一显示为家。前台、角色读取、每日自动查看和服务器后台主动消息共用该规则。",
        "Windows 全量 Node 回归 890／890、PhoneWeb.bundle 清单重建与差异检查通过。Mac 编译、签名和真实 iPhone 上的语音、朋友圈显示、相册删除、小事簿头像、自动控制、定位及家庭范围判断仍待验证。",
    ],
)

append_section(
    "AI开发项目_Bug记录模板.docx",
    "2026-08-21｜v1012｜朋友圈跳动、图片问号、相册删除、伴生控制与定位过早失败",
    [
        "现象：朋友圈真实回复已经成功，但落地时整页闪动并上下跳；角色生图点开先显示问号，稍后才闪出；共同相册不能单张删除；小事簿打不开且私人 App 锁屏头像消失；角色自动伴生操作显示失败而同一动作手动成功；前台定位 8 秒报失败，但后台稍后又能读取到同一真实位置。",
        "根因：朋友圈回复完成后仍触发整页重绘；图片查看器在 idb 短引用和网络资源解析前直接给 img；相册缺少来源安全的精确 tombstone；旧小事簿 tags 可能不是数组，私人头像 idb 缓存未就绪；原生控制在已执行后仍等待 HealthKit 刷新，网页可能先超时；定位先单独等 8 秒，再串行读其他数据，与 Core Location 的真实回调时间不匹配。",
        "修复：朋友圈只刷新目标社交区并保持滚动；图片解析、解码、错误状态分离；共同相册新增单张删除且不伤原聊天；标签兼容数组／字符串，头像缓存异步恢复；控制队列、严格 executed 回执和一次短暂错误重试；定位与健康／屏幕时间并发，并给真实位置 18 秒窗口。",
        "新增家庭位置：用户只能用十分钟内的新鲜真实坐标设置，精度太差拒绝保存；家庭半径考虑定位 accuracy，范围重叠按在家，明确离开才显示原地点。规则同步给独立云后台，避免前台说在家而后台主动消息误说在外。",
        "验证：伴生专项 56／56、语音与跨模块定向 92／92、全量 890／890，git diff --check 通过，私人包两个入口哈希一致。Mac 编译和真实 iPhone 尚未完成，不能把 Windows 结果写成真机成功。",
    ],
)

append_section(
    "AI开发项目_Bug修改规范.docx",
    "v1012 新增规范：成功链保护、原生回执和位置语义（2026-08-21）",
    [
        "用户已经真机确认成功的朋友圈回复与后台通知属于受保护基线。后续性能修复只能替换局部渲染、加载状态或回执等待，禁止重新设计模型路由、伪造角色内容或用旧逻辑覆盖已成功链路。",
        "原生设备命令只有收到 ok=true 且 stage=executed 才能记为成功。可以对明确短暂错误做一次有界重试；授权、稳定 ID、参数或不支持错误不得重试。控制回执不得被无关健康、屏幕时间或定位刷新阻塞。",
        "前台读取超时必须与原生异步回调和总桥接时限协调。多个独立读取应并行，不能把每项截止串行相加；后台稍后成功而前台先失败时，应先检查前台等待窗口和读取顺序，不能误判为权限完全失效。",
        "家庭位置必须来自真实新鲜坐标，不得写死地址或把旧缓存伪装成当前定位。只有在考虑 accuracy 后仍确定超出家庭半径，角色才可说用户在别处；前台、普通角色上下文和服务器主动消息必须使用同一算法。",
    ],
)

append_section(
    "AI开发项目_新聊天启动说明.docx",
    "2026-08-21 v1012 接手补充",
    [
        "当前候选基线为网页 v1012、私人 iOS 1.0.133 (133)、原生桥 25。v1011 真机已经确认朋友圈真实回复和后台通知成功，禁止回退这两条链路；朋友圈永远不能用本地假回复补成功。",
        "v1012 新增受保护点：内置语音后台迁移与 AI 身份重同步、朋友圈局部刷新、图片预解码、共同相册精确单张删除、重要照片收录、小事簿旧标签兼容、idb 头像恢复、伴生命令严格回执与一次短暂重试、18 秒并行定位和家庭位置 geofence。",
        "接手后先只读核对 Git、唯一 PhoneWeb.bundle、v1012／1.0.133 版本以及 890／890 回归。Mac 编译与真实 iPhone 验收尚未完成；定位后台晚到并不等于前台权限失败，家庭位置也不得替代真实定位采集。",
    ],
)

print("Updated four maintenance documents for v1012")
