from pathlib import Path

from docx import Document


ROOT = Path(__file__).resolve().parents[1]
DOCS = ROOT / "docs/maintenance"
MARKER = "v1063／1.0.186"


def append_section(filename, title, paragraphs):
    path = DOCS / filename
    document = Document(path)
    if any(MARKER in paragraph.text for paragraph in document.paragraphs):
        print(f"SKIP={path.name}")
        return
    document.add_heading(title, level=1)
    for paragraph in paragraphs:
        document.add_paragraph(paragraph)
    document.save(path)
    print(f"UPDATED={path.name}")


append_section(
    "AI开发项目_项目说明文档.docx",
    "v1063／1.0.186 外卖角色同意直传与首单恢复（2026-08-25）",
    [
        "网页核心升级为 v1063，私人 iOS 升级为 1.0.186（186），原生桥继续为 25。确认 v1061-v1062 的首单退化发生在前端角色到外卖自动化的交接层：当前真实模型自然同意但没有结构化动作时，系统把第二次隐藏模型补动作当作唯一入口；该请求为空、失败或过期时，外卖服务完全收不到搜索请求。",
        "当前用户消息明确提出点吃的，且当前真实角色回复以“好、行、可以、等着、没问题、交给我”等方式明确同意时，系统会直接从同一条消息提取品牌、商品、多件清单和常见规格，创建唯一授权任务并交给自动化；隐藏模型补动作只保留为低优先级兼容，不再阻塞首单。",
        "宽松入口仍要求当前真实模型回合、当前用户消息、账号、角色、会话、turnId 与 messageId 全部一致。普通聊天、角色拒绝、旧消息、页面重绘、后台恢复、完成／取消／过期任务不得新建任务；同一 messageId 重复处理保持幂等。",
        "原 taskId 澄清、商品去重、起送价检查、优惠券检查及最终付款本人确认保持原线路。普通角色回复、朋友圈回复、后台通知、远控字幕、共同生活和外置语音配置未改变。Windows 已做语法、端到端模拟与全量自动测试；Mac 编译、签名和真实 iPhone 覆盖安装仍待执行。",
    ],
)

append_section(
    "AI开发项目_Bug记录模板.docx",
    "v1063／1.0.186 外卖角色同意直传与首单恢复 Bug 记录（2026-08-25）",
    [
        "现象：用户第一次明确说想吃什么，角色回复“好、行、等着”等同意语，但外卖自动化没有搜索；重复说第二遍有时才启动，随后甚至所有首单都不再进入外卖服务。8788 服务健康且日志没有对应新请求。",
        "根因：v1061 引入的缺动作修复依赖第二次隐藏真实模型调用。第一次模型已经自然同意但漏动作时，只有隐藏调用成功返回唯一结构化标签才会执行；空结果、拒绝、异常、账号切换或回复过期都会静默丢失。v1062 只修复了全角标签格式，没有消除这个单点依赖。",
        "修复：在同任务澄清之后、隐藏修复之前增加当前回合直接批准桥。它仅在用户明确点餐且真实角色明确同意时运行，从原消息提取自然品牌／商品／多件清单并调用 roleRequest；保留 taskId、账号、会话、回合和消息幂等边界。",
        "验证：覆盖杨姥姥撒汤、随便点主食、兰州牛肉面、燕麦牛奶粥加茶叶蛋、宽松同意语、普通聊天不触发、角色拒绝不触发、同 messageId 不重复及网页／私人 Bundle 一致性。没有运行真实下单或付款。",
    ],
)

append_section(
    "AI开发项目_Bug修改规范.docx",
    "v1063／1.0.186 外卖角色同意直传规范（2026-08-25）",
    [
        "触发规范：当前用户消息明确提出点吃的，当前真实角色回复明确同意，即可直接交给自动化。允许常见口语、只说商品和多件清单；不得要求固定完整商品名或固定一句同意词。",
        "安全规范：宽松只放在语言识别层，不放宽授权身份层。角色、账号、会话、turnId、messageId、当前消息和任务状态必须一致；普通聊天、明确拒绝、旧消息、重绘与后台恢复不能创建新任务。",
        "幂等规范：每次合法启动只生成一个 taskId；同 messageId 或同 actionKey 重复处理不得再次搜索、加购或新建任务。澄清回答续接原 taskId，不重新执行首单意图。",
        "回归规范：必须用可执行模拟覆盖从角色自然同意到真实 search 请求的整条链路，不能只断言函数或提示词存在。发布前同时检查普通角色回复、朋友圈回复、后台通知、远控字幕和外置语音配置未改路由。",
    ],
)
