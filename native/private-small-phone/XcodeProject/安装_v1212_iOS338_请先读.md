# v1212 私人源码候选 iOS 338

本包是 Mac 待编译源码，不是 IPA。尚未完成 Xcode 签名构建、真实 iPhone 安装与功能验收。保留原 App、旧工程及旧包，解压到全新目录，打开 PhoneCompanionTest.xcodeproj，沿用原 Team、Bundle ID 和各 Target 签名配置。

应显示私人 iOS 1.0.338（338）、内置网页 v1212、原生桥36。

## 两边共有

- 角色设置 → 模型与路线诊断 → 请求故障诊断。请求开始即记录；空正文、非 JSON、流式响应、缺少正文字段、网络错误分别说明。复制内容不含请求正文、响应片段或 API Key。响应片段仅当前页面内存保留；故障元数据在本机保存，切换路线不删除。
- 诊断只帮助定位空回复，没有宣称修复受影响用户的真实上游故障，不改变原重试次数、不额外测试扣费、不擅自兼容新协议。
- 每条气泡北京时间开启后只显示 HH:mm:ss，不显示年月日或北京时间字样，不撑大短消息气泡；关闭恢复旧显示。消息原始时间不变。
- 此前未发布的纯英文无中文翻译拦截、心情标签格式提示一起纳入。原文开关两态均覆盖英文拦截；线上、约会、共同生活、电话和后台均有对应入口。其他原文／默认处理沿用原方式。

## 私人智能家居与既有保护

包含 iOS337 门锁内部操作记录隐藏修复及对应历史内部记录清理，普通用户消息不改。HomeKit Face ID、已知锁态、执行后真实回读保持；不将这些新功能加入网页。私人性能、恢复及后台 AI 拒绝隔离继续保留。

未完成的锁屏快捷指令草稿不进入运行包、不部署迁移。旧私人工作区仅作历史资料保留。

## 安装后验收

已有查手机权限及笔记下普通聊天能回复；诊断能复制并在重进后保留；双方时间只显示时分秒；原文开关开关正常；英文不会进入气泡或后台通知；门锁要求真实认证且内部标签不进入角色对白。后台英文拦截还要求配套服务器函数部署，以发布结果为准。
