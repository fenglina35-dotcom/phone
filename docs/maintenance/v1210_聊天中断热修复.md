# v1210 聊天中断热修复

2026年9月8日，用户报告 v1209 更新后发送消息显示 Can't find variable: c，要求直接修复网页和私人版并保存核心功能保护的长期规则。

原因已复现：myActivity(exceptId,since) 已用 getC(exceptId) 定义 self，却在小事簿筛选及作者说明中引用未定义的 c。buildSystem 在主账号角色拥有查手机授权、无 memorySince 跳过条件时调用它，请求模型前抛出 ReferenceError。未授权路径正常。没有用户规模遥测，不能把全体用户受影响作为已测事实。

修复只将两份 app.js 里的两处 c 改为 self，不新增全局变量，不捕获后静默跳过上下文，不改权限、持久化、模型线路、输出实验或私人后台逻辑。共享调用者包括 buildSystem 的聊天/电话等上下文、spyWatchRefresh 刷新、doSpyViewCore 自动查看。

新增 tests/chat-authorized-phone.test.mjs 与 scripts/check_chat_authorized_phone.cjs。浏览器脚本在旧代码先复现 ReferenceError，修复后覆盖网页与私人入口的授权开关、原文开关、空/已有小事簿、角色与账号隔离、消息进入模型请求并可见送达、忙碌释放、真实输入框与让TA回、自动查看、时间增量、撤销授权、持久化重载。只替换模型 HTTP，不替换 buildSystem 或 myActivity。

补测试时发现孤立 VM 的缺失角色用例需要 msgs/lastMsg 依赖；浏览器自动查看最初复用上轮相同回复，被既有去重拒收，改用不同模拟回复后正常。它们是测试夹具问题，未据此改动正常产品去重或业务代码。

发布门槛：核心功能必须有可运行的实际入口回归，先证明测试能捕捉原故障；默认空存档、源码正则、语法检查和测试总数均不能替代已有权限及历史状态下的聊天送达证据。网页与私人入口分别验证，共享消费者必须列出并检查。不能承诺绝对无 Bug，未验证的真实模型、原生/真机路径必须明确列出。

新版本网页/私人网页 v1210，私人 iOS 1.0.334 (334)，桥36不变。私人智能家居、卡顿和原生差异保持，旧版本和旧ZIP不覆盖。源码包只从干净提交归档并逐文件反读核验；本次不涉及 Supabase 函数，不重新部署后台服务。最终测试、推送、线上与包结果以实际收尾记录为准。

发布前验证：Node 全仓 1624 项通过，0 失败（.qa/v1210-all-tests.xml）。新增双入口核心门禁通过；原文实验浏览器20项通过（微信、合法标签、电话、单次约会、共同生活、多人场景、设置持久化）。新版本提升后再次执行核心门禁通过。旧测试中硬编码的版本标题更新为当前身份，未放松业务断言。两份 app.js 语法通过。

维护三份 DOCX 仅追加事故和工程规则，原段落保留校验通过；脚本最初的英文单引号转义错误修正后执行成功。按 documents 技能尝试渲染，但环境无 soffice.exe，文档视觉验收未完成；未把文档结构检查称为渲染通过。核心发布门禁也写入 AGENTS.md，用户授权的长期记忆写入独立扩展记录。
