from pathlib import Path

from docx import Document


ROOT = Path(__file__).resolve().parents[1]
DOCS = ROOT / "docs/maintenance"
MARKER = "v1058／1.0.181 共同生活、存储散热、转账回复与约会输入修复"


def append_section(filename, title, paragraphs):
    path = DOCS / filename
    document = Document(path)
    if any(MARKER in paragraph.text for paragraph in document.paragraphs):
        print(f"SKIP={path.name}")
        return
    document.add_heading(title, level=1)
    for paragraph in paragraphs:
        document.add_paragraph(paragraph)
    document.save(path)
    print(f"UPDATED={path.name}")


append_section(
    "AI开发项目_项目说明文档.docx",
    "v1058／1.0.181 共同生活、存储散热、转账回复与约会输入修复（2026-08-24）",
    [
        "网页核心升级为 v1058，私人 iOS 升级为 1.0.181（181），原生桥继续为 25。共同生活的返回、测试状态、开关和进入按钮使用真实 button，并通过 cohabActionTap 消化同一次触摸产生的重复点击；状态立即保存，关闭再打开不会无故失效，角色处于上班状态也不应阻断页面入口。",
        "共同生活开启后，角色不会只因模式已开启就自主拨入。只有用户当前回合明确要求角色主动打电话时，真实模型才可在该回合选择打或不打；旧消息、重绘、恢复前台与普通聊天不能补发新来电。普通回复、朋友圈回复、后台通知、远控字幕和外置语音配置保持原链路。",
        "图片恢复将失败项改为延迟、有限次数重试，避免同一批损坏或暂不可读图片持续热循环。私人原生环境优先使用原生存储容量信息，网页配额不足不再被误报成手机总空间将满；浏览器不具备原生容量时仍按浏览器能力如实提示。",
        "微信转账收款或退还只改变原卡片并且只结算一次。状态变化会通过既有 scheduleFeatureReply 与 featureEventNote 交给真实角色模型，使角色知道用户收下或退还了其转账；没有真实可见输出时不伪造角色台词。",
        "线下约会输入框按内容在 42px 至 90px 内自动增高，超过上限后仅在框内滚动，按钮保持底部对齐，不再把聊天正文顶住或拉出异常长光标。根网页和私人 PhoneWeb.bundle 已同步。Windows 完成语法、专项、根／私人一致性及全量 1014/1014 自动测试；Mac 编译、签名和真实 iPhone 覆盖安装仍待执行。",
    ],
)

append_section(
    "AI开发项目_Bug记录模板.docx",
    "v1058／1.0.181 共同生活、存储散热、转账回复与约会输入修复 Bug 记录（2026-08-24）",
    [
        "现象：部分账号的共同生活显示上班后进入、测试状态和返回都点不动，重新进入又被关闭；模式可能触发未获用户当回合授权的主动来电。个别手机同时出现图标空白、发热和存档快满误报。微信转账收下或退还后角色不知道结果。线下约会输入超过两行会顶住上方正文并出现很长的光标。",
        "根因：共同生活动作同时承接触摸和合成点击且缺少可靠结果／持久化边界；来电授权把模式状态和当前用户命令混在一起；图片批量恢复对失败项重复尝试，存储提示混用了浏览器配额和手机容量；转账状态更新未把方向明确的事件交给现有真实模型链路；约会 textarea 使用固定单行高度，首次渲染时还没有稳定绑定输入监听。",
        "修复：共同生活关键入口改为真实 button 并增加 cohabActionTap，cohabEnter 返回成功状态，开关立即保存；来电仅接受当前用户明确命令对应的模型决策。图片失败项延迟且限次重试，私人版读取原生容量。转账增加幂等的 transferCounterpartyReaction。约会输入增加 offInputAutoSize，并用页面级 input 监听覆盖首次渲染。",
        "验证中的问题与收口：最初只在 offNarrationDecorate 内绑定 textarea，真实页面首次进入时监听尚未建立，浏览器测试暴露第一轮输入仍不会增高；改为页面级委托监听后，390×844 视口实测输入框高 90px、内容高 120px、内部滚动开启，输入栏与正文不重叠。",
        "验证：根目录与私人 Bundle 的 JS 语法、关键文件一致性、共同生活通话闸门、存储散热、转账回复、约会输入及保护性回复链路专项通过；最终 Windows 全量 1014/1014。未在 Mac 执行 Xcode 编译、签名，也未做本版本真实 iPhone 安装验证。",
    ],
)

append_section(
    "AI开发项目_Bug修改规范.docx",
    "v1058／1.0.181 共同生活、存储散热、转账回复与约会输入修复规范（2026-08-24）",
    [
        "共同生活交互规范：入口、开关、测试和返回必须可单独点击，不能由角色地点或上班／在家状态阻断。触摸与合成 click 必须做短时幂等；状态变更立即保存。共同生活只提供上下文，不等于来电授权。",
        "主动来电规范：必须同时满足当前用户消息明确要求角色打来、当前真实模型结构化决定来电、账号／角色／会话／当前回合一致。旧消息、页面重绘、后台恢复、定时器和普通聊天不得创建来电；模型可选择不打，系统不得代替模型伪造决定。",
        "性能与存储规范：异步批处理失败必须有上限、退避和可恢复条件，不能在前台循环重试造成发热。浏览器 localStorage／IndexedDB 配额、原生应用容器容量和整机剩余空间必须分别描述；拿不到原生容量时不得用 5MB 等网页配额冒充手机总空间。",
        "角色事件规范：转账收下或退还等事实通过既有真实模型路由传递，事件方向必须写清楚，重复动作只能产生一次状态变化和一次回复。真实模型没有输出时允许既有的一次有限重试，但禁止固定假话冒充角色。",
        "多行输入规范：textarea 应按 scrollHeight 自适应但必须设最小／最大高度；达到上限后只允许内部滚动，按钮底对齐，正文滚动区不能被覆盖。监听必须覆盖首次动态渲染，样式和脚本应同时同步到网页、私人小手机入口和私人 index。",
        "发布规范：版本、Service Worker BUILD／HOTFIX、资源查询参数、Bundle Info、Xcode build／marketing version、原生 marker、测试断言、安装说明和私人 ZIP 必须同批更新。MacReady 只代表源码结构与打包检查通过；没有 Mac 编译、签名和真机安装就必须明确写未验证。",
    ],
)
