# v1202 / iOS323 私人源码包（2026-09-07）

最新授权：只制作私人 Mac 待编译源码包；网页不上传、不部署。允许为归档做本地提交，不推送远端。包内新增功能与网页候选相同；原生桥35不变。两边入口、本地模型替身与全仓测试通过不能代替真实模型、Mac签名编译、真实iPhone验收。十四套服装仍未完成，当前完整造型为两套。

以下 v1201/P08 是上一阶段历史记录：

# 像素少女：本地候选 v1201

2026-09-07。用户最后要求：私人版本和网页版暂不上传。本轮不提交、不推送、不打包；原安装包说明仍指向此前 v1200 / iOS 322，不得拿它给当前源码签名交付。

## 当前入口与体验

- 独立 P08 试玩：`preview/rose-doll-p01`，本地 `http://127.0.0.1:8327/`，保留原浏览器存档。这里的“让他照顾”仍是无需模型的动作预演。
- 正式本地入口：小手机 → 游戏大厅 → 像素少女。只接受当前账号的情侣空间绑定角色；`pixel-home.js` 负责角色请求和手机存档，内嵌游戏负责动画与玩法。
- 按钮名“让他照顾”。随机挑选现有三种食物之一，每次至多一份；照顾顺序、停顿、移动节奏、皮球次数和戳脸次数变化。八项包括喂食、洗澡、梳头、刷牙、洗脸、皮球、小熊、戳脸摸头。
- 饥饿先吃饭；饱腹超过 94 不喂；疲倦优先缩短活动并休息；睡着不玩耍。药水不当随机食物，不自动购物。洗澡先香皂后冲水，洁面先洗面奶后毛巾。
- 收藏移动到右侧原灯按钮位置。没有手动灯按钮；上床自动关灯，起床自动开灯。
- 鼠标动作调用与用户操作相同的喂食、洗漱、梳发和触摸函数，取消、切后台、离开入口后不再执行迟到动作。

## 角色与早上八点

正式入口调用绑定角色自己的模型路线，只发送至多 8000 字人设、玩家称呼、少女数值和动作/穿搭目录；不附加微信历史、照片或其他角色内容。请求同时安排未来七天穿搭；首次安排成功后，近期点击照顾可复用同一份新安排。重复点击等待中的照顾不会再发一轮模型请求；格式失败不自动纠正重试，不伪造角色回应。

每天设备当地时间 08:00 后应用当天预选造型，前台检查间隔 30 秒。网页关闭时不能运行模型或动画；下一次打开补上当天已安排的造型。不补演往日操作，不每天重刷同一套，不在计划失败时冒称角色已经挑选。自动计划失败当天不反复请求；用户主动点击照顾可再试。

当前采用已有完整人物原图的两套造型（草莓奶油、蓝莓来信），统一裁剪框、统一显示高度，不拼接头颈或新表情。十四套服装与进一步扩展仍待美术阶段，不能称为已完成。

## 隔离和回归范围

| 用户要求 | 代码 | 共享路径与保护 | 验证 |
| --- | --- | --- | --- |
| 随机照顾、皮球、戳脸、食物 | care-policy.js / game.js | 复用手动动作；按状态重验；取消令牌 | 独立预览与正式入口浏览器测试 |
| 正式游戏大厅入口 | app.js / pixel-home.js | 仅新增页面分支；旧页面继续原 render；新页面保留 iframe 防止回复刷新重建游戏 | 原游戏匹配、反复 render、退出 |
| 绑定角色照顾 | pixel-home.js / bridge.js | source、origin、会话令牌、账号与角色检查；解绑关闭小屋；凭据不传入 iframe | 模拟 HTTP 模型、格式失败一次、慢回复解绑 |
| 8 点穿搭 | pixel-home-policy.js | 本地日期、已应用日期、现有计划；存档按账号和绑定角色隔离 | 08:00 前后、重复、跨年、回拨、离线补当日 |
| 游戏时长/监管兼容 | app.js / couple-watch-runtime.js | 继续归为 games；伴侣参与时不发软件监管提醒 | 路由与原监管回归 |
| 私人包含网页功能 | PhoneWeb.bundle | 相同新增模块和 19MB 素材；只定点修改 private app.js，保留原生差异 | 入口与共享资源字节对照 |

## 验证与限制

- Windows Node 全仓 1569/1569 通过，0 失败。历史版本断言按 v1201 本地候选同步，业务断言保留；已发布安装指南仍按 v1200 校验。
- `tests/pixel-home.test.mjs`：7 项（包含在全仓），涵盖日期、随机食物、状态优先级、响应格式、存档字段。
- `scripts/check_pixel_home.cjs`：真实网页与私人内置页的 Edge 浏览器测试。HTTP 模型为测试数据，无真实 API 费用，不能视为真实模型或真实 iPhone 验收。
- 独立预览 `check-p08.cjs` 9 项，原音频 6 项，原核心时钟 7 项通过；320/430 窄屏与照顾鼠标截图已查看。
- 本地预览暂停后再恢复音频，需要遵守浏览器首次交互规则。进入正式小屋会暂停原音乐播放器，避免与小屋背景音乐重叠。
- 未提交、未上传、未打包、未 Mac 编译、未真实 iPhone 验收。

素材使用先前生成并已验收的完整人物、房间、道具和用户提供的两段 MP3，本轮没有重新生成图片或改变脸部比例。
