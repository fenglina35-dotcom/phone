# 第一百九十五次安装：v1071 私人 App 完整整合版

本次版本：

- 网页核心：v1071
- 私人 iOS：1.0.195（构建 195）
- 原生桥：25

## 本次修正私人 App 完整 Bundle 交付

1. 存储占用明细在私人 App 中不再遍历并反序列化全部 IndexedDB 媒体，只读取安全概要和原生容量估算。
2. 私人 App 的图片 JavaScript 内存缓存增加软上限；只释放可重新从本地存储读取的非可见副本，不删除真实图片、聊天或用户存档。
3. WebContent 第一次终止后等待 5 秒，并重新打开本安装包内唯一的 `PhoneWeb.bundle/index.html`。
4. 两分钟内第二次终止会停止自动重载并显示失败页，避免白屏、屏保和重载循环继续升温。
5. 私人包改为完整递归收录实际 `PhoneWeb.bundle`，包含个人微信页及二维码离线依赖，不再只按 Git 跟踪清单漏打资源。
6. 角色形象工作室的衣柜改为整齐双列卡片，列表只显示图片、名称和使用场景；详细描述保留在编辑页。测试生图不再被旧提示词把“坐在沙发上”误判成只拍空沙发，除非明确说不要人物。
7. 外卖商品名与规格要求分开判断。商品名里的“白糯米”等词不会再擅自变成额外加料；万达店“草莓桃儿白糯米酸奶奶昔”已真实验证到购物车草稿，保留平台默认标准糯米，未提交付款。
8. 网页核心仍为 v1071；公开网页不走上述私人 App 稳定性分支，但共享网页功能与私人 Bundle 保持一致。

## Mac 安装

1. 把整个 ZIP 复制到 Mac 后完整解压到全新目录，不要直接在压缩包里打开，也不要覆盖旧工程。
2. 打开 `PhoneCompanionTest.xcodeproj`，选择自己的 Team 和真实 iPhone。
3. 执行 Product → Clean Build Folder，再编译运行。
4. 覆盖安装后先打开版本页，必须显示 `v1071 / 1.0.195 / 原生桥 25`；若仍显示 1.0.194，说明手机运行的还是旧包。
5. 不要删除旧 App，除非已经确认数据备份；覆盖安装才能保留原手机数据用于这次复现。

## 真机验收顺序

1. 连续点击、打开和返回多个软件，观察触摸响应和温度至少 15 分钟。
2. 打开一次“存储占用明细”，确认不会因为计算 1.68GB 图片占用而卡死或持续发热。
3. 若 WebContent 终止，只应等待后恢复一次；再次终止应停在失败页，不能反复白屏和跳回屏保。
4. 冒烟检查普通微信、朋友圈真实回复、后台通知入聊、真实电量／位置／健康／屏幕时间、通话、外置语音配置和共同相册。
5. 打开角色形象工作室，确认衣柜为双列简洁卡片；输入“坐在沙发上拿着鞭子”测试时应生成人物，只有明确写“不要人物”才允许纯场景。
6. 外卖复查时先确认门店和商品，再看真实规格；不得把商品名中的词自动当作额外加料。

## 必须如实说明

Windows 已通过 1219／1219 自动测试，并确认根网页与私人 Bundle 的共享 JavaScript 一致；外卖还使用正式 8788 服务和已登录浏览器真实验证到购物车草稿。Windows 不能完成 Mac 编译、签名，也不能证明真实 iPhone 的长期温度、WebContent 恢复和生图模型输出已经全部通过。本包仍需按上述顺序完成 Mac 编译及真机验收后才能下最终结论。
