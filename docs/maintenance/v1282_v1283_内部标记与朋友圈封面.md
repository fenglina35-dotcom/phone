# v1282 网页 / v1283 私人内部标记与朋友圈封面修复

2026-09-21。私人 iOS 1.0.380 (380)，12 个 Target；原生桥 41 不变。

用户截图末条 APNs 为 [应用处理|提醒]。后台 parseRoleAppDecision 只接受开头标记，原文模式解析失败会保留整段文字；通知投影原先只隐藏心情标签。修复只在入库、APNs 投影、前台后台回拉、原文输出、普通解析边界删除应用处理的提醒/锁定标记。严格锁定解析器、实际设备命令、账号权限和消息文字不变；标签单独出现时不产生空消息。已有 iOS 通知不会被本补丁远程清除。

朋友圈封面合入 ff02c2f3：IDB 冷缓存时保留图片节点，页面重进时重取封面和头像。不重写用户图片和存档。

## 影响与验证

- 网页和私人：普通聊天、原文输出、服务器回拉；应用提醒标签隐藏，未知标签与功能卡片保留。
- 后台：phone-role-push 入库和 APNs 投影；心情元数据仍保留供客户端同步，锁定成功/失败选择和锁定权限不变。
- 6 项行为回归在旧实现全部失败，在修复后全部通过。公开服务候选同样 6/6 通过。
- 全量 Node：2242/2242 通过。两端真实页面 check_chat_authorized_phone.cjs：已有查手机授权、非空小事簿、角色/账号隔离、两种输出设置、带尾部标记回复、输入发送、手动回复、自动查手机、刷新恢复和忙碌释放通过，页面错误 0。
- 全量包含永久继承守卫、私人备份/通话、智能家居、朋友圈冷缓存等专项；新修复已登记永久守卫。
- 此轮原生代码仅提升版本字段。备份、语音、摄像头、智能家居执行和温馨小家资源不作业务修改。

## 服务发布边界

私人项目 qvuahlqimcfgeoetosnl 的线上下载与修复前本地源码一致。公开项目 lkhlyfpssmrjkkzhuzag 保留其独立批量解锁行为，由 scripts/prepare_public_app_visibility_patch.py 在哈希锁定的公开线上基线只套用相同两处可见性补丁，不覆盖私人源码到公开服务。仅部署 phone-role-push，不执行迁移，不部署备份或智能家居服务。

## 包与验收边界

scripts/package_private_v1283_ios380.py 从提交后的 HEAD 读取资源，重新解压校验；SOURCE_STATE 与 SHA256SUMS 保留完整继承清单。私人备份的 begin/chunk/commit/abort/progress、4 MB 上传分块、原生30分钟/JS32分钟等待、旧备份恢复、完整温馨小家052和上一版通话格式修复均为阻断检查。

本轮验证为 Windows 源码与浏览器执行，不是 Mac 编译、签名或真实 iPhone APNs 验收。DOCX 仅追加历史记录；代码发布不以 DOCX 渲染作为验收依据。

服务发布结果：私人和公开 phone-role-push 均已部署，重新通过 API 下载源码并与各自候选逐字比对一致。CLI 的统计事件关闭出现 PostHog timeout，部署结果已用实际下载内容独立核实，不重复部署。

维护 DOCX 已验证原 XML 仅追加记录、历史内容不变；render_docx.py 执行提示缺少 LibreOffice，未进行文档页面渲染验收。交付物为源码覆盖包，不含 DOCX 文档。
