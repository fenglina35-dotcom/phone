# 第三百一十二次安装：私人 v1186 共同生活键盘隔离修复版

本目录是私人 iOS `1.0.312 (312)`、内置网页功能基线 `v1184` 的 Mac 待编译源码。原生桥 35 保持不变；本次没有修改或推送网页版。

## 本次修复

- 撤回 v1185 对所有页面生效的 `UIKeyboardLayoutGuide` 容器，微信重新使用 v1184 已验证较正常的直接 `WKWebView` 与系统键盘安全区路径。
- 仅当共同生活输入框 `#off_in` 获得焦点时，临时关闭 `WKWebView` 的外层滚动，避免 WebKit 自动聚焦滚动与系统整页上推叠加造成二次弹跳。
- 键盘隐藏、切换到其他输入框或退出页面时恢复外层滚动；不监听滚动坐标，不调用 `setContentOffset`，不改网页 viewport。
- 共同生活仍复用原来的网页输入框、旁白切换与发送逻辑；网页文件和私人 `PhoneWeb.bundle` 均保持 v1184 内容不变。

## Mac 与真机验收

1. 不要先删除手机上的旧 App，也不要覆盖旧工程目录；先在 Mac 解压本次新目录。
2. 用 Xcode 打开 `PhoneCompanionTest.xcodeproj`，确认签名 Team 和 Bundle Identifier 后再编译安装。
3. 启动后确认原生诊断可见 `1.0.312 (312)`；设置页显示内置网页功能基线 `v1184` 属于正常现象。
4. 在共同生活中至少重复三次：首次点输入框、键盘已打开时切换“旁白”、收起键盘、再次打开。确认整页与输入栏同步上移和回落，没有黑色空隙、卡住或二次弹跳。
5. 单独回归微信：进入聊天、打开键盘、输入、收起、再次打开，确认布局已恢复且不受共同生活隔离逻辑影响。

Windows 自动测试只能证明源码作用域、版本契约和 ZIP 结构通过，不能代替 Mac 编译签名及真实 iPhone 键盘动画验收。
