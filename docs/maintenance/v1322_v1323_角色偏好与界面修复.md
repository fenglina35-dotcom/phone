# v1322 网页与 v1323 私人 iOS395 变更核验

2026-09-26。以实时远端 b11c90eb74e969681f554de6e493441471267501 为基线，在独立 .phone-six-fixes 工作树修改。原 phone-work/main 和两个独有提交保留。72fb6367 的逐条查手机去重按实际差异继承，19/18 个实现段可直接对应；朋友圈回复入口已经由较新实现等效替换，保留较新版。381 包的全部路径在本次私人源中仍存在。

## 影响与实现

- 网页和私人两边共有。气泡调色更新实心变量的同时更新 me/them 半透明变量，不重画页面；保留经典主题、背景图、消息窗口与滚动逻辑。
- 主动电话几率移至角色微信资料的聊天偏好，以 c.callProb 独立保存，默认 35，0 关闭；主动联系、有限回拨及后台同步共用 effCallProb(c)。删除全局编辑入口，保存角色后同步后台配置；其他角色独立，旧全局值保留在存档但不再作为运行设置。
- 布置任务开关只放情侣空间，S.couple.tasksEnabled 必须显式 true，默认关闭；不删除旧任务记录。普通角色和未绑定情侣均不能布置任务，角色资料与关系状态里的旧开关移除。
- 真人 1v1 已收款转账从确认收款事实派生独立回执，复用角色 payCard 和详情页；固定派生 ID，反向发送方与接收方，不修改原始历史、不新增资金结算，不向后台伪造聊天消息，禁止撤回派生回执。红包和群聊维持原路径。
- 四种回复长度缺省值统一 4096，随机度文案改温度设置；保留已有显式配置，包括温度 0、通话长度显式 0 跟随线上。保存或切换路线的默认值同样一致。
- 邀请码：两服务器 OPTIONS 预检从本机返回 200 且 CORS 匹配公开站点。YB2 码只发往隔离授权库，不能为了绕开网络把码发往不相干的库或绕过授权。受影响用户环境尚不明确，未宣称故障已修复，未消费真实邀请码；诊断后续结果另记。

## 验证与交付边界

行为回归在原实现复现了未同步半透明色、电话仍读全局、旧默认长度三项，两端共 6 项原先失败，修复后通过。新增情侣任务显式开启/角色边界及真人回执幂等测试，两端共 10 项通过；继承去重 16 项通过。网页/私人实际页面脚本 check_six_fixes.cjs 验证 5 项功能、两张转账卡与详情、0 页面错误。核心聊天 check_chat_authorized_phone.cjs 两端已有授权/非空档案/普通和原文/自动查看/发送/持久化通过。

旧测试中的全局设置、默认 900/1200 等按新规格更新；Windows CRLF 和 python3 命令假设修为跨平台验证，不变更玻璃轮廓及效果代码。全量最终结果由最终测试日志与发布记录补充。永久守卫增加本次修复及逐条去重标记。

私人包从已提交 Git 树读取，版本 v1323 / 1.0.395 (395) / 桥41。打包脚本修正沿用脚本中 SOURCE_STATE 写死为 v1301/iOS384 的旧元数据，并反核清单、解压内容和版本；不覆盖旧包。Mac/Xcode 编译签名及真实 iPhone 未验证，交付是 Mac 源码 ZIP，不是 IPA。

## 最终验证记录

- 全量 Node：2570/2570，通过；测试日志 phone-six-final-suite.txt。
- 授权链路补查：Node POST 两端 ECONNRESET；PowerShell 及从线上网页启动的全新 Edge 浏览器，向两端发送无会话的 session_check，均收到 400 与“本浏览器还没有授权”的预期业务响应，证明该时刻两条浏览器链路可达。没有发送真实邀请码，没有创建或删除授权。首轮错误动作 check 收到未知操作响应，仅用于可达性检查，不能算 session_check 验证。缺少受影响用户浏览器/网络/入口的证据，尚未找到用户故障根因。
- 三份维护 DOCX 追加记录并保留原文。文档渲染工具因本机缺少 LibreOffice soffice.exe 无法运行；未声称文档排版已完成视觉核验。

- 最终两端核心聊天、逐条查手机去重及新增页面行为脚本均通过；去重覆盖新记录、失败重试、跨共同生活和微信、持久化，0页面错误。候选包内容预检通过，1649个源文件，另加入SOURCE_STATE和SHA256SUMS两份清单。
