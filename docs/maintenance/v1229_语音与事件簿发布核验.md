# v1229 语音与事件簿发布核验

2026-09-09：用户授权把此前语音修复、日常事件簿图标与私人包核验一起打包推送。网页与私人内置 v1229，私人 iOS1.0.351（351），原生桥37。公开 North App 的五项未提交现场不纳入本次提交或打包。智能家居与卡顿能力只在私人包保留，本轮不改其控制逻辑。

## 事实与诊断

直接打开旧的《小手机_v1227_私人版_iOS350_覆盖包.zip》核对：含 daily-event-ledger.js，HTML 已加载，app.js 有“日常事件簿”入口。因此“文件完全漏包”不符合该压缩包证据；不能据此断言用户手机运行的就是该资源。旧 Mac 说明仍有“日常事件簿另做，不在本包”的历史文字，新说明明确纠正。

SPYICON 缺少 events 映射，导致显示“记”字。真实浏览器新增图标断言在旧实现失败；只给网页和私人各补一个 events: notebook 映射，采用与记仇本相同的 SVG。未改事件存储结构、权限、容量、记录逻辑和角色账号归属。

语音证据及9月5日逐函数对比见《v1228_语音格式回归核验_未发布.md》。v1228 未单独发布，本次以新版本 v1229 汇总。保留 TTS 地址、密钥、音色、模型；格式纠正最多额外请求一次。不生成虚构声音或翻译，不把缺译文的外语语音降为纯英文聊天。

## 影响及验收

| 用户要求 | 代码位置与两端入口 | 用户可见结果与验证 |
| --- | --- | --- |
| 日常记录进入私人包 | 两端 app.js、daily-event-ledger.js、HTML；查他手机 | 未生成手机内容时独立按钮、已生成手机时图标均可打开；默认关闭，开关与50至300条上限持久化 |
| 同款线条图标 | 两端 SPYICON | 真实页面 SVG 与记仇本完全相同；旧实现断言失败，修复后通过 |
| 明确与主动语音 | 两端 aiReply 及有界格式修复 | 44个实际消息落库/气泡场景通过；覆盖原样两态、频率0、缺译文和修复失败 |
| 核心聊天不受损 | 实际 buildSystem/myActivity 及授权/存档 | 双入口已授权查手机、非空小事簿、角色账号隔离、手动回复、自动查手机、重载通过 |
| 既有事件与控制不回退 | 在线、线下、共同生活 | 记录发生日期；隐藏心情；“全部解锁？想得美”不解锁；原样两态通过 |
| 私人能力不进入网页 | package_v1229.py 两端资源差异与原生检查 | 校验灯、门锁、空调、卡顿适配保留，网页无私人控制脚本；公开 App 不打包 |

全仓1778项通过。scripts/check_event_ledger_entry.cjs 检查实际两端首页与新包解压目录；scripts/check_daily_events_and_consent.cjs 检查真实记录和控制。首次新入口夹具漏设置情侣关系、重载误用预览模式，仅修正隔离夹具，未改产品权限或存档逻辑。

## 打包与发布边界

scripts/package_v1229.py 从提交的 HEAD 打包，拒绝覆盖旧包，按文件逐一检查 ZIP 和 SHA256SUMS.json。当前文档记录的是提交前已完成核验，实际源码提交、压缩包哈希和网页部署结果另记发布结果及交接文件，未完成前不得称已上线。

Windows 浏览器用隔离模型及音频预热，未连接用户账户、真实模型、TTS 或设备。私人 ZIP 是 Mac 待编译源码包，不是 IPA；Mac 签名编译、真实 iPhone 覆盖安装与声音播放未验证。维护 DOCX 仅追加并检查原有正文不变；当前依赖缺 LibreOffice，不能宣称完成版式渲染验收。
