# v1211 私人源码包 iOS 336

这是 Mac 待编译源码包，不是 IPA。当前 Windows 未进行 Xcode 编译、签名或真实 iPhone 安装验证。请解压到新目录，保留旧 App 和旧工程。

打开 PhoneCompanionTest.xcodeproj，沿用已有 Team、Bundle ID 和全部 Target 的签名配置。确认版本为 1.0.336（336）、内置网页 v1211、原生桥36。不要先删除手机上的旧 App，以免丢失本地记录。

两边共有的新功能：设置中的“每条气泡显示北京时间”。默认关闭；开启后使用消息保存的时间按 UTC+8 显示到秒，关闭恢复原样。旧记录没有时间时显示未知，不编造时间，不改变模型上下文。保留已有小事簿、X 角色归属、原样输出实验和聊天中断修复。

私人新增：后台明确 AI 拒绝说明作为系统错误保留，不进入角色气泡，即使开启原样输出亦如此；不执行附带标签，不反复要求模型重写安全拒绝。此功能还需要配套后台部署及私人角色 profile 同步，源码包本身不证明锁屏通知已经生效。

智能家居仅私人版：门锁图标使用对齐 SVG；仅已解锁事件交给角色，其他状态变化可读但不生成角色事件通知；保留 Face ID、已知状态与实际回读要求。用户此前仅确认手动开关锁，角色发起解锁仍需日志和真机验收，不能承诺本包已解决该问题。私人卡顿修复及原生差异完整保留。

快捷指令锁屏/关闭 App 后台执行尚未完成，不包含本次包中，也不部署其数据库草稿。

安装后至少确认：聊天能发能回；已有查手机授权可正常用；双方北京时间开关开关一次；角色和账号记录不串；门锁手动操作仍要求真实认证并读回；锁屏通知不将 AI 拒绝当作角色话。若 Xcode 报错，不改 Team/Bundle ID 反复试，请保留完整错误信息。
