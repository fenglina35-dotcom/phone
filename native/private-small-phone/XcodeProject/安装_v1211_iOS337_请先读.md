# v1211 私人源码包 iOS 337

打开 `PhoneCompanionTest.xcodeproj`，沿用已有 Team、Bundle ID 和全部 Target 的签名配置。确认版本为 1.0.337（337）、内置网页 v1211、原生桥 36。不要先删除手机上的旧 App，以免丢失本地记录。

## 本次修复

- 私人版门锁执行后，即使模型把 `[智能家电|门锁|action=unlock]` 混进改写或原始输出，也会在生成聊天气泡前再次移除。
- 升级后会清理以前已经误存成角色气泡的同类内部记录；用户自己发送的文字不会被改动。
- 门锁仍会照常执行，角色自己的正常回复仍会显示。
- Face ID、安全状态回读和门锁事件通知规则不变。
- 网页版本与卧室小灯页面没有改动。

## 当前状态

这是 Mac 待编译源码，不是 IPA；当前 Windows 环境无法完成 Xcode 签名、安装和真实 iPhone 验证。安装后请验证：让角色开锁成功时，只出现角色回复，不再出现内部控制记录。
