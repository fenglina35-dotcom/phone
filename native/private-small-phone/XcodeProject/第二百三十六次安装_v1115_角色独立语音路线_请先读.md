# 第二百三十六次安装：v1115 / 私人 iOS 1.0.236（236）

本包是完整 Mac/Xcode 源码包，递归包含当前完整 `PhoneWeb.bundle`，不会覆盖任何历史安装包。

角色设置的“聊天与主动”新增“语音音色与路线”。每个角色都能独立选择跟随当前默认路线，或固定使用语音路线一至四；每条路线还会为这个角色单独保存音色 ID。切换一个角色的官网、模型或音色，不会连带改变其他角色，也不会修改共同生活使用的模型路线。

升级前没有单独设置语音路线的角色继续跟随全局默认路线，并保留原有音色；只有主动给角色固定路线后，才改用该路线的接口、模型和专属音色。所选路线未完整配置时不会偷偷改用另一个官网。

本包同时保留当前工作区已完成的朋友圈真实内容感知、多角色评论去重、红色禁言提示与“去求ta解锁”，以及 v1111 已确认成功的微信／共同生活双向连续剧情、后台主动消息、通话、真人好友、媒体、微信总结、外卖、软件锁定和安卓安全启动功能。

Windows 已运行自动化、语法、版本、Bundle 字节一致性、实际浏览器点选保存与 ZIP 结构检查；这不等于 Mac 已编译签名，也不等于真实 iPhone 已验证。请在 Mac 上打开 `PhoneCompanionTest.xcodeproj`，选择私人 App scheme 后重新签名安装。
