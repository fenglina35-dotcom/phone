# 私人「小手机」iOS App

当前候选私人安装版本为 1.0.141 (141)，内置网页核心 v1020，原生桥 25。本版依据真机卡顿时“自定义本地图片同时黑屏、点击长延迟、后台入聊停住”的共同现象，修复私人 App 的本地图片恢复链路：每次重绘立即注入已经缓存的图片；暂时缺失的图片只在空闲时分批读取，并按失败次数退避，不再随每次点击重复整批访问 IndexedDB。Windows 全量自动回归 914/914；尚未在 Mac 编译、签名或在真实 iPhone 验证图片恢复、点击延迟、持续温度与后台入聊。

当前候选私人安装版本为 1.0.127 (127)，内置网页核心 v1006，原生桥 25。本版查明后台消息失败的真实原因：独立伴生云只有 APNs、没有模型提供方，同时私人隔离包装丢失 keepalive，旧界面又只认入队而没有核验模型与 APNs。v1006 同步当前角色实际使用的 HTTPS 模型线路、返回真实任务投递状态，并在关闭功能时清除线路；电话使用有界历史与相关记忆，朋友圈保存准确回复目标并禁止假回复。Windows 完整回归 857/857，独立云函数已部署；Mac 编译、签名和真实 iPhone 的电话、朋友圈、后台通知仍须验证。

当前候选私人安装版本为 1.0.121 (121)，内置网页核心 v1000，原生桥 25。本版恢复 v999 已验证的角色朋友圈时间轴与详情样式，修复点赞跳屏、巨大弹层、评论丢失、删除入口和上下文回复；角色微信设置改为五个独立直达分类。普通商品文字不再误改项圈头衔；远程操控保留原始目标与近期微信上下文，并限制模型等待，避免长时间停页。角色读取真实位置会等待新鲜定位结果，并把本次位置读取的进行中、成功或失败状态写入伴生设备操作记录。Windows 自动回归通过后仍须在 Mac 编译、签名并用真实 iPhone 验收，不能冒充真机通过。

当前候选私人安装版本为 1.0.100 (100)，内置网页核心 v978，原生桥 23。v978 在用户明确要求“全部锁定／解除全锁”而模型只返回“仅内置”执行标签时，仍按用户本轮真实指令升级为内外完整动作；模型拒绝、动作相反、问句和条件句不会误触发。通话记录删除或编辑单句后会保留原展开状态和阅读位置。四套透明玻璃图标主题的组件透明度偏好不再被固定主题选择器或私人 App 实体底色覆盖。Windows 自动回归通过后仍须在 Mac 编译、签名并用真实 iPhone 验收全锁回执、通话记录和透明度变化，不能冒充真机通过。

当前候选私人安装版本为 1.0.87 (87)，内置网页核心 v965，原生桥 23。本版把角色基础人设、世界书、真实事件与记忆置于稳定优先级，移除极端依恋、数值心情、上下位与随机活动等替角色做决定的旧控制器，同时保留挂断、拒接、失联、共同经历和角色主动调用电话、礼物、约会、游戏、音乐、放映室等能力。微信外部屏幕共享在角色说话时复用已激活的媒体音频会话，不再暂停抖音等正在播放的内容；受保护平台仍只能逐帧识别当前可见画面和烧录字幕，不能冒充提取完整字幕轨。私人 App 首页的大面积玻璃组件使用低风险合成路径，降低滑动、发热和内存压力下内容层消失及全页失去点击的问题。云端主动联系会读取角色已有的真实上下班作息，在到点后由角色按人设和记忆自然通知，不恢复随机“当前活动”。Windows 自动测试通过后仍需在 Mac 编译、签名并用真实 iPhone 验收，不能把静态检查写成真机通过。

当前候选私人安装版本为 1.0.79 (79)，内置网页核心 v957，原生桥 18。本版在音乐首页和在线搜索页接入网易云官方外链播放器：可粘贴 `music.163.com` 的歌曲、专辑或歌单完整链接，也可直接填写歌曲 ID，在小手机内打开网易云允许外链的内容；最近打开记录保存在本机。私人 WKWebView 只允许 `https://music.163.com/outchain/player` 子框架留在 App 内，其他远程导航仍交给 iOS 打开。Apple 中国区试听、Audius 公开曲库、本地 MP3/M4A、歌单导入导出和一起听原链路均保留；本版不保存网易云账号、抓取音源或绕过会员与版权限制。Windows 自动回归 665/665 通过，Mac 编译与真实 iPhone 播放验收仍需进行。

当前正式私人安装版本为 1.0.75 (75)，内置共享网页核心为 v953，原生桥契约仍为 18。本版把已验证的小号报备交付修复同步进私人 App：主号只接受明确包含小号名称和联系动作的真实报备，旧话题续写、泛化措辞、否认或空回复都不会误判成功；首次直加好友遇到兼容接口 HTTP 400 时会回退到本地自然开场，不再把配置错误当角色消息显示。小号身份隔离、统一自然系统、主动消息和电话等既有功能保持不变。Windows 自动测试通过后仍须在 Mac 使用 Xcode 编译、签名并做真机验收。

这是私人真实 iOS App 工程，不是远程网页壳。`XcodeProject` 已从用户 2026-08-11 提供的真实 Mac 工程导入，保留主 App、Report、Monitor、Shield 和通知扩展。主界面从安装包内的 `PhoneWeb.bundle/index.html` 加载共享网页核心（由 `小手机.html` 自动生成，不维护第二份业务源码）；Family Controls、Device Activity、Managed Settings、Keychain 和后台同步仍由原生 Target/扩展提供。

私人副本沿用真实工程已经签名可用的 Bundle ID、App Group、Apple Team 和扩展 ID，仅把显示名称改为「小手机」。它会替换本人设备上同 Bundle ID 的 North/伴生开发包，因此两者无法同时安装和控制；公开审核包的工程与提交不在这里修改。

当前私人安装版本为 1.0.19 (19)，内置共享网页核心为 v894。1.0.19 在同一 App 本机直连上保留独立 `readSessionId`、逐字段/逐 App 进度、原生与健康超时、心电概要、真实数值一致性守卫和明确手动解锁事件，并补齐角色主动提到电量、屏幕、App 用量、健康或位置时的真实读取触发；没有本次读取凭证时，“没刷新”等猜测会被拦截。普通网页查岗仍走微信、朋友圈、抖音等经典逐项横幅，不会被外置读取分支截走。统一 App 会在手机号账号认证后直接登记自己的设备身份和 APNs 令牌，角色资料页可核验 token、profile、cron 和下一次检查。本机命令仍必须收到 `stage = executed` 才能说成功。共同生活全天主动联系仍要求每次会话后随机安静 30–60 分钟；间隔 1 分钟只在该静默结束后生效。界面仍只显示手机号；密码不保存，令牌只存 Keychain。Mac 编译、迁移 003/004、Edge/APNs 部署及真机验收仍未完成。

当前私人安装版本为 1.0.20 (20)，内置共享网页核心为 v895。电量继续直接读 `UIDevice`，位置继续读 CoreLocation；HealthKit 在角色明确读取时会真正启用。屏幕总时长和逐 App 数据在中国等无法取得 `approvedWithDataAccess` 的地区，改由工程内既有 `DeviceActivityReport` 扩展按本次请求编号回传，不再只尝试欧盟限定的直接 API。读取全部会逐项列出电量、总屏幕、逐 App、步数、睡眠、心率、心电、HRV 和位置，未读到的项目必须说明真实原因。Supabase 迁移 003–006 与 `phone-role-push` 已部署，统一控制器同时兼容旧 `p_apns_env` 和新 `p_apns_environment` 参数。彻底清空会等待本地大聊天归档、共同生活上下文和服务端主动联系记忆全部清除。Windows 自动测试已通过；Mac 编译、签名和真机全链路验收仍未完成。

当前私人安装版本为 1.0.21 (21)，内置共享网页核心为 v896。用户要求读取全部时，聊天或通话中的普通模型回复不能再把范围缩成位置或电量；原生端在各请求读取器结束后写入同一轮完成回执，网页收到并核对后才允许角色逐项报告九类结果。后台报 `controller_user_id does not exist` 的远端前置迁移 002 已实际应用，四个 controller 字段与新旧统一控制器 RPC 已核验存在，迁移 002–006 均在远端记录中。Windows 475 项自动测试通过；Mac 编译、签名和真机全链路验收仍未完成。

当前私人安装版本为 1.0.23 (23)，内置共享网页核心为 v898。微信聊天、电话和共同生活共用同一把真实读取锁。用户说“查一次所有的”“再刷一遍全部数据”等自然说法时，读取会在普通模型前排队并立即变更生成代次；读取期间已经在生成或已经生成但尚未发送的普通回复全部作废。顶部横幅逐项显示正在读取什么，原生同一轮完成后才把成功事实交给角色，并只允许角色用自己的语气自然说一次。未返回项目和技术失败原因仍只留在内部。Windows 476 项自动测试全部通过；Mac 编译、签名和真机全链路验收仍未完成。
当前私人安装版本为 1.0.24 (24)，内置共享网页核心为 v899。微信聊天、电话和共同生活继续共用真实读取锁；门禁导致的正常静默不再触发“模型没有生成”重试提示。完成回执后只把真实事实交给角色模型，角色自行决定重点、句数以及一条或多条发送，程序不再代写固定清单；模型输出不安全时重新生成，仍不合格则保持静默。线下回应失败会显示可核对的余额／额度、鉴权、限流、配置、网络超时或空输出原因，失败不改原对话。Windows 478 项自动测试全部通过；Mac 编译、签名和真机全链路验收仍未完成。

当前私人安装版本为 1.0.25 (25)，内置共享网页核心为 v901。设置里的苹果兼容开关开启后，远程控制顶部横幅在私人 App 内额外下移 14px；关闭时保持旧位置，Android 不变。视频通话右下角增加小型真实摄像头按钮，可关闭、开启并翻转前后镜头；偏好设置可控制自动识图间隔和每次通话识图次数，口头说“你看一下／你看看这个东西”也会识别当前画面并由角色自然说话。Xcode 工程已声明相机用途、允许内联视频并只给安装包内页面相机／话筒权限。主动消息线上已增加 MiniMax 第三备用线路并验证到期正文连续两次由 APNs 接受；Windows 483/483 自动测试通过。仍需 Mac 编译、相机与横幅真机检查，以及真实 iPhone 前台、后台、上划退出三种通知可见性验收。

当前私人安装版本为 1.0.26 (26)，内置共享网页核心为 v902。视频画面回复改为独立高优先事件，不再带入旧用户话语；角色第一句必须说出当前画面的具体细节，不合格会自动重说。定时识别间隔改为分钟，次数上限只约束自动识别；用户口头要求角色看画面时每次重新截帧且不限次数。中央识别系统提示已删除，右下角只在工作时显示小转圈，失败短暂显示“失败”。仍需 Mac 编译及真实 iPhone 摄像头、翻转、分钟定时、主动不限次和语音回应验收。

当前私人安装版本为 1.0.27 (27)，内置共享网页核心为 v903。v902 隔离旧对话时把画面轮次做成了纯系统消息，部分兼容聊天接口因此返回上游异常；v903 只修正这层封装，把当前画面恢复为本轮唯一的 user 消息，正常聊天、识图接口、摄像头与其他通话逻辑不变。角色回复仍不得复读旧话，回复失败只在右下角显示“失败”。

v903 授权补充：保留第一次正常邀请码与扫脸／指纹恢复；删除设置中的生成迁移码、生成备用恢复码以及登录页代码恢复入口。Safari／Edge 合并也只允许本人扫脸或指纹，云端拒绝旧版本的迁移码、备用恢复码和本机身份自动恢复请求。

当前私人安装版本为 1.0.28 (28)，内置共享网页核心为 v904。视频摄像头自动识图间隔填写 0 后会立即撤销已运行的定时器，并在定时回调和识图入口再次拦截，因此只保留用户口头要求角色看画面的不限次数识图；全新安装默认也是 0。后台主动消息的每个气泡使用独立通知标识，不再折叠成“多个通知”，通知卡直接保留角色昵称和当前句正文。角色头像同步会先从大容量图片存储重新读取真实头像，通知扩展等待通信意图捐赠完成后再更新卡片，避免退化成昵称首字占位。iOS 系统仍会强制把通信头像裁成圆形并叠加 App 来源小图标，应用无权改成方形或移除来源标识。

当前私人安装版本为 1.0.30 (30)，内置共享网页核心为 v906。苹果兼容开关开启时，视频通话顶部角色备注和时间整体下移，关闭及安卓保持原位；聊天心情条在角色消息到达后实时更新。电子宠物最多四只，支持两天未喂变瘦、超过三天生病、商城购买恢复药和手册用药。普通回复退出后的服务器兜底缩短为一分钟，一分钟测试会明确显示链路缺项与任务编号；App感知新增立即真实测试，识别后通知角色，未回复五分钟后继续询问或锁定，测试不计正式次数和90分钟冷却。迁移 202608130001 与 phone-role-push 已部署；iOS 上划强退仍不保证静默唤醒，旧快照不得冒充当前数据。

当前私人安装版本为 1.0.31 (31)，内置共享网页核心为 v907。App立即查看不再被正式每日次数0拦截；一分钟测试、回复接力和设备读取接力不会因退后台后的瞬时状态或其他任务种类被取消，聊天先持久化后撤销兜底。网页离线更新校验已移除对旧迁移码函数的依赖，解决公开网页停在v901。语音翻译框可独立配色，角色消息支持单条删除；宠物每30天进化、可退化、睡眠回窝，“让TA照顾”会显示真实动作。迁移202608130002与phone-role-push已部署，Windows自动回归499/499通过；Mac五Target编译和真机通知仍需验收。

手机号功能启用前，不需要打开 Phone Provider，也不需要 Twilio 或短信服务。只需执行 `supabase/migrations/202608110001_private_phone_accounts.sql`，再在 Supabase Authentication 中手动建立一名已确认的私人用户：内部邮箱格式为 `smallphone.86手机号@example.com`，例如手机号 `13812345678` 对应 `smallphone.8613812345678@example.com`。App 登录页仍输入原手机号和该用户的密码。

当前私人安装版本为 1.0.44 (44)，内置共享网页核心为 v920，原生桥契约仍为 18。v919 在真实 iPhone 的普通电话和普通视频里仍然只有用户字幕，角色没有声音和角色字幕，因此不能记为完成。v920 对照 v800、v850、v907，恢复角色忙碌期丢弃识别、尾音隔离、角色播音前暂停识别和播音后重建；普通前台恢复网页 audio，只有后台或 PiP 才使用原生 call.audio.play。字幕动画没有修改。Windows 566 项自动测试通过，仍需 Mac 编译和真实 iPhone 验收。

当前私人安装版本为 1.0.46 (46)，内置共享网页核心仍为 v921，原生桥契约仍为 18。本版只把已确认的黑／灰白／粉白／蓝白四套高级玻璃主题、组件玻璃色调和透明度调节、可播放并随播放状态旋转的唱片组件写入私人 App；浅色三主题的存储圈余量色变浅，黑色主题不变。正式网页版没有上传，通话、字幕、识别与屏幕共享没有修改。

当前私人安装版本为 1.0.47 (47)，内置共享网页核心仍为 v921，原生桥契约仍为 18。本版把主屏固定为黑／灰白／粉白／蓝白四套高级玻璃主题：删除旧组件和重复快捷方式，恢复旧版微信好友页，支持 App 与三种保留组件自由混排并持久化位置，整体 App 外框放大，修正照片上传点击、黑色透明玻璃、浅色图标边缘、主题色与透明度调节和默认黑色玻璃。音乐播放器圆形唱片复用主屏组件的四套金属纹理，不复制组件方框、不改变唱片或封面尺寸；主屏与播放器唱片统一为 24 秒一圈，并继续只在播放时旋转、暂停时停止。正式网页版没有上传，通话、字幕、识别与屏幕共享没有修改。

当前私人安装版本为 1.0.53 (53)，内置共享网页核心为 v927，原生桥契约仍为 18。本版修复 iPhone 浏览器添加主屏幕后第一页应用名称被截断、底部安全区出现黑条，以及美化包导入后覆盖接收方主屏排列的问题。第一页面应用槽现在为图标与完整文字预留固定行高；苹果安全区外壳只在“苹果兼容适配”开启且处于 iPhone / iPad 主屏幕版时生效，安卓与私人 App 不受影响。美化包继续传递壁纸、图标、头像、气泡和音乐外观，但不再导入或导出主屏页面、组件和 Dock 的位置。通话、字幕、识别、屏幕共享与微信聊天逻辑没有修改。

## 目录

- `XcodeProject/`：可在 Mac Xcode 打开的真实六 Target 工程（含 ReplayKit 屏幕共享扩展）。
- `XcodeProject/PhoneCompanionTest/`：私人入口、本地 WKWebView、原生桥和伴生能力。
- `Contracts/`：服务端“单设备单控制器”租约契约；接入前不得让两套 App 同时下发命令。
- `Resources/private-phone-web.manifest.json`：从仓库共享核心打包的文件清单。
- `scripts/stage-private-phone-web.mjs`：生成 Xcode 资源目录，不产生第二套业务源码。

## 暂存网页资源

在仓库根目录执行：

```sh
node native/private-small-phone/scripts/stage-private-phone-web.mjs native/private-small-phone/XcodeProject/PhoneCompanionTest/PhoneWeb.bundle
```

输出 `PhoneWeb.bundle`。Xcode 的同步目录会把它作为资源包加入私人主 Target。每次网页核心变更后重新生成；不要直接编辑生成目录。

工程内已提交共享 `PhoneCompanionTest` Scheme。Mac 解压并打开 `.xcodeproj` 后应直接选择真实 iPhone 运行主 App，不再手动创建 Scheme，也不要误选 Report、Monitor、Shield 或通知扩展运行。

## 当前证据边界

- 审核中的公开 North 源码与提交没有修改；这里只维护私人副本。
- 已从真实工程确认 Team、Bundle ID、App Group 和五个 Target，不再使用猜测值。
- MapKit 看门狗日志命中真实 `ContentView.swift` 的常驻 `Map`；私人副本在 scene 进入 inactive 时移除地图，保留独立的定位记录。
- Windows 无法执行 Xcode/Swift 编译，最终仍须在 Mac 编译全部 Target 并真机回归。

当前私人安装版本为 1.0.35 (35)，内置共享网页核心为 v911，原生桥契约为 14。ReplayKit 在原生语音完成时冻结当刻系统画面，网页恢复后按一次性令牌读取，避免回到小手机后误识别通话页；私人通话退后台不再由网页主动关闭原生听筒，连续语音分段期间保持录音会话，后台最终语音会在回到小手机时补投。角色共享申请固定覆盖在通话界面上。后台 App 查看收到 view 命令后会真实刷新 Screen Time 并用请求后的新快照回执，不再回传内存旧值；每日限额重建明确纳入当日此前使用量。Mac 六 Target 编译、签名及真实 iPhone 后台麦克风、系统共享、Screen Time 和每日限额阈值仍需最终验收。

当前私人安装版本为 1.0.39 (39)，内置共享网页核心为 v915，原生桥契约为 16。ReplayKit 会把宿主后台取得的外部 App 帧放入独立的一次性交接槽，网页实时识别优先冻结该帧，减少切回小手机后被前台自身画面覆盖。实时共享理解新增角色自主观察状态：可以静默继续、只问一个问题并等待用户回答、再决定继续或结束；口头让角色看画面仍不限次数，自动次数上限和设置开关不变。系统 PiP 内容高压到 144，名字上移并给字幕留出四行空间；应用根视图透明，但 iOS 是否保留系统黑色背板仍需真机验收。Audius 公开搜索会过滤不可用或受流限制歌曲并只重试一次，本地音乐和一起听不变。Mac 六 Target 编译、签名、后台麦克风、真实外部 App 共享以及 PiP 系统合成仍需最终验收。

当前私人安装版本为 1.0.40 (40)，内置共享网页核心为 v916，原生桥契约为 17。实时共享理解已撤销固定 4 秒轮询：每一帧之后必须由角色明确选择继续及下一次间隔，也可以暂停提问、等待用户切换页面或主动结束；程序没有合法选择时默认结束，不能擅自继续扫描或强制说话。外部媒体、话筒和角色语音改用混音会话；共享识图明确忽略小手机自身悬浮通话框。通话字幕恢复逐字渐显、上浮，语音最终提交增加纠错等待；音乐搜索并行使用 Apple 中国区官方试听与 Audius 公共曲库，无需登录。共同生活状态会按已保存作息真实推进；宠物自然成长不再倒退。Windows JavaScript 语法检查和 545 项自动回归全部通过，Mac 全 Target 编译、签名及真实 iPhone 的 ReplayKit/音频混合仍需最终验收。

当前私人安装版本为 1.0.41 (41)，内置共享网页核心为 v917，原生桥契约仍为 17。网页大通话与原生 PiP 字幕共用内容、说话人和全部动画参数；原生识音启用语音处理，角色播音期间持续监听并排队处理用户插话，PiP 字幕更新不再反复重启音频会话。苹果兼容仅在开启时下移宠物返回键和角色微信登录顶部按钮。共同生活可把角色答应发送的内容真实写入角色微信或其参加的 AI 群；真人群新增仅群主可执行的云端原子解散，AI 群中的 AI 发言仍不可编辑。群解散迁移 `202608140001` 已在现用 Supabase 项目部署并登记。AI 账户首屏加入内置人工服务费与外置自选说明。Windows JavaScript 语法检查和 555 项自动回归全部通过；Mac 全 Target 编译、签名及真实 iPhone 的外部视频声、角色声、话筒、字幕和 PiP 仍需最终验收。

当前私人安装版本为 1.0.43 (43)，内置共享网页核心为 v919，原生桥契约仍为 18。普通语音和视频通话的每段角色语音在创建 AVAudioPlayer 前重新激活 playAndRecord/voiceChat/mixWithOthers 会话，恢复 v917 已验证的角色出声保障；免提最终识别与忙碌期排队也回退到 v917 路径，撤销 v918 会误伤真人话语的新增网页门禁。v918 已确认的整句字幕动画保持不变。微信输入框改为 iOS 不触发缩放的 16px，并让输入栏固定在功能／表情面板上方，打开面板先收起键盘。Windows 静态回归通过后仍必须在 Mac 和真实 iPhone 验收普通语音、普通视频、角色连续发声、免提对话、静音环境和微信输入／表情面板。


当前候选版本为私人 iOS 1.0.139 (139)，内置网页核心 v1018，原生桥 25。v1018 处理私人 App 点击后的主线程过载：大聊天归档未变化时不再重复整库序列化，普通保存优先在空闲时执行；前台恢复同步合并防抖；未启用屏幕共享时原生轮询降频，DeviceActivityReport 只在真实 Screen Time 读取窗口挂载。Windows 906/906 通过；Mac 五 Target 编译、签名和真实 iPhone 温度、点击、长聊天保存、Screen Time 与 ReplayKit 仍待验证。
