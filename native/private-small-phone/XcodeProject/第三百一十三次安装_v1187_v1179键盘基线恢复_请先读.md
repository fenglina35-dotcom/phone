# 第三百一十三次安装 私人 v1187 v1179 键盘基线恢复版

本目录是私人 iOS `1.0.313 (313)`、内置网页功能基线 `v1184` 的 Mac 待编译源码。原生桥 35 保持不变。本次只处理私人 App 的键盘承载和私人 Bundle 的线下末尾锚定，没有修改或推送公开网页版。

## 本次修复

- 逐项对照 v1179 到 v1186 的键盘改动，恢复 v1179 的完整宿主链，不再把后续失败方案叠加在一起。
- 微信恢复 v1179 的私人布局契约：直接 `WKWebView`、稳定全高宿主、私人 viewport 不声明 `interactive-widget`，并只让 `.chat-inputbar` 使用原有的 absolute 兼容规则。
- 共同生活和线下约会的 `.offinput` 不再借用微信定位规则；删除全局键盘导轨、键盘通知、外层滚动开关、偏移重写及其他原生键盘接管。
- 保留 16 像素输入字体、触摸去重和旁白切换不重复聚焦。点线下输入框时会先把内部消息区定位到最后一条，避免从前面几条消息的位置开始上推。

## Mac 与真机验收

1. 不要先删除手机上的旧 App，也不要覆盖旧工程目录。在 Mac 上把本次 ZIP 解压到新目录，再用 Xcode 打开 `PhoneCompanionTest.xcodeproj`。
2. 确认签名 Team 和 Bundle Identifier，编译安装后检查原生诊断版本为 `1.0.313 (313)`；设置页显示内置网页功能基线 `v1184` 是正常现象。
3. 微信至少重复两轮：进入最新消息底部，点输入框、输入文字、收起键盘、再次打开。确认最后一条消息保持在输入栏上方，页面不跳旧消息，输入栏与键盘同步移动。
4. 共同生活和线下约会至少重复三轮：首次点输入框、收起、再次打开。再把消息区向上滑到旧消息后点输入框，确认会先回到最后一条，并从真正底部随键盘一起上移。
5. 键盘保持打开时反复切换旁白与普通输入，确认不会收起重开、不会闪黑、不会多一次弹跳。再回微信复测一次，确认线下逻辑没有泄漏过去。

Windows 自动测试只能证明源码作用域、版本契约和 ZIP 结构，不能代替 Mac 编译签名及真实 iPhone 键盘动画验收。
