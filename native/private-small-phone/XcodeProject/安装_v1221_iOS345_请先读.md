# v1221 私人空调编译修复源码包 iOS 1.0.345 (345)

本包修复 v1220 用户在 Xcode 实际遇到的红色错误：HomeKitClimateBridge 对可空 linkedServices 直接 for-in。现在使用 linkedServices ?? []，没有关联服务就跳过，原本优先查直接服务、随后查关联服务、最后返回不可用的顺序不变。

保留 v1220 全部私人整合内容：智能空调与角色提示词、灯与门锁、卡顿相关存档合并写入与性能诊断、通知头像保护、电量健康刷新、锁定名字和批量解锁、外卖及现有共享功能。原生桥37，性能诊断334。公开网页保持 v1216，公开 North App 不在本包。

## 使用前

这是 Mac 待编译源码，不是已签名 IPA。解压到全新目录，打开新目录中的 PhoneCompanionTest.xcodeproj，不要继续编译旧 v1220 工程。不要先删除旧 App，不要覆盖旧工程目录。沿用原 Team、Bundle ID、签名和权限；六个 Target 的 Debug/Release 均应为 1.0.345 (345)，安装后显示 v1221。

通知头像保护的私人后台迁移仍未获确认、未部署。包内“后台先行/202609090001_notification_avatar_preserve.sql”必须先应用到私人项目并核验，再安装使用本候选；不要改公开后台。这与本次 Swift 编译错误是两个独立事项。

Windows 静态回归不等于苹果 SDK 编译；本包尚未在 Mac/Xcode 构建签名或真实 iPhone 验收。截图另外两条黄色并发隔离警告不在本次单行修复范围内，保持旧行为；如出现其他红色错误，请保留完整错误和行号。卡顿改善和通知头像恢复也仍需真机验证。
