"""Append release decisions without replacing any maintenance history."""
from pathlib import Path
from docx import Document
ROOT=Path(__file__).resolve().parents[1]
TITLE='2026年9月8日 v1213 公开North迁移、云端快捷指令与素材恢复'
ROWS={
 'AI开发项目_项目说明文档.docx':[
 '公开 North 仅迁移商店 1.0（8）已支持能力：步数、电量、屏幕使用、稳定ID应用控制、统一限额及授权位置/足迹。公开适配的连接凭据和状态独立存储，不读取私人伴生缓存。页面、正常聊天、共同生活和主动消息提示词均接入公开能力说明。公开原生没有手动解锁事件，不声称可迁移。私人原有手表健康、智能家居和原生适配保留。',
 '两边共有新增后台通知历史和设置内快捷指令入口，支持代发用户消息／报告事件。独立云端队列及既有服务器心跳执行，不依赖页面常开；使用已配对North连接鉴权。模型资料创建时经明确授权同步，以AES-GCM加密存储；触发Token只存哈希，可撤销，重复eventId不重复生成。默认5分钟冷却、北京日24次上限，失败不自动重试模型，回页面后按原时间持久化及确认收件。更换模型或清除记忆需撤销旧规则重新创建。',
 '像素少女两边共有：缓存写入失败不阻断网络响应、衣柜素材90秒超时及重试、图片3并发加载、缺少decode与OffscreenCanvas时回退。重试须通过父页面新会话标识与保存序号，保留衣柜参数、套装与照片。网页/私人内置v1213，私人iOS1.0.339（339），桥36。'],
 'AI开发项目_Bug记录模板.docx':[
 '旧公开报告缺usageDay、时区和完整读取会话元数据，直接套用私人解析器导致有效数据被丢弃。新增公开专用事实适配：保留原采集时间、不假定今天，未知不是0；当前公开步数0也可能代表未取得数据。重复快照仍接受新的命令回执，控制快照不清空旧使用报告。',
 '核心聊天回归发现扩展伴生可用性后，未授权网页也会请求后台完成接口；通过公开RPC绑定及逐角色云授权门禁阻止，非模型请求不会额外扣费。实际浏览器再次通过有授权、旧笔记、原文开关、普通发送、手动回复、查手机和持久化；私人测试明确注入原生壳标志，HTTP模型和云接口分别模拟，不再把打开私人文件路径误认为已进入私人运行分支。',
 '快捷指令SQL在事务回滚校验中捕获PL/pgSQL记录变量j和UPDATE别名j歧义，改为不同别名后公开与私人数据库事务校验通过；测试不留样例记录。云worker测试包含外部主人拒绝、加密、单次非流式生成、空回复无重试、撤销/去重/配额。未做真实用户付费模型、锁屏快捷指令或iPhone通知验收。',
 '素材故障回归包含缓存读写失败、图片接口缺失、素材失败后重新进入、重试后照片写入父存档、file协议保存恢复。Windows Edge夹具不等于故障用户安卓真机。最终测试数量、部署状态和ZIP哈希见v1213发布记录。'],
 'AI开发项目_Bug修改规范.docx':[
 '跨公开/私人迁移必须以实际商店二进制能力为边界，允许旧协议适配但不补造缺失元数据。新增共享模块必须镜像私人包并缓存校验；任一可选适配未加载时不得阻断核心聊天。',
 '云自动化需服务器幂等领取、事件去重、配额、独立主人鉴权、加密凭据和撤销检查。响应不确定不可自动再次生成扣费。入聊成功持久化后才能确认云收件，用户后来删除的消息不能被旧记录复活。',
 'SQL上线前在事务回滚中执行真实函数，不能以语法或源代码正则代替数据库执行验证。新增代码的浏览器测试应验证实际运行标志、隔离状态和失败回退。没有Mac/Xcode与真机证据，私人ZIP只能称待编译源码包。']}
for name,rows in ROWS.items():
 p=ROOT/'docs/maintenance'/name;doc=Document(p);old=[x.text for x in doc.paragraphs]
 assert TITLE not in old
 doc.add_page_break();doc.add_paragraph(TITLE,style='Heading 1')
 for row in rows:doc.add_paragraph(row)
 doc.save(p);assert [x.text for x in Document(p).paragraphs][:len(old)]==old
 print(name+': appended; historical paragraphs retained')
