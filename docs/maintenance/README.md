# 私人 v1214 电量与睡眠刷新

2026-09-09：私人内置v1214、iOS340、桥36。修复电量缓存新时间及伴生页未刷新健康数据；公开网页和公开North不变。全仓1694项及双入口核心聊天通过，实际打包推送与真机边界详见 [修复记录](v1214_私人电量睡眠刷新.md)。

# 后台通知隐藏心情标签

2026-09-09：公开与私人通知后台已更新，只在通知发送前移除心情元数据，保留原文同步；无须重装或更换 v1213 客户端。全仓1689项及双入口核心聊天回归通过，真实新锁屏通知待用户确认。详见 [修复与部署记录](2026-09-09_后台通知心情标签修复.md)。

# v1213 公开伴生、快捷指令与游戏素材恢复

本轮共享功能已经实现并完成隔离验证，公开/私人云分别更新。网页与私人内置 v1213，私人 iOS1.0.339（339），桥36。公开 North 仅迁移已支持能力；私人智能家居与手表健康不进入网页。详见 [v1213 发布范围与核验](v1213_发布核验.md)，是否正式发布以发布结果和远端核验为准。

# v1212 请求诊断与简洁时间

本轮授权汇总已完成改动打包推送：网页与私人内置页 v1212，私人 iOS1.0.338（338），桥36。请求故障诊断、短北京时间、此前纯英文与心情格式为两边共有；最新门锁内部标签隐藏仅私人。未完成快捷指令草稿继续排除。详见[v1212 逐项范围与核验](v1212_发布核验.md)，实际部署与产物以发布结果为准。

# 当前未发布 纯英文拦截与心情格式

2026年9月8日：用户授权修改两边共有的纯英文无翻译拦截及心情标签格式要求，开关两态均生效，覆盖线上、线下、电话与后台。空回复暂不修复，其余关闭时保留旧处理。仅本地代码，未推送、打包或部署；详见[本轮范围与验证](2026-09-08_纯英文拦截与心情格式_未发布.md)。

# v1211 发布范围与核验

2026年9月8日用户确认：两边共有的北京时间气泡更新网页并进入私人包；智能家居新改动仅私人。私人后台模型拒绝说明隔离和既有卡顿修复继续仅私人。候选网页/内置页v1211，私人iOS1.0.336（336），桥36。未完成快捷指令草稿不提交、不打包、不部署。

全仓1636/1636和双入口核心聊天、原文多场景、后台收件与时区可见性检查通过，模型/设备通道均隔离模拟，不等于真机验收。详情见[v1211 发布核验](v1211_发布核验.md)，实际包和部署结果见父项目目录的发布结果_v1211_2026-09-08.md。三DOCX仅追加且历史保留；当前依赖缺少soffice.exe，视觉渲染未完成。

以下为历史记录。

# v1205 私人工程包源码候选 角色穿搭与自定义衣柜

两边共有：2026年9月7日用户已授权网页推送及私人源码打包。网页 v1205，私人内置网页 v1205，私人iOS 1.0.326 (326)，原生桥35。此次发布沿用已验收的相同运行代码；新增打包脚本和发布说明。是否上线以远端部署和正式网页核验为准。

自定义套装支持起名、穿上、更新、改名、删除和导入导出；保留服装鞋袜发饰发型五官及微调参数。早上08:00由绑定角色按人设从全部衣柜和命名套装挑选衣服、鞋袜、发饰及发型，不再采用程序随机或自定义优先规则。晚上19:00固定兔兔睡衣和配套鞋袜发饰，发型仍由角色选择。身体比例及五官不变。记录实际选择和理由供角色识别。

模型失败保留当前穿搭，重新进入小屋可重试。App关闭时无法保证后台准点执行，恢复前台补当前时段。角色选择调用既有模型线路，不修改用户模型和密钥配置。用户正在编辑时不应用迟到选衣结果。

修复初始双小辫和月牙双小揪揪白边，保留原画布及定位。衣柜遮挡保存的外层叉号已移除；主屏返回缩为两个字，比例紧邻金币左侧，左右四排图标对齐，镜子返回按钮分行。

Windows全仓1591项检查通过，网页与私人浏览器的命名保存、配置恢复、20项照顾流程、角色穿搭管线和画面同步通过。模型响应为测试夹具，尚未真实模型服务、Mac编译签名或真实 iPhone验收。包内源码和资源须通过逐文件校验，此ZIP不是IPA，不称真机可运行版本。

在Mac解压到新目录，打开PhoneCompanionTest.xcodeproj，沿用原Team和Bundle ID，并检查所有Target的签名后编译安装。不要先删除原App，不要覆盖旧工程目录。新包名：小手机_v1205_私人版_iOS326_角色穿搭与自定义衣柜_Mac待编译源码包.zip。

以下为历史记录：

# 项目长期维护资料

2026-09-06 两边共有 v1195 / 私人 iOS1.0.317 (317) / 桥35。按用户新要求，聊天和软件从进入计数改为触发计数：达到阈值、必要互斥放行及授权有效时只加一次；短暂进入、重绘、恢复本身不计数。旧watchDaily没有triggers模式标记则一次性重置，不能把历史打开次数冒充触发次数；每天当地午夜重置。角色提示明确为第几次发现，不是第几次实际打开。已触发但模型失败仍属于一次触发，不自动重发。聊天10–60秒、软件20–60秒保持。 用户补充共同生活后台回来让TA回失效、强退恢复。复核发现网络超时只覆盖取得响应头，正文等待可以无限悬挂；多人剧场配角先答的异常路径仅清理目标，没有完整释放busy。新增整次模型调用的有界等待及后台恢复取消，取消错误不能被网络重试、副模型回退或保留旧候选吞掉。沿原finally释放忙碌，迟到结果不进入对话，用户重新点击后发新请求；不手工清空真实聊天，不强制刷新。支持方先失败也清理busy与activeActor。真实手机挂起尚待验收。 本次同步私人包v1191–v1194的监管标准内心解析与有效心情保留，清除私人仍存在的innerThoughtMissingAt隐藏判断；保留私人键盘坐标、旁白点击保护、原生能力与旧包。只做明确共享功能的定点同步，未用网页app.js整份覆盖私人适配。网页推送与源码包制作按用户授权进行；新包从干净提交归档，不覆盖旧包。包内双入口、共享监管/剧场脚本、心情及请求生命周期函数均须核对。Mac/Xcode编译、签名、真实iPhone安装仍未进行，此包仅Mac待编译源码候选，不是IPA。 新增触发计数及迁移回归、后台请求取消/迟到/墙钟超时测试、配角先答取消释放测试。Windows全仓最终目标1516项，实际通过结果以本轮执行为准。浏览器故障注入已证明网页及私人内置页后台恢复释放按钮、忽略旧结果、下一次点击送达；真实模型HTTP仅由测试替身控制，不等于真实网络或iPhone已通过。真实计时脚本 check_v1195_watch.cjs 同时检查两边聊天/抖音短停留不计数、第一次触发为1与标准内心送达。

2026-09-06 网页 v1194：用户确认顶部心情整栏消失，重开显示开关无效，要求错误格式保留上一条有效内心。旧逻辑仅在提取失败时不清空当前字段，但没有独立有效副本，显示还依赖当前字段非空；测试注入字段变空即可复现整栏隐藏。实际用户存档的清空来源未确定，不夸大为已取得设备现场。 新增内心格式校验与角色内独立 innerThoughtLastValid / innerThoughtLastValidAt。空值、非字符串、标签碎片、推理标记、仅不可见字符和无效占位词不覆盖有效内心。已有有效显示自动建立可持久化副本，顶部及详情统一读取当前有效值或最后有效值。显示开关开启时不再因缺内容隐藏整栏；完全没有可恢复历史时只显示省略号，不编造心情、不伪造更新时间。下一条有效生成更新副本；关闭开关仍完全隐藏。 范围仅网页v1194；私人保持v1190/iOS316/桥35，未修改未打包。聊天监管10–60秒、软件20–60秒及v1193标准内心解析均保留。新增6项运行时回归；全仓Windows1508/1508通过。scripts/check_v1194_mood.cjs通过：实际解析器遇到嵌套错误标签保留旧内容；空字段保持栏框；通过真实saveNowAsync写存档后退出预览、正常启动重载仍恢复；详情、开关及新有效内容一致；无历史只显示省略号。零页面错误。此为浏览器故障注入验证，不等于用户真机原始故障已复现。

2026-09-06 网页 v1193：修复监管已调用模型却丢弃正常回复的解析错误。buildSystem 明确要求 [内心|…]，旧监管解析器只移除心情等标签，漏掉标准内心标签，随后方括号过滤整轮拒收。新增回归在旧代码失败；现复用普通聊天 stripHiddenThoughtTags 和 wechatInnerThoughtValue，合法正文送达，确认授权及消息新鲜度后才更新角色心情。未解除操作标签/出戏拦截，未增加模型请求或伪造回复。 聊天保持10–60秒、软件20–60秒。用户明确只发布网页，私人仍v1190/iOS316/桥35，不修改、不打包。专项45/45，全仓Windows1502/1502通过。新浏览器脚本 scripts/check_v1193_watch_thought.cjs 使用真实时钟与真实提示词构造，仅模拟HTTP模型，返回标准内心标签，检查聊天和抖音的消息、通知、心情更新及重进页面。测试不等于用户真实模型和真机已验收。 顶部心情消失反馈仍待定位：扫描显示开关和内心写入路径，当前普通提取失败已保留旧值，未发现再次清空已确认内心的路径。不把监管内心解析修复宣称为所有心情消失已解决；还需确认用户是整栏消失还是内容空白，以及顶部心情开关状态。此前模拟响应只用心情标签，与真实提示词强制内心标签不一致，导致假阴性。测试数据必须覆盖真实协议格式，不能仅证明自选返回可用。

2026-09-06，网页 v1192：按用户最后指令，聊天监管改为随机 10–60 秒；软件监管保持 20–60 秒。未到阈值离开重计、每日清零、关闭权限撤销、伴侣自身及共同活动排除均保留。本次只提交网页，私人仍为 v1190 / iOS316 / 桥35，不修改、不打包。 用户确认 v1191 普通微信另一角色聊天停留一分钟仍无消息，但软件已能触发。补测 v1191 真实浏览器保持另一角色聊天页，约58.2秒发起一次请求，四条模型正文保存、顶部横幅可见。模型 HTTP 为模拟服务，尚未复现用户真实模型/设备的聊天失败，不能把调整阈值当作剩余故障已修复。v1192 全仓 Windows 1500/1500 通过；新脚本 scripts/check_v1192_chat_watch.cjs 验证九秒离开不触发、重入重新计时及一直留在另一聊天页的通知。最终浏览器与推送结果以实际执行记录为准。

当前网页候选：2026-09-06，网页 v1191，修复情侣空间监管不触发、有效模型回复被丢弃及达到阈值后切页取消回复。用户明确本轮只推送网页；私人仍为 v1190 / iOS 1.0.316 (316) / 原生桥 35，尚未包含本次修复。Windows 专项 43/43、全仓 1500/1500 通过，真实浏览器计时与模拟 HTTP 模型链路通过，仍待真实模型与手机复验。详见 v1191_监管触发修复验收.md。以下为历史记录，不代表当前线上版本。

当前共同候选：2026-09-06，网页源码及私人内置网页 v1190，私人 iOS 1.0.316 (316)，原生桥 35。情侣空间新增默认关闭的聊天监管、软件监管和软件选择；前台连续停留分别随机 30–60 秒、20–60 秒，离开重置，本地午夜清零次数；排除伴侣自身聊天与共同活动，仅传对象/软件名称和次数，不读取另一角色聊天正文，不显示系统提示。关闭权限或环境改变后旧生成作废，模型失败不伪造、不重试。保留 v1189 私人键盘候选；网页不推送，不宣称线上更新、Mac 编译或真机通过。

当前私人候选：2026-09-06，私人内置网页 v1189／私人 iOS 1.0.315 (315)，原生桥 35；公开网页仍为 v1184，未修改。真实 iPhone 已证明 v1188 只删除触摸聚焦滚动改写仍未解决共同生活输入栏的光标错位、首次弹跳和收起二段回落。复核历史版本后确认，私人共同生活输入栏所在的 `.phone` 仍处于 `fixed` 坐标系，而正常微信输入栏已使用 `absolute` 坐标系；v1189 只在私人内置页面让共同生活与微信共享稳定的几何坐标基础，并增加旁白切换后的短时点击穿透拦截，避免误触消息编辑／删除。微信业务与样式、公开网页、全局 WKWebView 和 SwiftUI 键盘安全区均不改动。Windows 自动测试不能证明真实 iPhone 动画已修复，仍须 Mac/Xcode 编译签名及真机重复验收。

当前共同发布：2026-09-05，网页 v1177／私人内置页面 v1177／私人 iOS 1.0.303 (303)，原生桥 35。普通主动消息不再把已经完成并已有角色回复的上一轮用户话语再次当成待回复：前端同步结构化近期角色消息和完整轮次边界，云端兼容当前中文日期格式并优先采用更新的文字记录；普通主动任务若仍只生成重复补答，最多真实纠正一次后保持静默，不伪造角色消息，`reply_handoff` 的真实未完成回复接力不受影响。共同生活把用户消息当时选择的“对谁说”固化在该轮消息上，整轮生成、提示词和演员顺序均使用这个快照，不再受稍后切换下拉框或异步状态影响；目标已经离场时明确提示，不静默回落到主角。真实外卖对肯德基汉堡、薯条、蛋挞、鸡翅等明确逐项清单逐个搜索、逐个加入，禁止自动替换成四件套／多人套餐或同类重复项；单点蛋挞在有价格证据时选择合格单品中的最低价，找不到明确单品时停止并说明，不模糊代选。网页和私人内置页同步包含上述可见行为；云端主动消息函数需与本次源码一并部署。Windows 自动测试不能代替 Mac 编译、签名、覆盖安装、真实 iPhone、真实模型和真实下单验收。

当前公开审核候选：2026-09-04，North 1.0 (8)。发布范围仅为 `native/public-north-review`、North 公开支持/隐私页和审核备注模板；网页小手机业务版本及 `native/private-small-phone` 私人版本均未修改。针对 Apple Guideline 2.5.1，公开版 HealthKit 授权集合、查询和上传快照现只保留今日步数，已删除活动能量、心率、HRV、睡眠和心境访问；界面及权限用途说明同步改为只声明步数。所有公开工程 Target 构建号统一为 8。Windows 公开审核专项回归不能代替 Mac Archive、签名、真机授权页检查和 App Store Connect 新二进制审核。

当前网页发布基线：2026-09-02，网页核心 v1149，缓存身份为 `v1149-mood-stability-1`。顶部心情在自然心声提取偶发失败时不再把上一条已经确认的心声清空或隐藏；只有取得新的有效心声后才替换显示内容。失败时间仍保留用于诊断，不制造固定心情、不把旧心情声称为本轮新结果。网页与私人候选均已同步这一小段共享业务修复，但按用户要求本轮先只提交、推送公开网页；私人候选不推送、不打包，等待网页实际表现确认后再单独发布。Windows 自动测试不能代替真实模型偶发失败及移动端聊天过程验收。

当前网页候选基线：2026-09-02，网页核心 v1148，缓存身份为 `v1148-android-startup-route-1`。公开网页入口移除会让旧版华为／QQ Android 内核整份脚本解析失败的可选链、空值合并和正则后向断言，并为 `flatMap`、`matchAll` 提供兼容回退；IndexedDB 打开增加 7 秒终止、一次短重试及空白写入保护，存档暂不可读时明确停止启动，不把空白状态写回原数据。模型路线诊断现在区分“设置页默认路线”和“角色独立路线”，可在诊断页直接选择跟随默认或路线一至四；切换后清除旧诊断，未发生新请求前明确显示“当前路线尚未发起新请求”，不再把切换前的路线三记录冒充当前结果。本轮只修改并发布公开网页，不同步私人 `PhoneWeb.bundle`、不制作私人安装包。Windows 针对性 25 项及全仓 1337 项自动测试通过；受影响安卓用户的原浏览器存档恢复、真实模型请求和线上 Service Worker 换版仍需本人设备验收。

当前候选基线：2026-08-31，网页核心 v1119／私人 iOS 1.0.240 (240)，原生桥契约 25。“App 图标美化”已补齐规则怪谈与惊悚抉择的独立上传、更换和复位入口，网页与私人内置页面保持相同清单并更新缓存身份。v1118 的主屏原生翻页与长按拖动边界、实际角色锁定标题、顶部心情入口、用户确认婚书日期、朋友圈/X 原位重新生成、无生图配置时的图文发布，以及外卖设备状态超时容错全部保留。v1117 的模型输出与存储稳定边界、v1116 的角色通话循环视频与自动记忆证据校验、v1115 的角色独立语音路线、v1111 已真机确认的微信／共同生活双向连续性及更早稳定边界全部保留。网页与私人完整 `PhoneWeb.bundle` 的业务核心逐字一致；Windows 自动回归不能代替 Mac 编译签名、真实 iPhone、真实模型、外卖云数据库恢复或真实支付链路验收。

当前候选基线：2026-08-28，网页核心 v1098／私人 iOS 1.0.222 (222)，原生桥契约 25。共同生活新增角色自主双人出行：只有双方已明确目的地、日期和准确未来出发时间时，角色才可生成一笔独立的云程双人订单；订单不写入微信或共同生活角色气泡，只在共同生活显示独立系统提示。行程按真实时间推进待出发、出行中和抵达状态，并作为隐藏结构化事实让角色记得何时出发、去过哪里；重复、同城、时间无效和非共同生活入口均不建单、不扣款。旧云程、一次性约会、共同生活总结、查手机和消息路线保持原边界。Windows 自动回归和包结构校验不能代替 Mac 编译签名及真实 iPhone/真实模型链路验收。

当前候选基线：2026-08-28，网页核心 v1097／私人 iOS 1.0.221 (221)，原生桥契约 25。普通微信、后台主动消息、普通通话和共同生活回复会在同一次真实模型调用中携带最近跨渠道时间线与当前进度约束，避免把假设当事实、把已完成事项退回未完成状态或无依据改口；关闭线上／共同生活同步时仍严格隔离两侧上下文。上下文设置扩大为 2–100 回合并明确标注最高 100：一条用户消息及其全部角色回复气泡算一回合，一整段语音或视频通话算一回合；长通话仍受原有容量保护。未新增模型调用、固定角色台词或静默丢弃。Windows 自动回归不能代替 Mac 编译签名、真实 iPhone 和真实模型链路验收。

当前候选基线：2026-08-28，网页核心 v1096／私人 iOS 1.0.221 (221)，原生桥契约 25。视频导入音乐恢复为保留原始可播放媒体容器，并在接受文件与写入后分别使用真实媒体探针确认可播放，拒绝损坏或当前设备不支持的文件；已完成的生图必须先持久写入聊天记录，写入异常也保留已生成图片且不得自动再次调用模型；网页版可手动读取原有网页云备份或私人镜像，但自动同步不得覆盖任何尚未人工读取的现有云记录；微信自动总结按新增用户回合达到设定轮数即触发，同一联系人同一账号并发只允许一次真实模型调用，失败不推进游标以便后续重试。Windows 自动回归不能代替 Mac 编译签名、真实 iPhone 媒体播放及真实云端账号验收。

当前候选基线：2026-08-27，网页核心 v1095／私人 iOS 1.0.220 (220)，原生桥契约 25。微信登录退出完成必须与远程操控结束共用可靠功能事件队列；退出后的模型回复不得再次消费退出前那条“登录微信／改备注”请求，否则会在可见气泡落盘前误启动第二次检查并把无气泡错误记为成功。完成轮若意外带出登录或远控标签，只剥离操作标签并保留真实角色话；最终仍以聊天记录确实新增可见 assistant 消息作为成功条件。Windows 自动回归和包结构校验不能代替 Mac 编译签名及真实 iPhone 验收。

当前候选基线：2026-08-27，网页核心 v1094／私人 iOS 1.0.219 (219)，原生桥契约 25。线上微信与共同生活顶部状态必须在同一轮对齐：旧状态仍为上班、外出或回家路上时，角色真实输出家中房间位置标签，或用户明确提出一起去家中房间且角色本轮明确带、抱、牵、陪过去，均先完成到家再写入位置。询问、否定、延后和仍在路上的话不能误判；不新增模型调用或固定角色台词。Windows 自动测试和包结构校验不能代替 Mac 编译签名及真实 iPhone 验收。

当前候选基线：2026-08-27，网页核心 v1090／私人 iOS 1.0.215 (215)，原生桥契约 25。门店和具体商品交给角色选择的自然外卖请求仍由角色本人决定；角色一旦明确答应去找或去点，同一授权回合必须生成具体门店、具体商品并保留用户明确规格。首次动作无效只允许一次严格纠正，仍失败即明确停止且不连接后台。浏览器界面级验证只使用假后端，没有真实订单或付款；Windows 自动测试不能代替 Mac 编译签名或真实 iPhone 验收。

上一候选基线：2026-08-27，网页核心 v1089／私人 iOS 1.0.214 (214)，原生桥契约 25。真人群聊退出函数已部署到群聊实际连接的业务 Supabase 项目；前台与后台模型从最近四小时真实微信、通话和共同生活消息沿用吃饭进度，已知用户去吃饭后只能询问下一步，不能退回重问是否吃饭。该规则补充模型上下文，不机械删除回复、不制造固定角色台词。Windows 自动测试和 RPC 无副作用探针不代表 Mac 编译、签名或真实成员账号退出验收。

更早候选基线：2026-08-27，网页核心 v1088／私人 iOS 1.0.213 (213)，原生桥契约 25。本版撤回 v1084 新增的伴生快照累计时长自动补锁，手动解锁后不会再被下一轮同步重新锁回；修改单个 App 限额时先清除该 App 的旧达标锁，再按新分钟数重新监控，其他 App 不受影响。v1087 的后台主动消息、通话暂停生成与解锁通知去重必达逻辑继续保留。Windows 自动测试只代表源码回归，Mac 编译、签名、真实 Screen Time 授权与真机系统屏蔽页仍待本包实测。

当前候选基线：2026-08-27，网页核心 v1087／私人 iOS 1.0.212 (212)，原生桥契约 25。本版保留 v1086 的私人 App 降载回退与图片引用修复，只针对后台快速接管和真实手动解锁补强：前台已显示的完全相同整组消息不再因服务器任务标签不同而重放；每个真实解锁事件仍按唯一 ID 处理并必须产生可见回复，模型首答若沉默、第三人称或复读则不进行第二次纠正调用，而以本次真实解锁事实给出不同的直接回复。后台转场后的快速接管保持不变，通话期间普通后台主动任务仍在生成前暂停。Windows 自动测试只代表源码回归，Mac 编译、签名、APNs 与真实 iPhone 解锁前后台链路仍待本包实测。

上一候选基线：2026-08-27，网页核心 v1086／私人 iOS 1.0.211 (211)，原生桥契约 25。本版只选择性撤销 v1085 中真机没有改善且造成可见图片长期问号的私人 App 降载策略：图片内存预算恢复 48M 字符、启动关键图片集合与可见图片每批 12 张恢复，非关键业务扫描不再被 v1085 新增的压力窗口暂停；WebContent 终止计数恢复为 Coordinator 内单次窗口，并在 5 秒后只尝试一次精确 Bundle 恢复。保留私人壳绕开昂贵 Intl 的安全边界、微信手动总结、伴生与解锁视角、后台通知、普通回复、朋友圈和语音线路；同时保留微信引用栏“×”取消引用修复。Windows 自动测试只代表源码回归，Mac 编译、签名以及真实 iPhone 的图片、卡顿、温度和白屏恢复仍待本包实测。

当前候选基线：2026-08-27，网页核心 v1085／私人 iOS 1.0.210 (210)，原生桥契约 25。私人 App 只预载当前必要图片，可见图片每批四张并在 WebContent 压力窗口延后解码；压力期间暂停非关键扫描但保留微信回复、服务器消息拉取和后台通知。WebContent 终止记录跨 Coordinator 持久化，两分钟内仅第一次在 15 秒后自动恢复，避免白屏重载风暴。微信对话总结新增手动总结：只读取尚未总结的新微信，无新内容与重复点击均不产生额外模型调用，电话和系统行不会混入。微信登录完成后即使内容未变化也会给出一轮真实角色反应，但不得重复旧话题或只汇报改备注；手动解锁事件新增当前聊天对象视角守卫。Windows 1111 项全量自动回归通过；Mac 编译、签名和真实 iPhone 的性能、温度、白屏恢复及功能验收仍须单独完成。

当前候选基线：2026-08-27，网页核心 v1084／私人 iOS 1.0.209 (209)，原生桥契约 25。前台微信回复不再提前创建第二条服务器模型任务；只有 App 真正退后台才启用回复接力，服务器在生成前和发送前都会复核该回合是否已经由前台完成，客户端也会丢弃与刚显示内容完全相同的旧接力。通话期间普通后台主动任务不生成，挂断后重新计算久未回复；共同生活期间伴生消息继续畅通，并按最后一条用户消息所在渠道送达。每次角色回复均使用最新后台真实时间，但不强迫每句话提时间。真实睡眠只取最近七天 HealthKit 的最新睡眠段并去除重叠，通话陪睡间隔不冒充设备睡眠；每日限额达到后及时补锁，红色显示“今日限额已达到”，与角色或手动锁定分开，旧日缓存不能触发今日补锁。v1081 自然点单、KFC 首页套餐以及后续红包兑换、订单回执、图文照片卡和屏幕共享历史继续保留；九珍/三柠真实规格仍待营业后验收。Windows 1105 项全量自动回归、外卖服务 154 项、浏览器实页与 ZIP 校验通过；Mac 编译签名和真机验收仍须单独记录。

当前候选基线：2026-08-21，网页核心 v1018／私人 iOS 1.0.139 (139)，原生桥契约 25。v1018 在 v1009 已有 iOS 合成层和原生写盘降压之外，继续修复私人 App 点击后的 JavaScript 主线程阻塞：大聊天归档使用有界结构指纹并在空闲时保存，前台恢复同步合并防抖；原生屏幕共享未启用时降低轮询，DeviceActivityReport 只在真实读取窗口挂载。Windows 906 项自动回归通过；Mac 编译、签名和真实 iPhone 温度、触摸、长聊天保存、Screen Time 与屏幕共享仍待验证。

当前维护基线：2026-08-18，网页核心 v978／私人 iOS 1.0.100 (100)，原生桥契约 23。用户本轮明确要求“全部锁定／解除全锁”且角色给出同方向控制标签时，即使模型错误标为“仅内置”，执行层仍按用户真实请求升级为全部已授权内置 App 加真实 iPhone 当前全部已选 App；角色拒绝、反方向动作、问句和条件句不会误触发。通话记录删除或编辑单句后保留展开状态与阅读位置。组件透明度偏好以更高选择器优先级覆盖四套图标主题固定玻璃值，私人 App 第二页实体底色也读取同一透明度；未自定义时仍保留设备兼容默认底色。Windows 自动回归通过后，仍需在 Mac 编译、签名并用真实 iPhone验收全锁回执、通话记录和透明度渲染。

本目录中的四份 Word 文档是项目的长期记录，不是临时参考资料。

## 开发前

在分析问题或修改代码前，先完整阅读并理解：

1. `AI开发项目_项目说明文档.docx`
2. `AI开发项目_Bug修改规范.docx`
3. `AI开发项目_Bug记录模板.docx`
4. `AI开发项目_新聊天启动说明.docx`

遇到相似问题时，先检索历史记录，优先采用已经验证有效的方案，不重复已有失败尝试。

## 开发后

- 新增功能、修改方案或形成重要决策：更新《项目说明文档》。
- 遇到 Bug、完成原因排查、形成解决方案或出现失败尝试：追加到《Bug记录模板》，不得删除历史记录。
- 发现新的开发规则或容易重复踩坑的问题：更新《Bug修改规范》。
- 每次修改核心功能后：主动检查上述三份文档是否需要同步更新。

《新聊天启动说明》用于确保新的开发会话按同一套顺序读取资料和处理问题。

当前维护基线：2026-08-11，网页核心 v894／私人 iOS 1.0.19 (19)，原生桥契约 11。本轮补齐角色真实数据自然语言触发与防假读取，恢复网页版微信／朋友圈／抖音等经典逐项查岗横幅，并将本机恢复快照从“完整度冻结”改为“当前滚动快照 + 历史完整快照”；网页没有加入 APP 专属锁定、HealthKit、Screen Time 或 APNs 能力。共同生活仍执行会话停止后随机 30–60 分钟静默及睡眠／忙碌语义。Mac 编译、签名、服务端部署和真机验收仍必须另行完成。

当前维护基线：2026-08-11，网页核心 v895／私人 iOS 1.0.20 (20)，原生桥契约 11。v895 修复彻底清空后本地大聊天归档、共同生活摘要和服务端主动联系上下文重新灌回旧记忆的问题，并把网页版大容量存档 IndexedDB 升级为可修复、可重试写入。私人 App 的真实读取改为逐项列出电量、屏幕总时长、逐 App、步数、睡眠、心率、心电、HRV 和位置：HealthKit 在本次明确读取时真正启用；Screen Time 在中国等不能取得 `approvedWithDataAccess` 的地区改由既有 DeviceActivityReport 扩展回传。远端迁移 003–006 和 `phone-role-push` 已部署，补齐统一控制器 RPC、旧参数兼容、全天随机主动联系和服务端记忆清空。网页运行时没有加入锁定、HealthKit、Screen Time 或 APNs 能力；这些路径均以本机原生桥存在为硬门槛。Windows 472 项自动测试通过；Mac 五 Target 编译、签名及真机读取／前后台／强退通知仍需验收。

当前维护基线：2026-08-12，网页核心 v896／私人 iOS 1.0.21 (21)，原生桥契约 11。读取全部以用户指令为不可降级范围，原生端只有在各请求读取器均得到成功、不可用或超时终态后才写 `readComplete` 完成回执；角色必须核对同一轮 `readSessionId`、全部范围和新鲜完成时间，再逐项报告九类结果。后台核验缺列的真实原因是远端漏装前置迁移 002，却已存在依赖这些列的后续统一控制器函数；002 已实际应用并核验四个 controller 字段，远端 002–006 均已登记。Windows 475 项自动测试通过；Mac 五 Target 编译、签名及一次全量读取、前后台／强退通知仍需真机验收。

当前维护基线：2026-08-12，网页核心 v898／私人 iOS 1.0.23 (23)，原生桥契约 11。v897 仍会漏过“查一次所有的”这类自然说法：解析器没有得到“全部数据”焦点，早期 nativeOnly 门禁退出，普通模型先生成；后置事实守卫虽又启动真实读取，却仍放行了非数据句，形成两个回复通道。v898 改为聊天、通话、共同生活共用全局读取状态和生成代次：读取排队时立即变更代次；任何普通回复在调用模型前、模型返回后和实际发送前都要复核，旧代次输出永久作废。完成回执是唯一放行入口，电话也必须等待完成回复真正说完才释放通道。不可用或超时只保留在内部诊断，不进入角色气泡。Windows 476 项自动测试全部通过；Mac 五 Target 编译、签名及真机一次全量读取仍需验收。
当前维护基线：2026-08-12，网页核心 v899／私人 iOS 1.0.24 (24)，原生桥契约 11。v898 的读取门禁虽能压住旧回复，但手动回复包装器会把“门禁故意没有产生气泡”误判为模型空输出，自动重试并显示突兀提示；读取完成路径仍可能用程序化逐项汇总替换模型话术。v899 以读取生成代次识别这类正常静默并禁止重试提示，同时删除固定汇总后备，只允许角色模型基于真实事实生成，安全校验失败时最多重新生成两次，仍失败则不发送。线下回复失败新增可诊断原因映射，分别说明余额／额度、鉴权、权限、限流、模型或接口配置、网络超时以及返回空内容，并保证原对话不变。Windows 478 项自动测试全部通过；Mac 五 Target 编译、签名及真机验收仍需完成。

当前维护基线：2026-08-12，网页核心 v900／私人 iOS 1.0.24 (24)，原生桥契约 11。聊天与 TTS 原本已分开，但主聊天、辅助模型的自由文本缺少用途校验，用户可能把 speech-2.8-hd 等语音模型送入 /chat/completions。v900 在保存、模型测试和最终 chatAPI 请求前三层拒绝 speech／tts 专用模型；旧错误路线不会被删除，只在启用或调用时提示改填“语音模型 / TTS”。正常聊天路线、共同生活固定路线和 TTS 生成链保持不变。Windows 479 项自动测试全部通过；本轮未修改私人 iOS 原生代码。

当前维护基线：2026-08-12，网页核心 v901／私人 iOS 1.0.25 (25)，原生桥契约 11。苹果兼容开关现在同时覆盖私人 App：仅开关开启时把远程控制顶部横幅向下移动 14px，关闭后保持 v900 原位，Android 不受影响。视频通话新增右下角小型真实摄像头开关、前后镜头翻转、按设置间隔自动识图、每次通话识图上限，以及“你看一下／你看看这个东西”等口头触发；摄像头关闭或通话结束立即停流，画面只以单帧送入既有视觉模型，不写存档。主动消息线上三参数 RPC 已部署并确认保留到期任务；分钟 cron 过去 24 小时 1440/1440 成功。原两条生成线路分别因余额不足和欠费不可用，现已接入 MiniMax OpenAI 兼容第三备用，受控在线测试连续两次完成“到期认领→正文生成→outbox→APNs sent”。Windows 483/483 自动测试通过。Mac 五 Target 编译、相机和苹果横幅真机检查，以及前台／后台／上划退出三种通知可见性仍需真实 iPhone 验收；电子结婚证按需求门槛暂未开始编码。

当前维护基线：2026-08-12，网页核心 v902／私人 iOS 1.0.26 (26)，原生桥契约 11。视频通话画面回复改为独立高优先事件并隔离旧用户话语；角色第一句必须点出当前画面的具体可见细节，不合格会自动重说。自动间隔改为分钟，旧秒数自动换算；单次次数上限只限制定时自动识别，用户口头要求看画面时每次重新截帧且不限次数。中央识别系统提示已删除，右下角仅显示工作转圈或失败文字。Windows 483/483 自动测试通过；Mac 编译与真实 iPhone 摄像头、翻转、分钟定时、主动不限次、语音回应仍需验收。

当前维护基线：2026-08-12，网页核心 v903／私人 iOS 1.0.27 (27)，原生桥契约 11。v902 画面隔离错误地产生纯 system 请求，部分 OpenAI 兼容聊天接口拒绝并显示上游异常；v903 只把当前画面恢复为本轮唯一 user 事件，旧对话仍隔离，正常聊天与视觉接口不变。画面回答失败不再进入中央字幕，仅右下角显示“失败”。

v903 授权补充：第一次正常邀请码保留；生成迁移码、生成备用恢复码、登录页代码恢复入口和客户端调用已移除，旧版本调用相关云端动作会返回 `biometric-required`。设备恢复及 Safari／Edge 授权合并只允许本人扫脸或指纹。

当前维护基线：2026-08-12，网页核心 v904／私人 iOS 1.0.28 (28)，原生桥契约 11。视频摄像头自动识图间隔 0 现在会立即清理当前通话定时器，并在旧定时回调和自动识图入口双重拦截；新用户默认 0，口头要求角色看画面仍每次重新截帧且不限次数。后台主动消息每个气泡使用独立通知标识，通知中心直接显示角色昵称与该句正文，不再折叠成“多个通知”。头像同步会在内存缓存缺失时重新读取 IndexedDB／私人本机大容量图片，再上传真实缩略图；通知扩展等待通信意图捐赠完成后更新内容。苹果系统强制通信头像为圆形并显示 App 来源小图标，无法改成方形或移除。Windows 完整回归 483/483 通过；仍需 Mac 五 Target 编译及真实 iPhone 的 0 间隔、口头识图、头像和逐句通知验收。

当前维护基线：2026-08-13，网页核心 v907／私人 iOS 1.0.31 (31)，原生桥契约 11。显式一分钟测试、App立即查看、回复接力和设备读取接力不再被页面退后台后的瞬时状态取消，不同任务种类互不覆盖；聊天回复先持久化再撤销服务器兜底。Service Worker 已删除对旧迁移码函数的错误完整性依赖，解决多人长期停在 v901。语音翻译框可独立配色，角色气泡支持单条删除；宠物每30天进化、可退化，睡眠回到窝内，“让TA照顾”可正常展示动作。远端迁移 202608130002 已应用登记，phone-role-push 已部署。Windows 完整回归 499/499 通过；Mac 五 Target 编译与真实 iPhone 通知、Screen Time、锁定仍需验收。


2026-09-06 更新：网页/私人 v1196，iOS 1.0.318 (318)，原生桥 35。缺失或无效内心不再追加模型请求，保留上一有效内心并正常发送正文；常见格式变体本地兼容。Windows 1519/1519 自动测试通过，网页/私人六例完整回复均一次模拟模型调用。截图标准内心可合法解析，不以通用拦截记录推断格式错误。Mac 编译及真实 iPhone 尚未验证；本轮 DOCX 追加内容与历史保留已核对，当前 Windows 缺少 LibreOffice，未完成视觉渲染检查。


2026-09-06 · v1197 共同生活设置内模型与路线诊断。两边共有：网页与私人内置页 v1197，私人 iOS 1.0.319 (319)，原生桥 35。用户确认调大模型回复长度后恢复，并要求在共同生活设置内部增加诊断入口。本轮只加入诊断，不修改长度限制、配角解析、重试或模型选择规则。 入口只在共同生活设置内部，独立记录主角/配角真实请求元数据，打开不请求模型，不记录正文/Key/地址。Windows1525/1525通过；网页和私人真实入口的模拟HTTP双角色流程及DOM检查通过。Mac编译/真机未验证。维护文档追加并核对历史保留，当前Windows无LibreOffice，未做DOCX视觉渲染。


2026-09-06 · v1198 线上与共同生活请求规模详情。两边共有：网页与私人内置页 v1198，私人 iOS 1.0.320 (320)，原生桥 35。用户要求在线上、线下模型路线诊断的请求规模下增加查看详情，显示各项占用，并打包私人版和推送网页。新增 request-size-details.js，复用原 wechatRequestStats 返回总数，附加纯数值分项；不改变请求内容、模型路线、历史范围、长度或重试。 分项合计严格等于请求总数，默认折叠，打开零模型调用。1531/1531测试及网页/私人双场景浏览器流程通过；Mac编译、真机未验证。维护DOCX追加历史已核对；Windows无LibreOffice，未完成视觉渲染。


2026-09-06 v1199 外卖补判误触发及私人诊断归因。两边共有：网页与私人内置网页 v1199，私人 iOS 1.0.321 (321)，原生桥 35。用户确认本轮原话是“忘了咬你”，上一句牛肉面有香菜。原代码用用户原话加角色前200字匹配外卖关键词，截图中的“下次点面”可触发隐藏动作补判；隐藏内心及记住标签里的点餐字样也能触发。旧函数已用截图原文复现，不把标准标签当格式错误。私人旧拦截覆盖层还会无条件收录所有非最后候选，即使原聊天正文已完整送达；截图“被本机处理替代”与该覆盖层一致。 修改仅限缺动作补判入口：普通关键词只扫描本轮用户话语；角色自主承诺的补判先排除内心/心情/记住/外卖记忆标签及未来、历史、假设语句，需当前可见点餐承诺或现有明确点餐、委托选择、订单澄清条件。实际订单执行、授权、金额及付款结果校验均保持。两边外卖辅助请求带用途元数据；私人诊断按完整可见正文判断，不因后续辅助调用就把已送达正文判为拦截。辅助响应仍可查看，但标为外卖动作补判，不称为聊天格式错误。真正截断或替换的候选继续保留。没有关闭整体校验，没有编造角色台词，也没有改动自动修复总上限。 Windows全仓1544/1544通过。浏览器实际网页及私人内置入口六组模拟HTTP流程：用户原话加截图回复各一次模型调用、四条正文送达；标准内心/记忆各一次调用；当前真实点餐承诺仍可进行第二次补判，私人只记录辅助结果且明确用途。打开诊断没有追加模型调用。保留有效内心及顶部栏框，未改共同生活、监管、请求规模详情及私人键盘。首次完整测试1543通过、1失败为安装指南路径期待旧名称，更新指南引用后通过；一次短记忆测试出现本地记忆不采纳记录，仍仅一次模型调用，不当作额外请求或改坏事实校验。 此为本地代码复现与模拟HTTP验证，未取得用户上游调用日志，不保证所有其他校验场景都只有一次请求。私人包是Mac待编译源码候选，未Mac编译、签名或真实iPhone验证。新版本只从干净提交归档，不覆盖旧包；网页按授权推送一次，线上生效另行核实。维护资料仅追加，历史保留。Windows没有LibreOffice，DOCX段落及历史保留已复查，视觉渲染未完成。


2026-09-06 v1200 前后台回复接力互斥。两边共有：网页与私人内置网页 v1200，私人 iOS 1.0.322 (322)，原生桥 35。用户报告退后台约一两分钟后先收到标为后台的回复，数秒后同一用户消息又被近似回答。用实际网页与私人内置入口、模拟模型 HTTP 和后台回包复现：后台先入聊，本地旧请求继续发送。此为代码路径复现，用户现场具体 triggerKind 尚未取得，不把泛称后台主动消息等同 scheduled。 修复仅关联普通微信 reply_handoff 与原用户消息 ID。本地模型等待不占用结果锁；真正开始处理候选或后台结果时取得同轮归属，完成标记存于原用户消息。后台先完成阻止本地旧结果，本地先完成阻止后台改写结果；不按文字相似度删除。正常正文入聊标注回复源。失败或空输出释放归属，保留接力；保存失败不确认收件、不发布完成活动和不取消后台，已暂存后台结果可再次保存。排队旧轮次在开始前核对完成标记，手动继续聊仍可用；不同消息、账号、角色互不取消。 新增 phone_role_push_pull 只读返回 replyToMessageId/replyToAccountId，按已有 task:<id> 关联同 target、role 的 reply_handoff 任务；不改数据表与模型策略。新增 phone_role_background_complete_turn 在原所有者鉴权下仅取消对应消息 ID 的主账号接力，保留新消息与设备任务。202609060001_reply_handoff_source_identity 已独立事务部署并登记，未重放旧迁移、未重新部署其他云函数；正式接口再做事务回滚数据验证，来源关联、取消范围、拒绝错误鉴权通过，测试记录残留0。 Windows全仓1562/1562通过。实际双入口十组浏览器模拟HTTP验证覆盖后台先回、本地先回、保存失败再收件、独立scheduled不被接管、旧后台到达时新的用户回复继续；另外覆盖空结果回退、账号隔离、相同文字不同消息、持久化后重新建运行时、队列旧轮与继续聊。首次扩大回归出现的失败来自隔离测试未加载新依赖或期待旧的直接push/finally文本，已更新实际辅助函数与断言并通过；不以修改预期掩盖行为失败。保留外卖降耗、请求规模明细、心情、监管、共同生活与私人键盘适配。 已发往上游的旧模型请求可能继续计费，不能宣称消除全部双请求费用；已送达的系统通知也不能保证撤回。身份不明的历史回包和scheduled/device等独立事件沿用原流程，未用模糊去重扩大拦截。私人包只为干净提交的Mac待编译源码候选，未Mac编译、签名和真实iPhone验证。维护文档仅追加并核对历史保留；本机无LibreOffice，DOCX视觉渲染尚未完成。
