# v1195 私人源码候选安装说明

本次属于两边共有。网页源码与私人内置网页均为 v1195；私人 iOS 1.0.317 (317)，原生桥 35。网页按用户授权推送，具体状态以 Git 结果为准。此包是 Mac 待编译源码候选，不是 IPA，尚未通过 Mac 编译签名或真实 iPhone 验收。

- 聊天监管保持10–60秒，软件保持20–60秒；只有实际触发才加一次，短暂进入不计数。旧打开次数一次性重置，新规则每日当地凌晨清零。
- 监管标准内心标签正常解析，角色只知道本日第几次发现，不知道未触发的打开次数；失败不伪造、不自动重发。
- 顶部心情错误格式保留上一条有效内容，重载后仍可读；历史彻底缺失只显示省略号，开启时不隐藏栏框。
- 共同生活后台恢复后，跨后台仍未完成的模型请求会退出等待并释放按钮。迟到结果不落入旧对话，不自动重试，需要重新点击“让TA回”。多人剧场提前失败也释放忙碌状态。
- 保留私人原生适配、v1189共同生活键盘坐标与旁白点击保护。不要先删除原 App；不要解压覆盖旧工程目录，使用全新目录。

在Mac打开 PhoneCompanionTest.xcodeproj，核对 Team、签名、设备及所有Target版本后编译覆盖安装。确认App内显示1.0.317 (317)和网页v1195，再逐项真机验证：短停留不计数、首次触发为1、第二次为2；错误内心与重开开关不隐藏栏；点回复等待时切后台再回来后可重新点击，旧回复不得重复出现。Windows测试与源码ZIP不能代替以上真机验收。
