# v1200 回复接力互斥

两边共有：网页源码与私人内置网页均为 v1200；私人 iOS 1.0.322 (322)，原生桥 35。网页按用户授权推送，失败只尝试一次。当前候选：网页源码与私人内置网页 v1200。

普通微信回复与后台回复接力按原用户消息 ID 关联。后台先送达后，本地旧模型结果不再重复进入聊天；本地先完成后，同轮后台改写结果不再重复入聊。保存失败不确认收件，允许再次拉取和保存。旧轮结束只取消对应消息的后台任务，不取消新消息或设备任务。随机主动消息、设备事件、共同生活及身份不明的旧回包保持原有流程；不通过相似文字删消息。

服务端需要本轮 202609060001_reply_handoff_source_identity.sql 的回包关联字段和精确取消接口；服务端已部署并登记，正式接口的回滚验证已通过。保留 v1199 外卖补判降耗、请求规模详情、顶部心情、监管及私人键盘适配。

不要先删除原 App，不要解压覆盖旧工程目录。Mac 使用新目录编译并检查 Team、签名及各 Target 版本。此包是 Mac 待编译源码候选，不是 IPA；未完成 Mac 编译、签名或真实 iPhone 验收。
