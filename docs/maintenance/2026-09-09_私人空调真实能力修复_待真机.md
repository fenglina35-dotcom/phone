# 私人空调真实能力修复（待真机）

- 日期：2026-09-09
- 范围：仅私人版 iPhone 的 HomeKit 空调页与角色空调控制；未修改网页版本、卧室小灯或门锁。
- 现象：ACN1-AIR 可开关、制冷和制热，但自动模式不执行；目标温度被误判为不可控；三级风速从 100% 减小时错误写入 99%，回读仍为 100%。
- 根因：温控特征可能位于同一配件的关联服务或制冷/制热阈值特征中；页面把模式与风速当作连续的通用能力，未按 HomeKit 实际元数据展示与写入。
- 修复：跨关联服务读取目标温度，并兼容制冷/制热阈值；模式按设备上报能力展示，ACN1-AIR 不再提供已证实不可执行的自动模式；风速按设备离散档位调节；所有写入必须精确回读确认。
- 角色状态：角色提示中提供最近一次 HomeKit 真实快照，包括开关、模式、设定温度、室温、风速和读取时间；设备未提供的值明确标为不可控制或未提供，不使用猜测值。
- 自动刷新：进入页面、回到前台、原生桥就绪时读取，并在页面可见期间每 20 秒刷新一次。
- 回归：旧实现新增用例 5 项失败；修复后专项 18/18 通过，仓库测试 1752/1752 通过，授权聊天检查通过。
- 尚未确认：Windows 无法编译 HomeKit Swift，也无法代替真实 iPhone 验证 ACN1-AIR 的温度阈值和三级风速写入；必须由下一次 Mac/Xcode 新版本安装后实测。
- 文档说明：两份既有 DOCX 维护文档当前含有另一项尚未提交的修改，为避免覆盖或混入他人工作，本次先使用独立记录，待对应任务完成后再合并。
